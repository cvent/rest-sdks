{
  "name": "root",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "sdk:generate": "pnpm run sdk:sync-versions && pnpm run sdk:generate:all",
    "sdk:generate:all": "pnpm run sdk:generate:typescript && pnpm run sdk:generate:java && pnpm run sdk:generate:csharp",
    "sdk:generate:csharp": "speakeasy run -t public-csharp --skip-versioning",
    "sdk:generate:java": "speakeasy run -t public-java --skip-versioning",
    "sdk:generate:typescript": "speakeasy run -t public-typescript --skip-versioning"
  }
}