/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { venueProfilesGetVenueDetailsOverview } from "../funcs/venueProfilesGetVenueDetailsOverview.js";
import { venueProfilesPatchVenueDetails } from "../funcs/venueProfilesPatchVenueDetails.js";
import { venueProfilesPatchVenueFacility } from "../funcs/venueProfilesPatchVenueFacility.js";
import { venueProfilesUpdateVenueDetails } from "../funcs/venueProfilesUpdateVenueDetails.js";
import { venueProfilesUpdateVenueFacility } from "../funcs/venueProfilesUpdateVenueFacility.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class VenueProfiles extends ClientSDK {
  /**
   * Update Venue Details
   *
   * @remarks
   * Update venue details for the specified venue, replacing the existing venue details data.
   */
  async updateVenueDetails(
    request: operations.UpdateVenueDetailsRequest,
    options?: RequestOptions,
  ): Promise<components.ExistingVenueDetails> {
    return unwrapAsync(venueProfilesUpdateVenueDetails(
      this,
      request,
      options,
    ));
  }

  /**
   * Patch Venue Details
   *
   * @remarks
   * Partially updates the specified venue's details using JSON Merge Patch, as defined in [RFC 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
   *
   * - Only fields present in the request body are updated; omitted fields remain unchanged.
   * - Set a field to `null` to unset it.
   * - If any part of the patch fails, the entire request will fail.
   *
   * **Array Handling:**
   * - Arrays (`locationTypes`) are treated as **full replacements**, not partial updates.
   * - If an array property is not included in the payload, no updates are made to that field.
   * - If an empty array is sent, it replaces the existing array with an empty array.
   */
  async patchVenueDetails(
    request: operations.PatchVenueDetailsRequest,
    options?: RequestOptions,
  ): Promise<components.VenueDetailsPatch> {
    return unwrapAsync(venueProfilesPatchVenueDetails(
      this,
      request,
      options,
    ));
  }

  /**
   * Get Venue Details Overview
   *
   * @remarks
   * Retrieve an overview of venue details for the specified venue.
   */
  async getVenueDetailsOverview(
    request: operations.GetVenueDetailsOverviewRequest,
    options?: RequestOptions,
  ): Promise<components.VenueDetailsOverview> {
    return unwrapAsync(venueProfilesGetVenueDetailsOverview(
      this,
      request,
      options,
    ));
  }

  /**
   * Update Venue Facility
   *
   * @remarks
   * Update the facility information for the specified venue, replacing all existing values with those provided in the request body.
   *
   * Field applicability varies by venue type:
   * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
   */
  async updateVenueFacility(
    request: operations.UpdateVenueFacilityRequest,
    options?: RequestOptions,
  ): Promise<components.ExistingVenueFacility> {
    return unwrapAsync(venueProfilesUpdateVenueFacility(
      this,
      request,
      options,
    ));
  }

  /**
   * Patch Venue Facility
   *
   * @remarks
   * Partially update the facility information for the specified venue using JSON Merge Patch, as defined in [RFC 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
   *
   * - Only fields present in the request body are updated; omitted fields remain unchanged.
   * - Set a field to `null` to unset it.
   * - If any part of the patch fails, the entire request will fail.
   * - `proximityFromAirport.unit` is required when `proximityFromAirport.distance` is provided.
   *
   * Field applicability varies by venue type:
   * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
   */
  async patchVenueFacility(
    request: operations.PatchVenueFacilityRequest,
    options?: RequestOptions,
  ): Promise<components.VenueFacilityPatch> {
    return unwrapAsync(venueProfilesPatchVenueFacility(
      this,
      request,
      options,
    ));
  }
}
