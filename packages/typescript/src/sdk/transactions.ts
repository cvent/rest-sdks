/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsGetAccountTransactionItem } from "../funcs/transactionsGetAccountTransactionItem.js";
import { transactionsGetAccountTransactionItems } from "../funcs/transactionsGetAccountTransactionItems.js";
import { transactionsGetAccountTransactions } from "../funcs/transactionsGetAccountTransactions.js";
import { transactionsGetTransaction } from "../funcs/transactionsGetTransaction.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";

export class Transactions extends ClientSDK {
  /**
   * List Account Transactions
   *
   * @remarks
   * Gets a paginated list of transactions in the account. Results are returned in ascending `lastModified` order, with a stable internal tie-breaker applied to guarantee a deterministic total order across pages. Ordering is not client-configurable. Newly created or updated transaction records might not appear immediately in query results.
   *
   * @see {@link #oauth2-auth-code-planner-admin} - More about OAuth2 authorization code support for administrators
   */
  async getAccountTransactions(
    request: operations.GetAccountTransactionsRequest,
    options?: RequestOptions,
  ): Promise<
    PageIterator<operations.GetAccountTransactionsResponse, { cursor: string }>
  > {
    return unwrapResultIterator(transactionsGetAccountTransactions(
      this,
      request,
      options,
    ));
  }

  /**
   * Get Transaction
   *
   * @remarks
   * Gets the transaction for a given transactionId. Newly created or updated transaction records might not appear immediately in query results.
   *
   * @see {@link #oauth2-auth-code-planner-admin} - More about OAuth2 authorization code support for administrators
   */
  async getTransaction(
    request: operations.GetTransactionRequest,
    options?: RequestOptions,
  ): Promise<components.TransactionDetailResponse> {
    return unwrapAsync(transactionsGetTransaction(
      this,
      request,
      options,
    ));
  }

  /**
   * List Account Transaction Items
   *
   * @remarks
   * Gets a paginated list of transaction items for a given transactionId in the account. Results are returned in ascending `lastModified` order, with a stable internal tie-breaker applied to guarantee a deterministic total order across pages. Ordering is not client-configurable. Newly created or updated transaction item records might not appear immediately in query results.
   *
   * @see {@link #oauth2-auth-code-planner-admin} - More about OAuth2 authorization code support for administrators
   */
  async getAccountTransactionItems(
    request: operations.GetAccountTransactionItemsRequest,
    options?: RequestOptions,
  ): Promise<
    PageIterator<
      operations.GetAccountTransactionItemsResponse,
      { cursor: string }
    >
  > {
    return unwrapResultIterator(transactionsGetAccountTransactionItems(
      this,
      request,
      options,
    ));
  }

  /**
   * List Transaction Item
   *
   * @remarks
   * Gets a transaction item for a given transactionItemId for a given transactionId in the account. Newly created or updated transaction item records might not appear immediately in query results.
   *
   * @see {@link #oauth2-auth-code-planner-admin} - More about OAuth2 authorization code support for administrators
   */
  async getAccountTransactionItem(
    request: operations.GetAccountTransactionItemRequest,
    options?: RequestOptions,
  ): Promise<components.TransactionItemAccountResponse> {
    return unwrapAsync(transactionsGetAccountTransactionItem(
      this,
      request,
      options,
    ));
  }
}
