/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { dlv } from "./dlv.js";

export interface Env {
  CVENTSDK_CLIENT_ID?: string | undefined;
  CVENTSDK_CLIENT_SECRET?: string | undefined;
  CVENTSDK_TOKEN_URL: string;
  CVENTSDK_SCOPES?: Array<string> | undefined;
  CVENTSDK_O_AUTH2_AUTHORIZATION_CODE?: string | undefined;

  CVENTSDK_DEBUG?: boolean | undefined;
}

export const envSchema: z.ZodType<Env, z.ZodTypeDef, unknown> = z.object({
  CVENTSDK_CLIENT_ID: z.string().optional(),
  CVENTSDK_CLIENT_SECRET: z.string().optional(),
  CVENTSDK_TOKEN_URL: z.string().default(
    "https://api-platform.cvent.com/ea/oauth2/token",
  ),
  CVENTSDK_SCOPES: z.string().transform(v => v ? v.split(",") : []).pipe(
    z.array(z.string()),
  ).optional(),
  CVENTSDK_O_AUTH2_AUTHORIZATION_CODE: z.string().optional(),

  CVENTSDK_DEBUG: z.coerce.boolean().optional(),
});

/**
 * Checks for the existence of the Deno global object to determine the environment.
 * @returns {boolean} True if the runtime is Deno, false otherwise.
 */
function isDeno() {
  if ("Deno" in globalThis) {
    return true;
  }

  return false;
}

let envMemo: Env | undefined = undefined;
/**
 * Reads and validates environment variables.
 */
export function env(): Env {
  if (envMemo) {
    return envMemo;
  }

  let envObject: Record<string, unknown> = {};
  if (isDeno()) {
    envObject = (globalThis as any).Deno?.env?.toObject?.() ?? {};
  } else {
    envObject = dlv(globalThis, "process.env") ?? {};
  }

  envMemo = envSchema.parse(envObject);
  return envMemo;
}

/**
 * Clears the cached env object. Useful for testing with a fresh environment.
 */
export function resetEnv() {
  envMemo = undefined;
}
