/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Oauth2TokenSecurity = {
  username: string;
  password: string;
};

/**
 * The grant type.
 */
export const GrantType = {
  ClientCredentials: "client_credentials",
  AuthorizationCode: "authorization_code",
  RefreshToken: "refresh_token",
} as const;
/**
 * The grant type.
 */
export type GrantType = ClosedEnum<typeof GrantType>;

export type Oauth2TokenRequest = {
  /**
   * The grant type.
   */
  grantType: GrantType;
  /**
   * The OAuth2 Client application ID.
   *
   * @remarks
   *
   * Must be a pre-registered client created in the developer portal.
   */
  clientId: string;
  /**
   * Can be a combination of any scopes associated with a client. Any scope requested must be pre-associated with the client or it will be ignored at runtime.
   *
   * @remarks
   * If the client doesn't request any scopes, the authentication server uses all scopes associated with the client.
   *
   * **Optional** - Only used if the grant_type is client_credentials.
   */
  scope?: string | undefined;
  /**
   * Must be the same *redirect_uri* that was used to get *authorization_code* in * /oauth2/authorize*.
   *
   * @remarks
   *
   * **Required only if grant_type is authorization_code.**
   */
  redirectUri?: string | undefined;
  /**
   * The refresh token.
   *
   * @remarks
   *
   * **Note:** The token endpoint returns *refresh_token* only when the grant_type is *authorization_code*.
   */
  refreshToken?: string | undefined;
  /**
   * The code that is returned from a successful * /oauth2/authorize*.
   *
   * @remarks
   *
   * **Required if grant_type is authorization_code.**
   */
  code?: string | undefined;
};

/**
 * A successful response returning an access token and a possible refresh token.
 */
export type Oauth2TokenResponse = {
  /**
   * The access token contains scopes and groups and is used to grant access to authorized resources.
   */
  accessToken: string;
  /**
   * The refresh token contains the information necessary to obtain a new access token.
   */
  refreshToken?: string | undefined;
  /**
   * The token type.
   */
  tokenType: string;
  /**
   * The lifetime in seconds of the access token.
   */
  expiresIn?: number | undefined;
};

/** @internal */
export type Oauth2TokenSecurity$Outbound = {
  username: string;
  password: string;
};

/** @internal */
export const Oauth2TokenSecurity$outboundSchema: z.ZodType<
  Oauth2TokenSecurity$Outbound,
  z.ZodTypeDef,
  Oauth2TokenSecurity
> = z.object({
  username: z.string(),
  password: z.string(),
});

export function oauth2TokenSecurityToJSON(
  oauth2TokenSecurity: Oauth2TokenSecurity,
): string {
  return JSON.stringify(
    Oauth2TokenSecurity$outboundSchema.parse(oauth2TokenSecurity),
  );
}

/** @internal */
export const GrantType$outboundSchema: z.ZodNativeEnum<typeof GrantType> = z
  .nativeEnum(GrantType);

/** @internal */
export type Oauth2TokenRequest$Outbound = {
  grant_type: string;
  client_id: string;
  scope?: string | undefined;
  redirect_uri?: string | undefined;
  refresh_token?: string | undefined;
  code?: string | undefined;
};

/** @internal */
export const Oauth2TokenRequest$outboundSchema: z.ZodType<
  Oauth2TokenRequest$Outbound,
  z.ZodTypeDef,
  Oauth2TokenRequest
> = z.object({
  grantType: GrantType$outboundSchema,
  clientId: z.string(),
  scope: z.string().optional(),
  redirectUri: z.string().optional(),
  refreshToken: z.string().optional(),
  code: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    grantType: "grant_type",
    clientId: "client_id",
    redirectUri: "redirect_uri",
    refreshToken: "refresh_token",
  });
});

export function oauth2TokenRequestToJSON(
  oauth2TokenRequest: Oauth2TokenRequest,
): string {
  return JSON.stringify(
    Oauth2TokenRequest$outboundSchema.parse(oauth2TokenRequest),
  );
}

/** @internal */
export const Oauth2TokenResponse$inboundSchema: z.ZodType<
  Oauth2TokenResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  access_token: z.string(),
  refresh_token: z.string().optional(),
  token_type: z.string(),
  expires_in: z.number().int().default(3600),
}).transform((v) => {
  return remap$(v, {
    "access_token": "accessToken",
    "refresh_token": "refreshToken",
    "token_type": "tokenType",
    "expires_in": "expiresIn",
  });
});

export function oauth2TokenResponseFromJSON(
  jsonString: string,
): SafeParseResult<Oauth2TokenResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Oauth2TokenResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Oauth2TokenResponse' from JSON`,
  );
}
