/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetSignaturesRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * The sort order passed as a parameter, used to control the order of the
   *
   * @remarks
   * search results as a priority ordered list of sorts
   *
   * There are two orders:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * event.id
   *   * session.id
   *   * attendee.id
   *   * type
   *   * created
   *   * lastModified (default)
   */
  sort?: string | undefined;
  /**
   * A filter string passed in the body of the request, narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * There are six comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *   * greater than: gt
   *   * greater or equal: ge
   *   * less than: lt
   *   * less than or equal: le
   *
   * The following fields are filterable:
   *   * id (eq|ne)
   *   * event.id (eq|ne)
   *   * session.id (eq|ne)
   *   * attendee.id (eq|ne)
   *   * type (eq|ne)
   *   * created (eq|ne|lt|le|gt|ge)
   *   * lastModified (eq|ne|lt|le|gt|ge)
   *
   * The following operators are available:
   *   * and
   *   * or
   */
  filter?: string | undefined;
};

export type GetSignaturesResponse = {
  result: components.SignaturesResponse;
};

/** @internal */
export type GetSignaturesRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  sort?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetSignaturesRequest$outboundSchema: z.ZodType<
  GetSignaturesRequest$Outbound,
  z.ZodTypeDef,
  GetSignaturesRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  sort: z.string().optional(),
  filter: z.string().optional(),
});

export function getSignaturesRequestToJSON(
  getSignaturesRequest: GetSignaturesRequest,
): string {
  return JSON.stringify(
    GetSignaturesRequest$outboundSchema.parse(getSignaturesRequest),
  );
}

/** @internal */
export const GetSignaturesResponse$inboundSchema: z.ZodType<
  GetSignaturesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.SignaturesResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getSignaturesResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetSignaturesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetSignaturesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetSignaturesResponse' from JSON`,
  );
}
