/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetResponsesRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Use filter query parameters to limit results
   *
   * @remarks
   * to data that matches your criteria. See
   * [Filters](/docs/rest-api/reference/filters) for details.
   *
   * Supported fields and operators are listed below:
   *
   * | Field         | Operators   | Notes                                                                                                                                                                        |
   * |---------------|-------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
   * | id            | `eq`, `ne`  |                                                                                                                                                                              |
   * | survey.id     | `eq`        | Use only survey.id to get responses for all questions in the survey.                                                                                                         |
   * | respondent.id | `eq`, `ne`  | Use only respondent.id to get responses for specific respondents.                                                                                                            |
   * | event.id      | `eq`        |                                                                                                                                                                              |
   * | session.id    | `eq`        |                                                                                                                                                                              |
   * | question.id   | `eq`        | Use only question.id to get responses for specific questions, which may belong to one or more surveys. question.id can be combined with other fields by 'and' operator only. |
   * | chapter.id    | `eq`        | Use only chapter.id to get responses for all questions in the chapter of a particular survey.                                                                                |
   * | attempt       | `eq`        |                                                                                                                                                                              |
   *
   * The following logical operators are supported for combining filters:
   * * `and`
   * * `or`
   *
   * **Usage:**
   * 1. chapter.id and survey.id cannot be used in combination with any other fields.
   * 2. Multiple chapter.id or survey.id values must be separated by `or` (not `and`). Example: `filter=chapter.id eq '04ca6ae2-0dc3-487b-953e-86d6abbdf7d3' or chapter.id eq '1983f506-a4a5-4a05-b3f6-ff7f7afc58f1'`
   * 3. question.id can be combined with other fields using `and` operator only.
   */
  filter?: string | undefined;
};

export type GetResponsesResponse = {
  result: components.ResponsesPaginatedResponse;
};

/** @internal */
export type GetResponsesRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetResponsesRequest$outboundSchema: z.ZodType<
  GetResponsesRequest$Outbound,
  z.ZodTypeDef,
  GetResponsesRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getResponsesRequestToJSON(
  getResponsesRequest: GetResponsesRequest,
): string {
  return JSON.stringify(
    GetResponsesRequest$outboundSchema.parse(getResponsesRequest),
  );
}

/** @internal */
export const GetResponsesResponse$inboundSchema: z.ZodType<
  GetResponsesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.ResponsesPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getResponsesResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetResponsesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetResponsesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetResponsesResponse' from JSON`,
  );
}
