/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetEventBudgetTotalsRequest = {
  /**
   * Unique ID of an event.
   */
  id: string;
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter string passed in the body of the request, narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * There is one comparison type that can be used in filter expressions:
   *   * equal: eq
   *
   * The following fields are filterable:
   *    * version.id (eq)
   *    * version.name (eq)
   *    * version.default (eq|ne)
   *
   * The following operators are available:
   *   * and
   *   * or
   */
  filter?: string | undefined;
};

export type GetEventBudgetTotalsResponse = {
  result: components.EventBudgetPaginatedResponse;
};

/** @internal */
export type GetEventBudgetTotalsRequest$Outbound = {
  id: string;
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetEventBudgetTotalsRequest$outboundSchema: z.ZodType<
  GetEventBudgetTotalsRequest$Outbound,
  z.ZodTypeDef,
  GetEventBudgetTotalsRequest
> = z.object({
  id: z.string(),
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getEventBudgetTotalsRequestToJSON(
  getEventBudgetTotalsRequest: GetEventBudgetTotalsRequest,
): string {
  return JSON.stringify(
    GetEventBudgetTotalsRequest$outboundSchema.parse(
      getEventBudgetTotalsRequest,
    ),
  );
}

/** @internal */
export const GetEventBudgetTotalsResponse$inboundSchema: z.ZodType<
  GetEventBudgetTotalsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.EventBudgetPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getEventBudgetTotalsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetEventBudgetTotalsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetEventBudgetTotalsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetEventBudgetTotalsResponse' from JSON`,
  );
}
