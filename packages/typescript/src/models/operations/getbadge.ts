/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetBadgeRequest = {
  /**
   * ID of an event.
   */
  id: string;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * These are the comparison types that can be used in filter expressions:
   * * equal: eq
   *
   * The following fields are filterable:
   * * id (eq)
   * * attendee.id (eq)
   */
  filter?: string | undefined;
};

export type GetBadgeResponse = {
  result: components.BadgePaginatedResponse;
};

/** @internal */
export type GetBadgeRequest$Outbound = {
  id: string;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetBadgeRequest$outboundSchema: z.ZodType<
  GetBadgeRequest$Outbound,
  z.ZodTypeDef,
  GetBadgeRequest
> = z.object({
  id: z.string(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getBadgeRequestToJSON(
  getBadgeRequest: GetBadgeRequest,
): string {
  return JSON.stringify(GetBadgeRequest$outboundSchema.parse(getBadgeRequest));
}

/** @internal */
export const GetBadgeResponse$inboundSchema: z.ZodType<
  GetBadgeResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.BadgePaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getBadgeResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetBadgeResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetBadgeResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetBadgeResponse' from JSON`,
  );
}
