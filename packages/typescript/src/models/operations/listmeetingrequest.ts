/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListMeetingRequestRequest = {
  /**
   * Meeting request form ID.
   */
  id: string;
  /**
   * The maximum number of requests or forms that can be fetched at once.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators. The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * @remarks
   * These are the comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *
   * The following fields are filterable:
   *   * meeting-request.id (eq)
   *   * sourceId (eq)
   *   * status (eq|ne)
   */
  filter?: string | undefined;
};

export type ListMeetingRequestResponse = {
  result: components.MeetingRequestPaginatedResponse;
};

/** @internal */
export type ListMeetingRequestRequest$Outbound = {
  id: string;
  limit: number;
  token?: string | undefined;
  after?: string | undefined;
  before?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const ListMeetingRequestRequest$outboundSchema: z.ZodType<
  ListMeetingRequestRequest$Outbound,
  z.ZodTypeDef,
  ListMeetingRequestRequest
> = z.object({
  id: z.string(),
  limit: z.number().int().default(10),
  token: z.string().optional(),
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  filter: z.string().optional(),
});

export function listMeetingRequestRequestToJSON(
  listMeetingRequestRequest: ListMeetingRequestRequest,
): string {
  return JSON.stringify(
    ListMeetingRequestRequest$outboundSchema.parse(listMeetingRequestRequest),
  );
}

/** @internal */
export const ListMeetingRequestResponse$inboundSchema: z.ZodType<
  ListMeetingRequestResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.MeetingRequestPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listMeetingRequestResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListMeetingRequestResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListMeetingRequestResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListMeetingRequestResponse' from JSON`,
  );
}
