/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetCampaignsRequest = {
  /**
   * Used to query records that have been created before this time point. Defaults to current time.
   */
  before?: string | undefined;
  /**
   * Used to query records that have been created after this time point. Defaults to 6 months back from current time.
   */
  after?: string | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * 'OR' is not supported. Only 'AND' operator is supported.
   *
   * The following comparison types can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *   * greater or equal: ge
   *   * less than or equal: le
   *
   * The following fields are filterable:
   *   * campaign.name (eq|ne)
   *   * campaign.id (eq|ne)
   *   * campaign.active (eq|ne)
   *   * created (eq|le|ge)
   *   * lastModified (eq|le|ge)
   */
  filter?: string | undefined;
};

export type GetCampaignsResponse = {
  result: components.CampaignListPaginatedResponse;
};

/** @internal */
export type GetCampaignsRequest$Outbound = {
  before?: string | undefined;
  after?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetCampaignsRequest$outboundSchema: z.ZodType<
  GetCampaignsRequest$Outbound,
  z.ZodTypeDef,
  GetCampaignsRequest
> = z.object({
  before: z.string().optional(),
  after: z.string().optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getCampaignsRequestToJSON(
  getCampaignsRequest: GetCampaignsRequest,
): string {
  return JSON.stringify(
    GetCampaignsRequest$outboundSchema.parse(getCampaignsRequest),
  );
}

/** @internal */
export const GetCampaignsResponse$inboundSchema: z.ZodType<
  GetCampaignsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.CampaignListPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getCampaignsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetCampaignsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetCampaignsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetCampaignsResponse' from JSON`,
  );
}
