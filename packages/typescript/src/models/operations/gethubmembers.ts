/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetHubMembersRequest = {
  /**
   * The identifier of an Events+ Hub
   */
  hubId: string;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Use filter query parameters to limit results
   *
   * @remarks
   * to data that matches your criteria. See
   * [Filters](/docs/rest-api/reference/filters) for details.
   *
   * Supported fields and operators are listed below:
   *
   * | Field                   | Operators  |
   * |-------------------------|------------|
   * | firstName               | `contains` |
   * | lastName                | `contains` |
   * | emailAddress            | `contains` |
   * | jobTitle                | `contains` |
   * | companyName             | `contains` |
   * | designation             | `contains` |
   * | workAddress.city        | `contains` |
   * | workAddress.state       | `contains` |
   * | workAddress.country     | `contains` |
   *
   * Limit for the number of fields that can be passed in a filter is 35.
   *
   * The following logical operators are supported for combining filters:
   * * and
   * * or
   */
  filter?: string | undefined;
  /**
   * The sort order passed as a parameter, used to control the order of the
   *
   * @remarks
   * search results as a priority ordered list of sorts
   *
   * There are two orders:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * firstName (member first name)
   *   * lastName (member last name)
   *   * emailAddress (member email address)
   *   * jobTitle (member job title)
   *   * designation (Designation of member)
   *   * workAddress.city (Work City of member)
   *   * workAddress.state (Work State of member)
   *   * workAddress.country (Work Country of member)
   *   * companyName (member company)
   *   * registrationDate (member registration date)
   *   * lastLoginDate (member last login date)
   */
  sort?: string | undefined;
};

export type GetHubMembersResponse = {
  result: components.PaginatedExistingMember;
};

/** @internal */
export type GetHubMembersRequest$Outbound = {
  hubId: string;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
  sort?: string | undefined;
};

/** @internal */
export const GetHubMembersRequest$outboundSchema: z.ZodType<
  GetHubMembersRequest$Outbound,
  z.ZodTypeDef,
  GetHubMembersRequest
> = z.object({
  hubId: z.string(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
  sort: z.string().optional(),
});

export function getHubMembersRequestToJSON(
  getHubMembersRequest: GetHubMembersRequest,
): string {
  return JSON.stringify(
    GetHubMembersRequest$outboundSchema.parse(getHubMembersRequest),
  );
}

/** @internal */
export const GetHubMembersResponse$inboundSchema: z.ZodType<
  GetHubMembersResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.PaginatedExistingMember$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getHubMembersResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetHubMembersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetHubMembersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetHubMembersResponse' from JSON`,
  );
}
