/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListTaxesRequest = {
  /**
   * Unique ID of an event.
   */
  id: string;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Use filter query parameters to limit results
   *
   * @remarks
   * to data that matches your criteria. See
   * [Filters](https://developers.cvent.com/docs/rest-api/reference/filters) for details.
   *
   * Supported fields and operators are listed below:
   *
   * | Field        | Operators                          |
   * |--------------|------------------------------------|
   * | id           | `eq`, `in`                         |
   * | active       | `eq`                               |
   * | created      | `eq`, `ne`, `lt`, `le`, `gt`, `ge` |
   * | lastModified | `eq`, `ne`, `lt`, `le`, `gt`, `ge` |
   *
   * The following logical operators are supported for combining filters:
   * * and
   */
  filter?: string | undefined;
};

export type ListTaxesResponse = {
  result: components.TaxesPaginatedResponse;
};

/** @internal */
export type ListTaxesRequest$Outbound = {
  id: string;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const ListTaxesRequest$outboundSchema: z.ZodType<
  ListTaxesRequest$Outbound,
  z.ZodTypeDef,
  ListTaxesRequest
> = z.object({
  id: z.string(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function listTaxesRequestToJSON(
  listTaxesRequest: ListTaxesRequest,
): string {
  return JSON.stringify(
    ListTaxesRequest$outboundSchema.parse(listTaxesRequest),
  );
}

/** @internal */
export const ListTaxesResponse$inboundSchema: z.ZodType<
  ListTaxesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.TaxesPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listTaxesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListTaxesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListTaxesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListTaxesResponse' from JSON`,
  );
}
