/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetSurveyRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Use filter query parameters to limit results
   *
   * @remarks
   * to data that matches your criteria. See
   * [Filters](/docs/rest-api/reference/filters) for details.
   *
   * Supported fields and operators are listed below:
   *
   * | Field | Operators   |
   * |-------|-------------|
   * | id    | `eq`, `ne`  |
   * | type  | `eq`, `ne`  |
   *
   * The following logical operators are supported for combining filters:
   * * `and`
   * * `or`
   */
  filter?: string | undefined;
};

export type GetSurveyResponse = {
  result: components.SurveysPaginatedResponse;
};

/** @internal */
export type GetSurveyRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetSurveyRequest$outboundSchema: z.ZodType<
  GetSurveyRequest$Outbound,
  z.ZodTypeDef,
  GetSurveyRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getSurveyRequestToJSON(
  getSurveyRequest: GetSurveyRequest,
): string {
  return JSON.stringify(
    GetSurveyRequest$outboundSchema.parse(getSurveyRequest),
  );
}

/** @internal */
export const GetSurveyResponse$inboundSchema: z.ZodType<
  GetSurveyResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.SurveysPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getSurveyResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetSurveyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetSurveyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetSurveyResponse' from JSON`,
  );
}
