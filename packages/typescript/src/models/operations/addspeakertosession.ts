/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import * as components from "../components/index.js";

export type AddSpeakerToSessionRequest = {
  /**
   * Unique Id of a session
   */
  id: string;
  /**
   * Unique Id of a speaker
   */
  speakerId: string;
  /**
   * Assign a category and order to the session's speaker. If no category is included in the body or the body isn't provided, the speaker's default category will be used. Similarly if no order is provided or no body is provided the speaker will be placed last in the ordered list of speakers for the session.
   */
  sessionSpeakerAssignment?: components.SessionSpeakerAssignment | undefined;
};

/** @internal */
export type AddSpeakerToSessionRequest$Outbound = {
  id: string;
  speakerId: string;
  "session-speaker-assignment"?:
    | components.SessionSpeakerAssignment$Outbound
    | undefined;
};

/** @internal */
export const AddSpeakerToSessionRequest$outboundSchema: z.ZodType<
  AddSpeakerToSessionRequest$Outbound,
  z.ZodTypeDef,
  AddSpeakerToSessionRequest
> = z.object({
  id: z.string(),
  speakerId: z.string(),
  sessionSpeakerAssignment: components.SessionSpeakerAssignment$outboundSchema
    .optional(),
}).transform((v) => {
  return remap$(v, {
    sessionSpeakerAssignment: "session-speaker-assignment",
  });
});

export function addSpeakerToSessionRequestToJSON(
  addSpeakerToSessionRequest: AddSpeakerToSessionRequest,
): string {
  return JSON.stringify(
    AddSpeakerToSessionRequest$outboundSchema.parse(addSpeakerToSessionRequest),
  );
}
