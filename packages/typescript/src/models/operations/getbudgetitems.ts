/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetBudgetItemsRequest = {
  /**
   * Unique ID of an event.
   */
  id: string;
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter string passed in the body of the request, narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * There are six comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *   * greater than: gt
   *   * greater or equal: ge
   *   * less than: lt
   *   * less than or equal: le
   *
   * The following fields are filterable:
   *    * id (eq)
   *    * costType (eq|ne)
   *    * category.id (eq|ne)
   *    * category.name (eq|ne)
   *    * subCategory.id (eq|ne)
   *    * subCategory.name (eq|ne)
   *    * status (eq|ne)
   *    * vendor.id (eq|ne)
   *    * vendor.name (eq|ne)
   *    * date (gt|ge|lt|le)
   *    * customFields.{uuid} (eq|ne|lt|le|gt|ge)
   *    * deleted (eq|ne) **Note:** Budget items that are deleted are available for 3 months after deletion.
   *      After this, they are removed and no longer appear in search results.
   *
   * The following operators are available:
   *   * and
   *   * or
   */
  filter?: string | undefined;
};

export type GetBudgetItemsResponse = {
  result: components.BudgetItemsPaginatedResponse;
};

/** @internal */
export type GetBudgetItemsRequest$Outbound = {
  id: string;
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetBudgetItemsRequest$outboundSchema: z.ZodType<
  GetBudgetItemsRequest$Outbound,
  z.ZodTypeDef,
  GetBudgetItemsRequest
> = z.object({
  id: z.string(),
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getBudgetItemsRequestToJSON(
  getBudgetItemsRequest: GetBudgetItemsRequest,
): string {
  return JSON.stringify(
    GetBudgetItemsRequest$outboundSchema.parse(getBudgetItemsRequest),
  );
}

/** @internal */
export const GetBudgetItemsResponse$inboundSchema: z.ZodType<
  GetBudgetItemsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.BudgetItemsPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getBudgetItemsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetBudgetItemsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetBudgetItemsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetBudgetItemsResponse' from JSON`,
  );
}
