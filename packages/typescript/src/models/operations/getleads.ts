/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetLeadsRequest = {
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * These are the comparison types that can be used in filter expressions:
   * * equal: eq
   *
   * The following fields are filterable:
   * * event.id (eq)
   * * exhibitor.id (eq)
   * * deleted (eq)
   */
  filter?: string | undefined;
};

export type GetLeadsResponse = {
  result: components.LeadsPaginatedResponse;
};

/** @internal */
export type GetLeadsRequest$Outbound = {
  limit: number;
  token?: string | undefined;
  before?: string | undefined;
  after?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetLeadsRequest$outboundSchema: z.ZodType<
  GetLeadsRequest$Outbound,
  z.ZodTypeDef,
  GetLeadsRequest
> = z.object({
  limit: z.number().int().default(100),
  token: z.string().optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  after: z.date().transform(v => v.toISOString()).optional(),
  filter: z.string().optional(),
});

export function getLeadsRequestToJSON(
  getLeadsRequest: GetLeadsRequest,
): string {
  return JSON.stringify(GetLeadsRequest$outboundSchema.parse(getLeadsRequest));
}

/** @internal */
export const GetLeadsResponse$inboundSchema: z.ZodType<
  GetLeadsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.LeadsPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getLeadsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetLeadsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLeadsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLeadsResponse' from JSON`,
  );
}
