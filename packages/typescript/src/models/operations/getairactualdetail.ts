/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetAirActualDetailRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A list of fields to be expanded, returning additional information alongside the specified fields. The expanded entity is retrieved and displayed inline for each specified expansion field. The respective scopes for each expansion are required.
   *
   * @remarks
   *
   * The following fields are expandable:
   *   * airActualDetails.airActualAnswers.question (<a href="#operation/getEventQuestions">List Event Questions</a>)
   */
  expand?: Array<components.ExpandAirActualQuestionAnswer> | undefined;
  /**
   * The sort order passed as a parameter, used to control the order of the
   *
   * @remarks
   * search results as a priority ordered list of sorts
   *
   * There are two orders:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * airActualDetails.name
   *   * airActualDetails.lastModified
   * where i <= limit
   */
  sort?: string | undefined;
  /**
   * "A filter query string narrows search results and supports the combination of logical and comparison operators. The filter adheres to the pattern filter='field' comparisonType 'value'."
   *
   * @remarks
   *
   * These are the comparison types that can be used in filter expressions:
   * * equal: eq
   * * not equal: ne
   *
   * Field filterable:
   * * attendee.id (Attendee Id)
   *
   * Limits for the number of fields that can be passed in a filter 17
   *
   * The following operators are available:
   * * or
   */
  filter?: string | undefined;
  /**
   * ID of an event.
   */
  id: string;
};

export type GetAirActualDetailResponse = {
  result: components.AirActualDetail;
};

/** @internal */
export type GetAirActualDetailRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  expand?: Array<string> | undefined;
  sort?: string | undefined;
  filter?: string | undefined;
  id: string;
};

/** @internal */
export const GetAirActualDetailRequest$outboundSchema: z.ZodType<
  GetAirActualDetailRequest$Outbound,
  z.ZodTypeDef,
  GetAirActualDetailRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  expand: z.array(components.ExpandAirActualQuestionAnswer$outboundSchema)
    .optional(),
  sort: z.string().optional(),
  filter: z.string().optional(),
  id: z.string(),
});

export function getAirActualDetailRequestToJSON(
  getAirActualDetailRequest: GetAirActualDetailRequest,
): string {
  return JSON.stringify(
    GetAirActualDetailRequest$outboundSchema.parse(getAirActualDetailRequest),
  );
}

/** @internal */
export const GetAirActualDetailResponse$inboundSchema: z.ZodType<
  GetAirActualDetailResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.AirActualDetail$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getAirActualDetailResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetAirActualDetailResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetAirActualDetailResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetAirActualDetailResponse' from JSON`,
  );
}
