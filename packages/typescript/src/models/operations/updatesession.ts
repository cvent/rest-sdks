/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import * as components from "../components/index.js";

export type UpdateSessionRequest = {
  /**
   * Unique Id of a session
   */
  id: string;
  /**
   * Contains values that you'll update the session with.
   *
   * @remarks
   * Passing a null value in a field renders the field blank.
   * Omitting a field from the request body renders the field blank
   * on the session record.
   */
  sessionUpdate: components.SessionUpdate;
};

/** @internal */
export type UpdateSessionRequest$Outbound = {
  id: string;
  "session-update": components.SessionUpdate$Outbound;
};

/** @internal */
export const UpdateSessionRequest$outboundSchema: z.ZodType<
  UpdateSessionRequest$Outbound,
  z.ZodTypeDef,
  UpdateSessionRequest
> = z.object({
  id: z.string(),
  sessionUpdate: components.SessionUpdate$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    sessionUpdate: "session-update",
  });
});

export function updateSessionRequestToJSON(
  updateSessionRequest: UpdateSessionRequest,
): string {
  return JSON.stringify(
    UpdateSessionRequest$outboundSchema.parse(updateSessionRequest),
  );
}
