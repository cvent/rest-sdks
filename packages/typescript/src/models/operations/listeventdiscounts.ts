/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListEventDiscountsRequest = {
  /**
   * Unique ID of an event.
   */
  id: string;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * The sort order passed as a parameter, used to control the order of the
   *
   * @remarks
   * search results as a priority ordered list of sorts.
   *
   * There are two orders:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * name
   *   * code
   *   * method.type
   *   * effectiveTo
   *   * effectiveFrom
   *   * type
   *   * level
   */
  sort?: string | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * There are two comparison types that can be used in filter expressions:
   *    * equal: eq
   *    * includes value(s): in
   *
   * The following fields are filterable:
   *    * id (in)
   *    * method.type (eq)
   *    * type (eq)
   *    * level (eq)
   *
   * The following operators are available:
   *    * and
   *    * or
   */
  filter?: string | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
};

export type ListEventDiscountsResponse = {
  result: components.ExistingDiscountPaginatedResponse;
};

/** @internal */
export type ListEventDiscountsRequest$Outbound = {
  id: string;
  limit: number;
  token?: string | undefined;
  sort?: string | undefined;
  filter?: string | undefined;
  before?: string | undefined;
  after?: string | undefined;
};

/** @internal */
export const ListEventDiscountsRequest$outboundSchema: z.ZodType<
  ListEventDiscountsRequest$Outbound,
  z.ZodTypeDef,
  ListEventDiscountsRequest
> = z.object({
  id: z.string(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  sort: z.string().optional(),
  filter: z.string().optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  after: z.date().transform(v => v.toISOString()).optional(),
});

export function listEventDiscountsRequestToJSON(
  listEventDiscountsRequest: ListEventDiscountsRequest,
): string {
  return JSON.stringify(
    ListEventDiscountsRequest$outboundSchema.parse(listEventDiscountsRequest),
  );
}

/** @internal */
export const ListEventDiscountsResponse$inboundSchema: z.ZodType<
  ListEventDiscountsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.ExistingDiscountPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listEventDiscountsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListEventDiscountsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEventDiscountsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEventDiscountsResponse' from JSON`,
  );
}
