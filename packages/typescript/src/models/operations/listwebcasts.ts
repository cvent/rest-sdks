/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListWebcastsRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Use filter query parameters to limit results
   *
   * @remarks
   * to data that matches your criteria. See
   * [Filters](/docs/rest-api/reference/filters) for details.
   *
   * Supported fields and operators are listed below:
   *
   * | Field          | Operators                                |
   * |----------------|------------------------------------------|
   * | id             | `eq`, `ne`                               |
   * | event.id       | `eq`, `ne`                               |
   * | session.id     | `eq`, `ne`, `is empty`, `is not empty`   |
   * | appointment.id | `eq`, `ne`, `is empty`, `is not empty`   |
   * | exhibitor.id   | `eq`, `ne`, `is empty`, `is not empty`   |
   * | sourceId       | `eq`, `ne`, `is empty`, `is not empty`   |
   * | solutionType   | `eq`, `ne`                               |
   * | provider       | `eq`, `ne`                               |
   * | type           | `eq`, `ne`                               |
   * | status         | `eq`, `ne`                               |
   * | meetingId      | `eq`, `ne`                               |
   * | created        | `eq`, `ne`, `lt`, `le`, `gt`, `ge`       |
   * | lastModified   | `eq`, `ne`, `lt`, `le`, `gt`, `ge`       |
   * | playerType     | `eq`, `ne`                               |
   */
  filter?: string | undefined;
};

export type ListWebcastsResponse = {
  result: components.WebcastPaginatedResponse;
};

/** @internal */
export type ListWebcastsRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const ListWebcastsRequest$outboundSchema: z.ZodType<
  ListWebcastsRequest$Outbound,
  z.ZodTypeDef,
  ListWebcastsRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function listWebcastsRequestToJSON(
  listWebcastsRequest: ListWebcastsRequest,
): string {
  return JSON.stringify(
    ListWebcastsRequest$outboundSchema.parse(listWebcastsRequest),
  );
}

/** @internal */
export const ListWebcastsResponse$inboundSchema: z.ZodType<
  ListWebcastsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.WebcastPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listWebcastsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListWebcastsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListWebcastsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListWebcastsResponse' from JSON`,
  );
}
