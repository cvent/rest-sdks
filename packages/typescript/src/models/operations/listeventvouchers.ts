/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListEventVouchersRequest = {
  /**
   * Unique ID of an event.
   */
  id: string;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * The sort order for the results, expressed as a priority-ordered list of field and direction pairs.
   *
   * @remarks
   *
   * There are two sort directions:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * code
   *
   * When not provided, results are sorted by `code` ascending.
   */
  sort?: string | undefined;
  /**
   * Use filter query parameters to limit results
   *
   * @remarks
   * to data that matches your criteria. See
   * [Filters](/docs/rest-api/reference/filters) for details.
   *
   * Supported fields and operators are listed below:
   *
   * | Field | Operators |
   * |-------|-----------|
   * | id    | `in`      |
   *
   * No logical operators are available.
   */
  filter?: string | undefined;
};

export type ListEventVouchersResponse = {
  result: components.VouchersPaginatedResponse;
};

/** @internal */
export type ListEventVouchersRequest$Outbound = {
  id: string;
  limit: number;
  token?: string | undefined;
  before?: string | undefined;
  after?: string | undefined;
  sort?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const ListEventVouchersRequest$outboundSchema: z.ZodType<
  ListEventVouchersRequest$Outbound,
  z.ZodTypeDef,
  ListEventVouchersRequest
> = z.object({
  id: z.string(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  after: z.date().transform(v => v.toISOString()).optional(),
  sort: z.string().optional(),
  filter: z.string().optional(),
});

export function listEventVouchersRequestToJSON(
  listEventVouchersRequest: ListEventVouchersRequest,
): string {
  return JSON.stringify(
    ListEventVouchersRequest$outboundSchema.parse(listEventVouchersRequest),
  );
}

/** @internal */
export const ListEventVouchersResponse$inboundSchema: z.ZodType<
  ListEventVouchersResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.VouchersPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listEventVouchersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListEventVouchersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListEventVouchersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListEventVouchersResponse' from JSON`,
  );
}
