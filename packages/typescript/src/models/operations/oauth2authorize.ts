/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The response type must be code. Indicates the client wants an authorization code (authorization code grant flow).
 *
 * @remarks
 *
 * **Note:** *implicit flow* or the *token* value is not supported at this time.
 */
export const ResponseType = {
  Code: "code",
} as const;
/**
 * The response type must be code. Indicates the client wants an authorization code (authorization code grant flow).
 *
 * @remarks
 *
 * **Note:** *implicit flow* or the *token* value is not supported at this time.
 */
export type ResponseType = ClosedEnum<typeof ResponseType>;

export type Oauth2AuthorizeRequest = {
  /**
   * The response type must be code. Indicates the client wants an authorization code (authorization code grant flow).
   *
   * @remarks
   *
   * **Note:** *implicit flow* or the *token* value is not supported at this time.
   */
  responseType: ResponseType;
  /**
   * The OAuth2 Client application ID.
   *
   * @remarks
   *
   * Must be a pre-registered client created in the developer portal.
   */
  clientId: string;
  /**
   * The URL to which the authentication server redirects the browser after authorization has been granted by the user.
   *
   * @remarks
   *
   * A redirect URI must:
   *
   * * Be an absolute URI.
   * * Be pre-registered with a client.
   * * **NOT** include a fragment component.
   * * Wildcards are **NOT** supported
   *
   * Requires HTTPS over HTTP except for http://localhost for testing purposes only.
   *
   * **Note:** App callback URLs such as *com.myclientapp://myclient/redirect* are also supported.
   */
  redirectUri: string;
  /**
   * An opaque value the client application adds to the initial request. The authorization server
   *
   * @remarks
   * includes this value when redirecting back to the client.
   *
   * This value must be used by the client to prevent CSRF attacks.
   *
   * **Optional** but strongly recommended.
   */
  state?: string | undefined;
  /**
   * Can be a combination of any scopes associated with a client. Scopes must be separated by spaces.
   *
   * @remarks
   * Any scope used must be pre-associated with the client or it will be ignored at runtime.
   *
   * **Note:** If the client doesn't request any scopes, the authentication server uses all
   * scopes associated with the client.
   */
  scope?: string | undefined;
};

export type Oauth2AuthorizeResponse = {
  headers: { [k: string]: Array<string> };
};

/** @internal */
export const ResponseType$outboundSchema: z.ZodNativeEnum<typeof ResponseType> =
  z.nativeEnum(ResponseType);

/** @internal */
export type Oauth2AuthorizeRequest$Outbound = {
  response_type: string;
  client_id: string;
  redirect_uri: string;
  state?: string | undefined;
  scope?: string | undefined;
};

/** @internal */
export const Oauth2AuthorizeRequest$outboundSchema: z.ZodType<
  Oauth2AuthorizeRequest$Outbound,
  z.ZodTypeDef,
  Oauth2AuthorizeRequest
> = z.object({
  responseType: ResponseType$outboundSchema,
  clientId: z.string(),
  redirectUri: z.string(),
  state: z.string().optional(),
  scope: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    responseType: "response_type",
    clientId: "client_id",
    redirectUri: "redirect_uri",
  });
});

export function oauth2AuthorizeRequestToJSON(
  oauth2AuthorizeRequest: Oauth2AuthorizeRequest,
): string {
  return JSON.stringify(
    Oauth2AuthorizeRequest$outboundSchema.parse(oauth2AuthorizeRequest),
  );
}

/** @internal */
export const Oauth2AuthorizeResponse$inboundSchema: z.ZodType<
  Oauth2AuthorizeResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Headers: z.record(z.array(z.string())).default({}),
}).transform((v) => {
  return remap$(v, {
    "Headers": "headers",
  });
});

export function oauth2AuthorizeResponseFromJSON(
  jsonString: string,
): SafeParseResult<Oauth2AuthorizeResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Oauth2AuthorizeResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Oauth2AuthorizeResponse' from JSON`,
  );
}
