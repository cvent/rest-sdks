/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetAccountBudgetItemsRequest = {
  /**
   * Used to query records that have been modified after this time point.
   */
  after: Date;
  /**
   * Used to query records that have been modified before this point
   */
  before: Date;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter string passed as a query parameter, narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   * There are six comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *
   * The following fields are filterable:
   *    * event.id (eq|ne)
   *    * rfp.id (eq|ne)
   *    * budgetVersion.id (eq|ne)
   *    * id (eq|ne)
   *    * category.id (eq|ne)
   *    * category.name (eq|ne)
   *    * subCategory.id (eq|ne)
   *    * subCategory.name (eq|ne)
   *
   * The following operators are available:
   *   * and
   *   * or
   */
  filter?: string | undefined;
};

export type GetAccountBudgetItemsResponse = {
  result: components.AccountBudgetItemsPaginatedResponse;
};

/** @internal */
export type GetAccountBudgetItemsRequest$Outbound = {
  after: string;
  before: string;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetAccountBudgetItemsRequest$outboundSchema: z.ZodType<
  GetAccountBudgetItemsRequest$Outbound,
  z.ZodTypeDef,
  GetAccountBudgetItemsRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()),
  before: z.date().transform(v => v.toISOString()),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getAccountBudgetItemsRequestToJSON(
  getAccountBudgetItemsRequest: GetAccountBudgetItemsRequest,
): string {
  return JSON.stringify(
    GetAccountBudgetItemsRequest$outboundSchema.parse(
      getAccountBudgetItemsRequest,
    ),
  );
}

/** @internal */
export const GetAccountBudgetItemsResponse$inboundSchema: z.ZodType<
  GetAccountBudgetItemsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.AccountBudgetItemsPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getAccountBudgetItemsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetAccountBudgetItemsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetAccountBudgetItemsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetAccountBudgetItemsResponse' from JSON`,
  );
}
