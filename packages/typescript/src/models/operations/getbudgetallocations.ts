/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetBudgetAllocationsRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * Use filter query parameters to limit results
   *
   * @remarks
   * to data that matches your criteria. See
   * [Filters](/docs/rest-api/reference/filters) for details.
   *
   * Supported fields and operators are listed below:
   *
   * | Field            | Operators                    |
   * |:-----------------|:-----------------------------|
   * | budgetVersion.id | `eq`                         |
   * | budgetItem.id    | `eq`                         |
   * | category.id      | `eq`, `ne`                   |
   * | subcategory.id   | `eq`, `ne`                   |
   * | generalLedger.id | `eq`, `ne`                   |
   * | value            | `lt`, `le`, `gt`, `ge`, `eq` |
   *
   * The following logical operators are supported for combining filters:
   * * and
   * * or
   */
  filter?: string | undefined;
  /**
   * Unique ID of an event.
   */
  id: string;
};

export type GetBudgetAllocationsResponse = {
  result: components.BudgetAllocationsPaginatedListResponse;
};

/** @internal */
export type GetBudgetAllocationsRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
  id: string;
};

/** @internal */
export const GetBudgetAllocationsRequest$outboundSchema: z.ZodType<
  GetBudgetAllocationsRequest$Outbound,
  z.ZodTypeDef,
  GetBudgetAllocationsRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
  id: z.string(),
});

export function getBudgetAllocationsRequestToJSON(
  getBudgetAllocationsRequest: GetBudgetAllocationsRequest,
): string {
  return JSON.stringify(
    GetBudgetAllocationsRequest$outboundSchema.parse(
      getBudgetAllocationsRequest,
    ),
  );
}

/** @internal */
export const GetBudgetAllocationsResponse$inboundSchema: z.ZodType<
  GetBudgetAllocationsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.BudgetAllocationsPaginatedListResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getBudgetAllocationsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetBudgetAllocationsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetBudgetAllocationsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetBudgetAllocationsResponse' from JSON`,
  );
}
