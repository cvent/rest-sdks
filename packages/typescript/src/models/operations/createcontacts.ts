/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import * as components from "../components/index.js";

export type CreateContactsRequest = {
  /**
   * This parameter determines whether the POST operation should be treated as an upsert. Upserts update existing contacts if they already exist, and otherwise create new contacts.
   *
   * @remarks
   *
   * True indicates that this operation creates new contacts and updates existing contacts if they exist. False means that it only creates new contacts, and attempting to create an existing contact will result in the API returning an error.
   *
   * Existing contacts are matched to the upsert request via the account's [match key](https://support.cvent.com/s/communityarticle/Allowing-Multiple-People-to-Use-the-Same-Email-Address). By default, the match key for contacts is their email. The match key can also be altered to match contacts based on a combination of their first name, last name, and email.
   */
  upsert?: boolean | undefined;
  /**
   * List of contacts to be created. Up to **10 contacts** can be created per call.
   */
  requestBody: Array<components.ContactCreateRequestJson>;
};

/** @internal */
export type CreateContactsRequest$Outbound = {
  upsert: boolean;
  RequestBody: Array<components.ContactCreateRequestJson$Outbound>;
};

/** @internal */
export const CreateContactsRequest$outboundSchema: z.ZodType<
  CreateContactsRequest$Outbound,
  z.ZodTypeDef,
  CreateContactsRequest
> = z.object({
  upsert: z.boolean().default(false),
  requestBody: z.array(components.ContactCreateRequestJson$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    requestBody: "RequestBody",
  });
});

export function createContactsRequestToJSON(
  createContactsRequest: CreateContactsRequest,
): string {
  return JSON.stringify(
    CreateContactsRequest$outboundSchema.parse(createContactsRequest),
  );
}
