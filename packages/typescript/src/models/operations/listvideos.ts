/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListVideosRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * There are six comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *   * greater than: gt
   *   * greater or equal: ge
   *   * less than: lt
   *   * less than or equal: le
   *
   * The following fields are filterable:
   *   * id (eq|ne)
   *   * events (eq|ne)
   *   * sessions (eq|ne)
   *   * exhibitors (eq|ne)
   *   * speakers (eq|ne)
   *   * source.id (eq|ne)
   *   * status (eq|ne)
   *   * tags (eq|ne)
   *   * recording.id (eq|ne)
   *
   * The following filterable fields have been deprecated, use the plural form above:
   *   * event.id (eq|ne)
   */
  filter?: string | undefined;
  /**
   * True means only videos marked as deleted will be queried. False means only non-deleted videos will be queried.
   */
  deleted?: boolean | undefined;
};

export type ListVideosResponse = {
  result: components.VideosPaginatedResponse;
};

/** @internal */
export type ListVideosRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
  deleted: boolean;
};

/** @internal */
export const ListVideosRequest$outboundSchema: z.ZodType<
  ListVideosRequest$Outbound,
  z.ZodTypeDef,
  ListVideosRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
  deleted: z.boolean().default(false),
});

export function listVideosRequestToJSON(
  listVideosRequest: ListVideosRequest,
): string {
  return JSON.stringify(
    ListVideosRequest$outboundSchema.parse(listVideosRequest),
  );
}

/** @internal */
export const ListVideosResponse$inboundSchema: z.ZodType<
  ListVideosResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.VideosPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listVideosResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListVideosResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListVideosResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListVideosResponse' from JSON`,
  );
}
