/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListSeatingRequest = {
  /**
   * ID of an event.
   */
  id: string;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter string passed in the body of the request, narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * There are eight comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *   * greater than: gt
   *   * greater or equal: ge
   *   * less than: lt
   *   * less than or equal: le
   *   * starts with: sw
   *   * contains a value: contains
   *   * includes value(s): in
   *
   * The following fields are filterable:
   *   * id (eq)
   *
   * The following operators are available:
   *   * and
   *   * or
   */
  filter?: string | undefined;
};

export type ListSeatingResponse = {
  result: components.SeatingPaginatedResponse;
};

/** @internal */
export type ListSeatingRequest$Outbound = {
  id: string;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const ListSeatingRequest$outboundSchema: z.ZodType<
  ListSeatingRequest$Outbound,
  z.ZodTypeDef,
  ListSeatingRequest
> = z.object({
  id: z.string(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function listSeatingRequestToJSON(
  listSeatingRequest: ListSeatingRequest,
): string {
  return JSON.stringify(
    ListSeatingRequest$outboundSchema.parse(listSeatingRequest),
  );
}

/** @internal */
export const ListSeatingResponse$inboundSchema: z.ZodType<
  ListSeatingResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.SeatingPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listSeatingResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListSeatingResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSeatingResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSeatingResponse' from JSON`,
  );
}
