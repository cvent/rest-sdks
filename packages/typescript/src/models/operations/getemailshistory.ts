/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetEmailsHistoryRequest = {
  /**
   * Used to query records that have been created before this time point. Defaults to current time.
   */
  before?: string | undefined;
  /**
   * Used to query records that have been created after this time point. Defaults to 6 months back from current time.
   */
  after?: string | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * 'OR' is not supported. Only 'AND' operator is supported.
   *
   * The following comparison types can be used in filter expressions:
   *   * equal: eq
   *   * greater or equal: ge
   *   * less than or equal: le
   *
   * The following fields are filterable:
   *   * event.id (eq)
   *   * attendee.id (eq)
   *   * contact.id (eq)
   *   * status (eq)
   *   * lastModified (eq|le|ge)
   */
  filter?: string | undefined;
};

export type GetEmailsHistoryResponse = {
  result: components.EmailHistoryPaginatedResponse;
};

/** @internal */
export type GetEmailsHistoryRequest$Outbound = {
  before?: string | undefined;
  after?: string | undefined;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetEmailsHistoryRequest$outboundSchema: z.ZodType<
  GetEmailsHistoryRequest$Outbound,
  z.ZodTypeDef,
  GetEmailsHistoryRequest
> = z.object({
  before: z.string().optional(),
  after: z.string().optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function getEmailsHistoryRequestToJSON(
  getEmailsHistoryRequest: GetEmailsHistoryRequest,
): string {
  return JSON.stringify(
    GetEmailsHistoryRequest$outboundSchema.parse(getEmailsHistoryRequest),
  );
}

/** @internal */
export const GetEmailsHistoryResponse$inboundSchema: z.ZodType<
  GetEmailsHistoryResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.EmailHistoryPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getEmailsHistoryResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetEmailsHistoryResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetEmailsHistoryResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetEmailsHistoryResponse' from JSON`,
  );
}
