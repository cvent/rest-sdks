/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetAttendeeCreditsRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * The `sort` query parameter sorts items in the list of results and organizes them according to your preference.
   *
   * @remarks
   * There are two orders:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * lastModified
   */
  sort?: string | undefined;
  /**
   * Use the `expand` query parameter to include additional related information in the response. For each specified value, the related entity is retrieved and displayed inline.
   *
   * @remarks
   *
   * An expandable object typically contains the ID of a related object in its response properties.
   *
   * The following fields are expandable:
   *   * creditType(Includes only the `creditType.name` in the response. A full list of properties for `creditType` is not currently provided).
   */
  expand?: Array<components.Expand2> | undefined;
  /**
   * A filter query string narrows search
   *
   * @remarks
   * results and supports the combination of logical and comparison
   * operators.
   *
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * These are the comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *
   * The following fields are filterable:
   * * attendee.id (eq|ne)
   * * event.id (eq|ne)
   * * session.id (eq|ne)
   * * creditType.id (eq|ne)
   * * type (eq)
   *
   * Note:
   * * lastModified: Used by the 'before' and 'after' filters.
   */
  filter?: string | undefined;
};

export type GetAttendeeCreditsResponse = {
  result: components.AttendeeCreditsPaginatedResponse;
};

/** @internal */
export type GetAttendeeCreditsRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  sort?: string | undefined;
  expand?: Array<string> | undefined;
  filter?: string | undefined;
};

/** @internal */
export const GetAttendeeCreditsRequest$outboundSchema: z.ZodType<
  GetAttendeeCreditsRequest$Outbound,
  z.ZodTypeDef,
  GetAttendeeCreditsRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  sort: z.string().optional(),
  expand: z.array(components.Expand2$outboundSchema).optional(),
  filter: z.string().optional(),
});

export function getAttendeeCreditsRequestToJSON(
  getAttendeeCreditsRequest: GetAttendeeCreditsRequest,
): string {
  return JSON.stringify(
    GetAttendeeCreditsRequest$outboundSchema.parse(getAttendeeCreditsRequest),
  );
}

/** @internal */
export const GetAttendeeCreditsResponse$inboundSchema: z.ZodType<
  GetAttendeeCreditsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.AttendeeCreditsPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getAttendeeCreditsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetAttendeeCreditsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetAttendeeCreditsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetAttendeeCreditsResponse' from JSON`,
  );
}
