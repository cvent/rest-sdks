/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListAvailableTimesRequest = {
  /**
   * The unique identifier of an appointment event.
   */
  id: string;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators. The filter adheres to the pattern filter='field' comparisonType 'value'.
   *
   * @remarks
   *
   * There are six comparison types that can be used in filter expressions:
   * * equal: eq
   * * not equal: ne
   * * greater than: gt
   * * greater or equal: ge
   * * less than: lt
   * * less than or equal: le
   *
   * The following fields are filterable:
   * * appointmentType.id (eq)
   * * location.id (eq)
   *
   * No operators are available
   */
  filter?: string | undefined;
};

export type ListAvailableTimesResponse = {
  result: components.AvailableTimesPaginatedResponse;
};

/** @internal */
export type ListAvailableTimesRequest$Outbound = {
  id: string;
  limit: number;
  token?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const ListAvailableTimesRequest$outboundSchema: z.ZodType<
  ListAvailableTimesRequest$Outbound,
  z.ZodTypeDef,
  ListAvailableTimesRequest
> = z.object({
  id: z.string(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  filter: z.string().optional(),
});

export function listAvailableTimesRequestToJSON(
  listAvailableTimesRequest: ListAvailableTimesRequest,
): string {
  return JSON.stringify(
    ListAvailableTimesRequest$outboundSchema.parse(listAvailableTimesRequest),
  );
}

/** @internal */
export const ListAvailableTimesResponse$inboundSchema: z.ZodType<
  ListAvailableTimesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.AvailableTimesPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listAvailableTimesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAvailableTimesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAvailableTimesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAvailableTimesResponse' from JSON`,
  );
}
