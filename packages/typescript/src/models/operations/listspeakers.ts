/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListSpeakersRequest = {
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * The sort order passed as a parameter, used to control the order of the
   *
   * @remarks
   * search results as a priority ordered list of sorts
   *
   * There are two orders:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * lastModified
   *   * lastName
   *   * firstName
   *   * company
   *   * title
   *   * order
   */
  sort?: string | undefined;
  /**
   * Locale code in IETF Language tag format. Indicates desired locale of return values.
   */
  locale?: string | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'. If the "match" filter is passed,
   * the endpoint will return a relevance score for each attendee in the list. Higher scores represent a higher
   * relevance to the filter string.
   *
   * These are the comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *   * greater than: gt
   *   * greater or equal: ge
   *   * less than: lt
   *   * less than or equal: le
   *   * starts with: sw
   *   * contains a value: contains
   *   * match a term: match
   *   * includes value(s): in
   *
   * The following fields are filterable:
   *   * category.id (eq|ne)
   *   * category.name (eq|ne|sw|contains)
   *   * code (eq|ne|sw|contains)
   *   * company (eq|ne|sw|contains)
   *   * contact.id (eq|ne)
   *   * email (eq|ne|sw|contains|match)
   *   * event.id (eq|ne)
   *   * featured (eq|ne)
   *   * firstName (eq|ne|sw|contains|match)
   *   * id (eq|ne|in)
   *   * lastModified (eq|ne|gt|ge|lt|le)
   *   * lastName (eq|ne|sw|contains|match)
   *   * title (eq|ne|sw|contains)
   *   * visibility (eq|ne)
   *
   * The following operators are available:
   *   * and
   *   * or
   */
  filter?: string | undefined;
};

export type ListSpeakersResponse = {
  result: components.SpeakerPaginatedResponse;
};

/** @internal */
export type ListSpeakersRequest$Outbound = {
  limit: number;
  token?: string | undefined;
  sort?: string | undefined;
  locale?: string | undefined;
  filter?: string | undefined;
};

/** @internal */
export const ListSpeakersRequest$outboundSchema: z.ZodType<
  ListSpeakersRequest$Outbound,
  z.ZodTypeDef,
  ListSpeakersRequest
> = z.object({
  limit: z.number().int().default(100),
  token: z.string().optional(),
  sort: z.string().optional(),
  locale: z.string().optional(),
  filter: z.string().optional(),
});

export function listSpeakersRequestToJSON(
  listSpeakersRequest: ListSpeakersRequest,
): string {
  return JSON.stringify(
    ListSpeakersRequest$outboundSchema.parse(listSpeakersRequest),
  );
}

/** @internal */
export const ListSpeakersResponse$inboundSchema: z.ZodType<
  ListSpeakersResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.SpeakerPaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listSpeakersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListSpeakersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSpeakersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSpeakersResponse' from JSON`,
  );
}
