/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ListAttendeesRequest = {
  /**
   * Used to query records that have been added or updated after this time point. Default to the beginning of time of the data store.
   */
  after?: Date | undefined;
  /**
   * Used to query records that have been added or updated before this time point.
   */
  before?: Date | undefined;
  /**
   * The maximum number of records to return per page.
   */
  limit?: number | undefined;
  /**
   * The continuation token returned from a previous class. This must be a valid UUID v4 if provided.
   *
   * @remarks
   * This will override any other pageable parameters provided.
   */
  token?: string | undefined;
  /**
   * This endpoint allows you to request additional information as an expanded response using the expand query parameter.
   *
   * @remarks
   * The expanded entity is retrieved and displayed inline for each specified expand value.
   *
   * An object contains the ID of a related object in its response properties. You can expand these objects,
   * along with others, in line using the expand query parameter.
   *
   * You can expand multiple objects at the same time by identifying multiple items in the expand array.
   *
   * The respective scopes for each expansion are required.
   *
   * The following fields are expandable:
   *   * answers.question
   *     * **Scope**: event/events:read
   *     * **Reference**: <a href="#operation/getEventQuestions">List Event Questions</a>
   *   * registrationType
   *     * **Scope**: event/registration-types:read
   *     * **Reference**: <a href="#operation/listRegistrationTypes">List Registration Types</a>
   */
  expand?: Array<components.Expand> | undefined;
  /**
   * The `sort` query parameter sorts items in the list of results and organizes them according to your preference.
   *
   * @remarks
   * There are two orders:
   *   * ascending: ASC
   *   * descending: DESC
   *
   * The following fields are sortable:
   *   * contact.firstName
   *   * contact.lastName
   *   * contact.title
   *   * contact.company
   *   * registeredAt
   *   * checkIn
   */
  sort?: string | undefined;
  /**
   * This allows users to include deleted guests in the response.
   */
  includeDeletedGuests?: boolean | undefined;
  /**
   * A filter query string narrows search results and supports the combination of logical and comparison operators.
   *
   * @remarks
   * The filter adheres to the pattern filter='field' comparisonType 'value'.
   * These are the comparison types that can be used in filter expressions:
   *   * equal: eq
   *   * not equal: ne
   *   * greater than: gt
   *   * greater or equal: ge
   *   * less than: lt
   *   * less than or equal: le
   *   * starts with: sw
   *   * is null: is null
   *   * is not null: is not null
   *   * is empty: is empty
   *   * is not empty: is not empty
   *
   * The following fields are filterable:
   *   * event.id (eq|ne)
   *   * id (eq|ne)
   *   * confirmationNumber (eq|ne)
   *   * checkedIn (eq|ne)
   *   * checkIn (eq|ne|lt|le|gt|ge)
   *   * checkOut (eq|ne|lt|le|gt|ge)
   *   * registrationPath.id (eq|ne)
   *   * invitationList.id (eq|ne)
   *   * registrationType.id (eq|ne|is null|is not null|is empty|is not empty)
   *   * referenceId (eq|ne)
   *   * externalReference.type (eq)
   *   * externalReference.id (eq)
   *   * externalReference.referenceId (eq)
   *   * note (eq|ne)
   *   * guest (eq|ne)
   *   * group.leader (eq|ne)
   *   * group.id(eq|ne)
   *   * unsubscribed (eq|ne)
   *   * registeredAt (eq|ne|lt|le|gt|ge)
   *   * registrationLastModified (eq|ne|lt|le|gt|ge)
   *   * invitedBy (eq|ne)
   *   * responseMethod (eq|ne)
   *   * status (eq|ne)
   *   * lastModified (eq|ne|lt|le|gt|ge)
   *   * created (eq|ne|lt|le|gt|ge)
   *   * contact.id (eq|ne)
   *   * visibility (eq|ne)
   *   * contact.title (eq|ne|sw)
   *   * contact.company (eq|ne|sw)
   *   * contact.firstName (eq|ne|sw)
   *   * contact.lastName (eq|ne|sw)
   *   * answers.question.{question id} (eq|ne)
   *   * contact.email (eq|ne|sw)
   *   * attendeeLastModified (eq|ne|lt|le|gt|ge)
   *   * appointmentGroup.id (eq|ne)
   *   * contact.deleted (eq|ne)
   *   * primaryId (eq|ne)
   *   * deletedGuest (eq|ne)
   *
   * Note:
   *   * lastModified: Will be updated when any field in the response has changed. Used by the 'before' and 'after' filters.
   *   * attendeeLastModified: Will be updated only when some field other than contact.* has changed. Not used by the 'before' and 'after' filters.
   *   * deletedGuest: Filter returns guests marked as deleted. This filter works only when the query parameter `includeDeletedGuests` is true. True indicates the request should include deleted guests in the response.
   */
  filter?: string | undefined;
};

export type ListAttendeesResponse = {
  result: components.AttendeePaginatedResponse;
};

/** @internal */
export type ListAttendeesRequest$Outbound = {
  after?: string | undefined;
  before?: string | undefined;
  limit: number;
  token?: string | undefined;
  expand?: Array<string> | undefined;
  sort?: string | undefined;
  includeDeletedGuests: boolean;
  filter?: string | undefined;
};

/** @internal */
export const ListAttendeesRequest$outboundSchema: z.ZodType<
  ListAttendeesRequest$Outbound,
  z.ZodTypeDef,
  ListAttendeesRequest
> = z.object({
  after: z.date().transform(v => v.toISOString()).optional(),
  before: z.date().transform(v => v.toISOString()).optional(),
  limit: z.number().int().default(100),
  token: z.string().optional(),
  expand: z.array(components.Expand$outboundSchema).optional(),
  sort: z.string().optional(),
  includeDeletedGuests: z.boolean().default(false),
  filter: z.string().optional(),
});

export function listAttendeesRequestToJSON(
  listAttendeesRequest: ListAttendeesRequest,
): string {
  return JSON.stringify(
    ListAttendeesRequest$outboundSchema.parse(listAttendeesRequest),
  );
}

/** @internal */
export const ListAttendeesResponse$inboundSchema: z.ZodType<
  ListAttendeesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Result: components.AttendeePaginatedResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function listAttendeesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAttendeesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAttendeesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAttendeesResponse' from JSON`,
  );
}
