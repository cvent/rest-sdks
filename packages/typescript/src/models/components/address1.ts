/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Address details.
 */
export type Address1 = {
  /**
   * ISO 3166 code for the country.
   */
  countryCode: string;
  /**
   * The name of the city.
   */
  city: string;
  /**
   * ISO 3166-2 code for the state/province/region.
   */
  regionCode?: string | undefined;
};

/** @internal */
export const Address1$inboundSchema: z.ZodType<
  Address1,
  z.ZodTypeDef,
  unknown
> = z.object({
  countryCode: z.string(),
  city: z.string(),
  regionCode: z.string().optional(),
});

export function address1FromJSON(
  jsonString: string,
): SafeParseResult<Address1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Address1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Address1' from JSON`,
  );
}
