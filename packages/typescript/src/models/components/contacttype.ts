/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * This entity represents a contact type at contact level.
 */
export type ContactType = {
  /**
   * The ID of the contact type.
   */
  id: string;
  /**
   * The contact type code. Must be unique in the account.
   */
  code: string;
  /**
   * Name of the contact type.
   */
  name: string;
  /**
   * Description of the contact type.
   */
  description?: string | undefined;
  /**
   * Indicates whether the contact type is active.
   */
  active?: boolean | undefined;
};

/** @internal */
export const ContactType$inboundSchema: z.ZodType<
  ContactType,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  code: z.string(),
  name: z.string(),
  description: z.string().optional(),
  active: z.boolean().optional(),
});

export function contactTypeFromJSON(
  jsonString: string,
): SafeParseResult<ContactType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContactType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContactType' from JSON`,
  );
}
