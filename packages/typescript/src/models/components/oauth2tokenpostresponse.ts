/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Oauth2TokenPostResponse = {
  /**
   * The access token contains scopes and groups and is used to grant access to authorized resources.
   */
  accessToken: string;
  /**
   * The refresh token contains the information necessary to obtain a new access token.
   */
  refreshToken?: string | undefined;
  /**
   * The OAuth2 token type. Always `Bearer`.
   */
  tokenType: string;
  /**
   * The lifetime in seconds of the access token.
   */
  expiresIn: number;
};

/** @internal */
export const Oauth2TokenPostResponse$inboundSchema: z.ZodType<
  Oauth2TokenPostResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  access_token: z.string(),
  refresh_token: z.string().optional(),
  token_type: z.string(),
  expires_in: z.number().int().default(3600),
}).transform((v) => {
  return remap$(v, {
    "access_token": "accessToken",
    "refresh_token": "refreshToken",
    "token_type": "tokenType",
    "expires_in": "expiresIn",
  });
});

export function oauth2TokenPostResponseFromJSON(
  jsonString: string,
): SafeParseResult<Oauth2TokenPostResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Oauth2TokenPostResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Oauth2TokenPostResponse' from JSON`,
  );
}
