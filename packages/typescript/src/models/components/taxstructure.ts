/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { TaxAndFee, TaxAndFee$inboundSchema } from "./taxandfee.js";

/**
 * Details about taxes and fees applicable to a room.
 */
export type TaxStructure = {
  /**
   * Statement disclosing tax and fee information.
   */
  disclosureStatement: string;
  /**
   * List of tax or fee items.
   */
  items: Array<TaxAndFee>;
};

/** @internal */
export const TaxStructure$inboundSchema: z.ZodType<
  TaxStructure,
  z.ZodTypeDef,
  unknown
> = z.object({
  disclosureStatement: z.string(),
  items: z.array(TaxAndFee$inboundSchema),
});

export function taxStructureFromJSON(
  jsonString: string,
): SafeParseResult<TaxStructure, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxStructure$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxStructure' from JSON`,
  );
}
