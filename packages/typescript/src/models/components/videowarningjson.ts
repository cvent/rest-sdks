/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  VideoWarningCodeJson,
  VideoWarningCodeJson$inboundSchema,
} from "./videowarningcodejson.js";

/**
 * This is used to denote the warnings for a video entity.
 */
export type VideoWarningJson = {
  /**
   * This is used to denote the warning codes for a video entity.
   */
  code: VideoWarningCodeJson;
  /**
   * Warning message for the video. Message is a developer convenience never to be used in any context that needs localisation, subject to change at any time.
   */
  message: string;
};

/** @internal */
export const VideoWarningJson$inboundSchema: z.ZodType<
  VideoWarningJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  code: VideoWarningCodeJson$inboundSchema,
  message: z.string(),
});

export function videoWarningJsonFromJSON(
  jsonString: string,
): SafeParseResult<VideoWarningJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VideoWarningJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VideoWarningJson' from JSON`,
  );
}
