/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { DatesJson0, DatesJson0$inboundSchema } from "./datesjson0.js";
import {
  DayOfWeekJson0,
  DayOfWeekJson0$inboundSchema,
} from "./dayofweekjson0.js";

/**
 * Date pattern for days on which event can occur. Used when dateMode is DATE_PATTERN.
 */
export type DatePatternJson0 = {
  /**
   * Note along with date pattern requirements.
   */
  note?: string | undefined;
  /**
   * The ISO 8601 end date of the event.
   */
  endDate: RFCDate;
  /**
   * The ISO 8601 start date of the event.
   */
  startDate: RFCDate;
  /**
   * Number of days the event will go on.
   */
  durationDays: number;
  /**
   * Days of the week when event can start.
   */
  startDays: Array<DayOfWeekJson0>;
  /**
   * List of dates generated based on the date pattern.
   */
  generatedDates?: Array<DatesJson0> | undefined;
};

/** @internal */
export const DatePatternJson0$inboundSchema: z.ZodType<
  DatePatternJson0,
  z.ZodTypeDef,
  unknown
> = z.object({
  note: z.string().optional(),
  endDate: z.string().transform(v => new RFCDate(v)),
  startDate: z.string().transform(v => new RFCDate(v)),
  durationDays: z.number().int(),
  startDays: z.array(DayOfWeekJson0$inboundSchema),
  generatedDates: z.array(DatesJson0$inboundSchema).optional(),
});

export function datePatternJson0FromJSON(
  jsonString: string,
): SafeParseResult<DatePatternJson0, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DatePatternJson0$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DatePatternJson0' from JSON`,
  );
}
