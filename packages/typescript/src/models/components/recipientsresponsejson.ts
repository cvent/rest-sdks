/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SendEmailStatusJson,
  SendEmailStatusJson$inboundSchema,
} from "./sendemailstatusjson.js";

/**
 * Response object indicating the status of send email request for a particular recipient.
 */
export type RecipientsResponseJson = {
  /**
   * The ID of the recipient you're emailing.
   */
  id: string;
  /**
   * It is used to denote the status of the sent email.
   */
  status: SendEmailStatusJson;
  /**
   * The error description describes the reason of a failed email.
   */
  errorMessage?: string | undefined;
};

/** @internal */
export const RecipientsResponseJson$inboundSchema: z.ZodType<
  RecipientsResponseJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  status: SendEmailStatusJson$inboundSchema,
  errorMessage: z.string().optional(),
});

export function recipientsResponseJsonFromJSON(
  jsonString: string,
): SafeParseResult<RecipientsResponseJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RecipientsResponseJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RecipientsResponseJson' from JSON`,
  );
}
