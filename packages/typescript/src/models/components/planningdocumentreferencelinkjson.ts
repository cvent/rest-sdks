/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A reference link contains the URL that provides direct access to download the file for the document. The expiration of the URL can be included if applicable.
 */
export type PlanningDocumentReferenceLinkJson = {
  /**
   * The URL to download the file resource for the document. This URL will expire after the time indicated by the field expiration. If not provided, the URL is considered dynamic and not guaranteed to remain constant. Clients should always retrieve the latest URL before attempting to download the file, as it may change over time.
   */
  url?: string | undefined;
  /**
   * The ISO 8601 formatted date and time at which the reference document will expire if an explicit expiration is defined.
   */
  expiration?: Date | undefined;
};

/** @internal */
export const PlanningDocumentReferenceLinkJson$inboundSchema: z.ZodType<
  PlanningDocumentReferenceLinkJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  url: z.string().optional(),
  expiration: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
});

export function planningDocumentReferenceLinkJsonFromJSON(
  jsonString: string,
): SafeParseResult<PlanningDocumentReferenceLinkJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlanningDocumentReferenceLinkJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlanningDocumentReferenceLinkJson' from JSON`,
  );
}
