/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PaginationLinksJson,
  PaginationLinksJson$inboundSchema,
} from "./paginationlinksjson.js";

/**
 * Represents pagination information for a collection of resources.
 */
export type PagingJson25 = {
  /**
   * The pagination token for the next page; if available. You can use this to determine if there is additional data to fetch
   */
  nextToken?: string | undefined;
  /**
   * The pagination token for the current page
   */
  currentToken?: string | undefined;
  /**
   * The number of records to return on the page not to exceed 200
   */
  limit?: number | undefined;
  /**
   * The total number of records available
   */
  totalCount?: number | undefined;
  /**
   * Represents pagination links for navigating between pages of data.
   */
  links: PaginationLinksJson;
};

/** @internal */
export const PagingJson25$inboundSchema: z.ZodType<
  PagingJson25,
  z.ZodTypeDef,
  unknown
> = z.object({
  nextToken: z.string().optional(),
  currentToken: z.string().optional(),
  limit: z.number().int().optional(),
  totalCount: z.number().int().optional(),
  _links: PaginationLinksJson$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
  });
});

export function pagingJson25FromJSON(
  jsonString: string,
): SafeParseResult<PagingJson25, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PagingJson25$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PagingJson25' from JSON`,
  );
}
