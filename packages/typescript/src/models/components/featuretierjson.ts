/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { TierJson, TierJson$inboundSchema } from "./tierjson.js";

/**
 * Tier available for the feature
 */
export type FeatureTierJson = {
  /**
   * Represents the type of license that the user can choose.
   */
  name: TierJson;
  /**
   * If a particular tier is locked to perform any mutation over it
   */
  locked?: boolean | undefined;
  /**
   * Generic message informing tier to be locked due to some feature in use
   */
  lockReason?: string | undefined;
};

/** @internal */
export const FeatureTierJson$inboundSchema: z.ZodType<
  FeatureTierJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: TierJson$inboundSchema,
  locked: z.boolean().optional(),
  lockReason: z.string().optional(),
});

export function featureTierJsonFromJSON(
  jsonString: string,
): SafeParseResult<FeatureTierJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeatureTierJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeatureTierJson' from JSON`,
  );
}
