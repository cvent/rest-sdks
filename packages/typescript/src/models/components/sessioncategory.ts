/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * This is used to denote the category of a session.
 */
export type SessionCategory = {
  /**
   * A string that represents a specified session category.
   */
  id?: string | undefined;
  /**
   * The name of the session category.
   */
  name: string;
  /**
   * A description for the session category.
   */
  description?: string | undefined;
};

/** @internal */
export const SessionCategory$inboundSchema: z.ZodType<
  SessionCategory,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  name: z.string(),
  description: z.string().optional(),
});

export function sessionCategoryFromJSON(
  jsonString: string,
): SafeParseResult<SessionCategory, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionCategory$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionCategory' from JSON`,
  );
}
