/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ScheduleAction,
  ScheduleAction$inboundSchema,
} from "./scheduleaction.js";

/**
 * A scheduled action.
 */
export type Schedule = {
  /**
   * Unique ID of the scheduled task.
   */
  id?: string | undefined;
  /**
   * The ISO 8601 zoned date time when the session will be moved to the next stage: provisioning, activation, deactivation or archival
   */
  scheduledExecution?: Date | undefined;
  /**
   * This is used to denote the action to perform with the scheduled task
   */
  action?: ScheduleAction | undefined;
};

/** @internal */
export const Schedule$inboundSchema: z.ZodType<
  Schedule,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  scheduledExecution: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ).optional(),
  action: ScheduleAction$inboundSchema.optional(),
});

export function scheduleFromJSON(
  jsonString: string,
): SafeParseResult<Schedule, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Schedule$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Schedule' from JSON`,
  );
}
