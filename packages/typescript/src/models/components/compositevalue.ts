/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BudgetEstimate,
  BudgetEstimate$inboundSchema,
} from "./budgetestimate.js";
import {
  MeetingRoomRequirement,
  MeetingRoomRequirement$inboundSchema,
} from "./meetingroomrequirement.js";
import {
  SleepingRoomRequirement,
  SleepingRoomRequirement$inboundSchema,
} from "./sleepingroomrequirement.js";

/**
 * The structured representation of a complex question answer. Contains the same data as `value`, parsed into typed objects instead of JSON-serialized strings. This field is **read-only** — to write complex question answers, use `value` instead.
 *
 * @remarks
 *
 * The structure of the array depends on the question `type`.
 */
export type CompositeValue =
  | Array<MeetingRoomRequirement>
  | Array<SleepingRoomRequirement>
  | Array<BudgetEstimate>;

/** @internal */
export const CompositeValue$inboundSchema: z.ZodType<
  CompositeValue,
  z.ZodTypeDef,
  unknown
> = z.union([
  z.array(MeetingRoomRequirement$inboundSchema),
  z.array(SleepingRoomRequirement$inboundSchema),
  z.array(BudgetEstimate$inboundSchema),
]);

export function compositeValueFromJSON(
  jsonString: string,
): SafeParseResult<CompositeValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompositeValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompositeValue' from JSON`,
  );
}
