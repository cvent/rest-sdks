/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  RoomBlockDetails,
  RoomBlockDetails$inboundSchema,
} from "./roomblockdetails.js";

/**
 * Contains details related to room allotment for a day.
 */
export type RoomBlock = {
  /**
   * Unique identifier for the room block.
   */
  id?: string | undefined;
  /**
   * The ISO 8601 date when the rooms are required.
   */
  date?: RFCDate | undefined;
  /**
   * Counter for day number.
   */
  dayNumber?: number | undefined;
  /**
   * Total number of rooms required for the day.
   */
  totalRooms?: number | undefined;
  /**
   * Contains detail of quantity required for each room type.
   */
  details?: Array<RoomBlockDetails> | undefined;
};

/** @internal */
export const RoomBlock$inboundSchema: z.ZodType<
  RoomBlock,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  date: z.string().transform(v => new RFCDate(v)).optional(),
  dayNumber: z.number().int().optional(),
  totalRooms: z.number().int().optional(),
  details: z.array(RoomBlockDetails$inboundSchema).optional(),
});

export function roomBlockFromJSON(
  jsonString: string,
): SafeParseResult<RoomBlock, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RoomBlock$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RoomBlock' from JSON`,
  );
}
