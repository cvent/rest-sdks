/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Dates on which event will occur.
 */
export type DatesJson0 = {
  /**
   * True indicates these are preferred dates.
   */
  preferred?: boolean | undefined;
  /**
   * Unique identifier for dates.
   */
  id?: string | undefined;
  /**
   * The ISO 8601 end date of the event.
   */
  endDate?: RFCDate | undefined;
  /**
   * The ISO 8601 start date of the event.
   */
  startDate?: RFCDate | undefined;
  /**
   * A note detailing the requirements for the dates.
   */
  note?: string | undefined;
};

/** @internal */
export const DatesJson0$inboundSchema: z.ZodType<
  DatesJson0,
  z.ZodTypeDef,
  unknown
> = z.object({
  preferred: z.boolean().optional(),
  id: z.string().optional(),
  endDate: z.string().transform(v => new RFCDate(v)).optional(),
  startDate: z.string().transform(v => new RFCDate(v)).optional(),
  note: z.string().optional(),
});

export function datesJson0FromJSON(
  jsonString: string,
): SafeParseResult<DatesJson0, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DatesJson0$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DatesJson0' from JSON`,
  );
}
