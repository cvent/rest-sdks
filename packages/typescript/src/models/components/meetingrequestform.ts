/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  MeetingRequestCustomFieldJson,
  MeetingRequestCustomFieldJson$inboundSchema,
} from "./meetingrequestcustomfieldjson.js";

/**
 * A meeting request form.
 */
export type MeetingRequestForm = {
  /**
   * The ID of the meeting request form.
   */
  id: string;
  /**
   * User-defined name of the meeting request form.
   */
  name: string;
  /**
   * User-defined list of statuses for the meeting request form.
   */
  statuses?: Array<string> | undefined;
  /**
   * A boolean indicating a meeting request form's active status. When true, the meeting request form is available for new request submissions. When false, only updates can be made to existing requests for this form
   */
  active?: boolean | undefined;
  /**
   * The list of questions that will need to be answered by someone creating a meeting request from this form.
   */
  questions?: Array<MeetingRequestCustomFieldJson> | undefined;
};

/** @internal */
export const MeetingRequestForm$inboundSchema: z.ZodType<
  MeetingRequestForm,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  name: z.string(),
  statuses: z.array(z.string()).optional(),
  active: z.boolean().optional(),
  questions: z.array(MeetingRequestCustomFieldJson$inboundSchema).optional(),
});

export function meetingRequestFormFromJSON(
  jsonString: string,
): SafeParseResult<MeetingRequestForm, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MeetingRequestForm$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MeetingRequestForm' from JSON`,
  );
}
