/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The reference to the related entity. Contains only the ID of the related entity.
 */
export type Uuid = {
  /**
   * A string that has to be a format matching the industry standard uuid
   */
  id: string;
};

/** @internal */
export const Uuid$inboundSchema: z.ZodType<Uuid, z.ZodTypeDef, unknown> = z
  .object({
    id: z.string(),
  });
/** @internal */
export type Uuid$Outbound = {
  id: string;
};

/** @internal */
export const Uuid$outboundSchema: z.ZodType<Uuid$Outbound, z.ZodTypeDef, Uuid> =
  z.object({
    id: z.string(),
  });

export function uuidToJSON(uuid: Uuid): string {
  return JSON.stringify(Uuid$outboundSchema.parse(uuid));
}
export function uuidFromJSON(
  jsonString: string,
): SafeParseResult<Uuid, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Uuid$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Uuid' from JSON`,
  );
}
