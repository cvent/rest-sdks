/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Representation of a leg detail in air request.
 */
export type LegDetail = {
  /**
   * Name of the airport where the flight of the passenger's journey originates.
   */
  origin?: string | undefined;
  /**
   * Name of the airport where the flight of the passenger's journey ends.
   */
  destination?: string | undefined;
  /**
   * The ISO 8601 zoned date time (in UTC) of the flight origin.
   */
  date?: Date | undefined;
  /**
   * General time of day or specific time when the flight of the passenger's journey starts.
   */
  time?: string | undefined;
};

/** @internal */
export const LegDetail$inboundSchema: z.ZodType<
  LegDetail,
  z.ZodTypeDef,
  unknown
> = z.object({
  origin: z.string().optional(),
  destination: z.string().optional(),
  date: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  time: z.string().optional(),
});

export function legDetailFromJSON(
  jsonString: string,
): SafeParseResult<LegDetail, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LegDetail$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LegDetail' from JSON`,
  );
}
