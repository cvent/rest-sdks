/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ReservationNightRateJson,
  ReservationNightRateJson$inboundSchema,
} from "./reservationnightratejson.js";

/**
 * Indicator of night's status.
 */
export const ReservationNightJsonStatus = {
  Confirmed: "confirmed",
  Waitlisted: "waitlisted",
} as const;
/**
 * Indicator of night's status.
 */
export type ReservationNightJsonStatus = ClosedEnum<
  typeof ReservationNightJsonStatus
>;

/**
 * Reservation night details.
 */
export type ReservationNightJson = {
  /**
   * Night date.
   */
  stayDate: RFCDate;
  /**
   * Indicator of night's status.
   */
  status: ReservationNightJsonStatus;
  /**
   * True indicates the rates are hidden for this night.
   */
  hideRates?: boolean | undefined;
  /**
   * Rate details for a single reservation night.
   */
  rate: ReservationNightRateJson;
};

/** @internal */
export const ReservationNightJsonStatus$inboundSchema: z.ZodNativeEnum<
  typeof ReservationNightJsonStatus
> = z.nativeEnum(ReservationNightJsonStatus);

/** @internal */
export const ReservationNightJson$inboundSchema: z.ZodType<
  ReservationNightJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  stayDate: z.string().transform(v => new RFCDate(v)),
  status: ReservationNightJsonStatus$inboundSchema,
  hideRates: z.boolean().optional(),
  rate: ReservationNightRateJson$inboundSchema,
});

export function reservationNightJsonFromJSON(
  jsonString: string,
): SafeParseResult<ReservationNightJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReservationNightJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReservationNightJson' from JSON`,
  );
}
