/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DistributionListSecurityType,
  DistributionListSecurityType$inboundSchema,
} from "./distributionlistsecuritytype.js";
import {
  DistributionListStatus,
  DistributionListStatus$inboundSchema,
} from "./distributionliststatus.js";

/**
 * Model representing a distribution list info. Only required if the contact group type is DISTRIBUTION_LIST.
 */
export type DistributionListInfo = {
  /**
   * Type of distribution list security. For a public list, contacts can sign up themselves. For a semi-private list, contacts can add themselves, but a planner must approve their membership before they can receive emails. For a private list, only planners can add contacts. Contacts can always remove themselves from lists on the Manage Profile page.
   */
  securityType?: DistributionListSecurityType | undefined;
  /**
   * Internal note for Distribution Lists.
   */
  internalNote?: string | undefined;
  /**
   * Type of distribution list status.
   */
  status?: DistributionListStatus | undefined;
};

/** @internal */
export const DistributionListInfo$inboundSchema: z.ZodType<
  DistributionListInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  securityType: DistributionListSecurityType$inboundSchema.optional(),
  internalNote: z.string().optional(),
  status: DistributionListStatus$inboundSchema.optional(),
});

export function distributionListInfoFromJSON(
  jsonString: string,
): SafeParseResult<DistributionListInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DistributionListInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DistributionListInfo' from JSON`,
  );
}
