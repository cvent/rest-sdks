/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SwitchBackTargetUsagePlanJson,
  SwitchBackTargetUsagePlanJson$inboundSchema,
} from "./switchbacktargetusageplanjson.js";

/**
 * Object representing an API Platform Usage Tier, including information when the usage plan is scheduled to revert to a previous plan, if any.
 */
export type PublicUsagePlan = {
  /**
   * The name of the usage tier
   */
  name: string;
  /**
   * The daily quota of the usage tier. If this value is null, this means the usage tier is unlimited.
   */
  quota: number;
  /**
   * The burst limit capacity of the usage tier
   */
  burstLimit: number;
  /**
   * The per-second steady rate-limit of the usage tier
   */
  rateLimit: number;
  /**
   * Object representing the target usage plan and the date when the current plan is scheduled to revert to it. This object is only present when a switch-back is scheduled.
   */
  switchBackPlan?: SwitchBackTargetUsagePlanJson | undefined;
};

/** @internal */
export const PublicUsagePlan$inboundSchema: z.ZodType<
  PublicUsagePlan,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  quota: z.number(),
  burstLimit: z.number(),
  rateLimit: z.number(),
  switchBackPlan: SwitchBackTargetUsagePlanJson$inboundSchema.optional(),
});

export function publicUsagePlanFromJSON(
  jsonString: string,
): SafeParseResult<PublicUsagePlan, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicUsagePlan$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicUsagePlan' from JSON`,
  );
}
