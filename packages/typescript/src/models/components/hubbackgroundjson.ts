/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The background image of the Events+ hub.
 */
export type HubBackgroundJson = {
  /**
   * The optimized URL of the Events+ hub's background image. The background image appears on the Events+ hub registration page.
   */
  url?: string | undefined;
  /**
   * The original URL of the Events+ hub background image. The background appears on the Events+ hub Registration page.
   */
  originalUrl?: string | undefined;
  /**
   * Alternate text for the background image.
   */
  altText?: string | undefined;
};

/** @internal */
export const HubBackgroundJson$inboundSchema: z.ZodType<
  HubBackgroundJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  url: z.string().optional(),
  originalUrl: z.string().optional(),
  altText: z.string().optional(),
});

export function hubBackgroundJsonFromJSON(
  jsonString: string,
): SafeParseResult<HubBackgroundJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HubBackgroundJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HubBackgroundJson' from JSON`,
  );
}
