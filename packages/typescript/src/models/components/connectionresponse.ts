/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Status of the connection. `REQUESTED` indicates the connection request is being processed. `NOT_CONNECTED` indicates there is no current connection. `CONNECTED` indicates this housing event is connected to a registration event.
 */
export const ConnectionResponseStatus = {
  NotConnected: "NOT_CONNECTED",
  Connected: "CONNECTED",
  Requested: "REQUESTED",
} as const;
/**
 * Status of the connection. `REQUESTED` indicates the connection request is being processed. `NOT_CONNECTED` indicates there is no current connection. `CONNECTED` indicates this housing event is connected to a registration event.
 */
export type ConnectionResponseStatus = ClosedEnum<
  typeof ConnectionResponseStatus
>;

/**
 * Connection response.
 */
export type ConnectionResponse = {
  /**
   * ID for a housing event in Passkey.
   */
  eventId: number;
  /**
   * Status of the connection. `REQUESTED` indicates the connection request is being processed. `NOT_CONNECTED` indicates there is no current connection. `CONNECTED` indicates this housing event is connected to a registration event.
   */
  status: ConnectionResponseStatus;
  /**
   * True indicates the integrated partner has callbacks enabled by default.
   */
  defaultCallbacks: boolean;
};

/** @internal */
export const ConnectionResponseStatus$inboundSchema: z.ZodNativeEnum<
  typeof ConnectionResponseStatus
> = z.nativeEnum(ConnectionResponseStatus);

/** @internal */
export const ConnectionResponse$inboundSchema: z.ZodType<
  ConnectionResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  eventId: z.number().int(),
  status: ConnectionResponseStatus$inboundSchema,
  defaultCallbacks: z.boolean(),
});

export function connectionResponseFromJSON(
  jsonString: string,
): SafeParseResult<ConnectionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectionResponse' from JSON`,
  );
}
