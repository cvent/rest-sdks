/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Specifies if the amount is a fixed value or a percentage.
 */
export const AmountType = {
  Amount: "amount",
  Percentage: "percentage",
} as const;
/**
 * Specifies if the amount is a fixed value or a percentage.
 */
export type AmountType = ClosedEnum<typeof AmountType>;

/**
 * Defines how often the tax or fee is applied.
 *
 * @remarks
 * - per_guest: Applied per guest.
 * - per_room: Applied per room.
 * - per_night: Applied per night.
 * - per_stay: Applied per stay.
 */
export const Frequency = {
  PerGuest: "per_guest",
  PerRoom: "per_room",
  PerNight: "per_night",
  PerStay: "per_stay",
} as const;
/**
 * Defines how often the tax or fee is applied.
 *
 * @remarks
 * - per_guest: Applied per guest.
 * - per_room: Applied per room.
 * - per_night: Applied per night.
 * - per_stay: Applied per stay.
 */
export type Frequency = ClosedEnum<typeof Frequency>;

/**
 * A single tax or fee applicable to a room.
 */
export type TaxAndFeeJson = {
  /**
   * Name of the tax or fee.
   */
  name: string;
  /**
   * Amount or percentage value of the tax or fee.
   */
  amount: number;
  /**
   * Specifies if the amount is a fixed value or a percentage.
   */
  amountType: AmountType;
  /**
   * Defines how often the tax or fee is applied.
   *
   * @remarks
   * - per_guest: Applied per guest.
   * - per_room: Applied per room.
   * - per_night: Applied per night.
   * - per_stay: Applied per stay.
   */
  frequency: Frequency;
  /**
   * Indicates if the tax or fee is included in the base room rate set by the hotel (before any taxes or fees are applied).
   *
   * @remarks
   *
   * - true: The tax or fee is already part of the nightly rate set by the hotel.
   * - false: The tax or fee will be added to the hotel nightly rate as a separate charge.
   */
  partOfBaseRate: boolean;
  /**
   * Indicates if the tax or fee is included in the displayed rate shown to the guest.
   *
   * @remarks
   *
   * - true: The tax or fee is included in the displayed rate as part of the room rate. (Displayed rate = base room rate + tax/fee.)
   * - false: The tax or fee is listed separately at the end of the booking process. (Displayed rate is the room rate only.)
   */
  partOfDisplayRate: boolean;
};

/** @internal */
export const AmountType$inboundSchema: z.ZodNativeEnum<typeof AmountType> = z
  .nativeEnum(AmountType);

/** @internal */
export const Frequency$inboundSchema: z.ZodNativeEnum<typeof Frequency> = z
  .nativeEnum(Frequency);

/** @internal */
export const TaxAndFeeJson$inboundSchema: z.ZodType<
  TaxAndFeeJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  amount: z.number(),
  amountType: AmountType$inboundSchema,
  frequency: Frequency$inboundSchema,
  partOfBaseRate: z.boolean(),
  partOfDisplayRate: z.boolean(),
});

export function taxAndFeeJsonFromJSON(
  jsonString: string,
): SafeParseResult<TaxAndFeeJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxAndFeeJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxAndFeeJson' from JSON`,
  );
}
