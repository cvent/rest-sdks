/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Early bird pricing for a fee, where attendees receive a discounted fee if they register before a given date.
 */
export type EarlyBirdPricingJson = {
  /**
   * The ID of an early bird price.
   */
  id?: string | undefined;
  /**
   * Fee amount for an early bird price.
   */
  amount?: number | undefined;
  /**
   * The ISO 8601 formatted date by which attendees should register to receive the early bird price.
   */
  registerByDate?: RFCDate | undefined;
  /**
   * Maximum refund amount for an early bird price.
   */
  maxRefund?: number | undefined;
};

/** @internal */
export const EarlyBirdPricingJson$inboundSchema: z.ZodType<
  EarlyBirdPricingJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  amount: z.number().optional(),
  registerByDate: z.string().transform(v => new RFCDate(v)).optional(),
  maxRefund: z.number().optional(),
});

export function earlyBirdPricingJsonFromJSON(
  jsonString: string,
): SafeParseResult<EarlyBirdPricingJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EarlyBirdPricingJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EarlyBirdPricingJson' from JSON`,
  );
}
