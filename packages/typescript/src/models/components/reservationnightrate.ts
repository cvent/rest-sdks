/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdditionalGuestFees,
  AdditionalGuestFees$inboundSchema,
} from "./additionalguestfees.js";

/**
 * Rate details for a single reservation night.
 */
export type ReservationNightRate = {
  /**
   * Base rate.
   */
  base: number;
  /**
   * ISO 4217 currency code.
   */
  currency: string;
  /**
   * List of additional guest fees.
   */
  additionalGuestFees?: Array<AdditionalGuestFees> | undefined;
  /**
   * The room rate displayed to guests, which includes any fees configured to display in the hotel tax settings.
   */
  displayRate?: number | undefined;
};

/** @internal */
export const ReservationNightRate$inboundSchema: z.ZodType<
  ReservationNightRate,
  z.ZodTypeDef,
  unknown
> = z.object({
  base: z.number(),
  currency: z.string(),
  additionalGuestFees: z.array(AdditionalGuestFees$inboundSchema).optional(),
  displayRate: z.number().optional(),
});

export function reservationNightRateFromJSON(
  jsonString: string,
): SafeParseResult<ReservationNightRate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReservationNightRate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReservationNightRate' from JSON`,
  );
}
