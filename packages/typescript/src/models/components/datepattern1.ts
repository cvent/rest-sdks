/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Dates1, Dates1$inboundSchema } from "./dates1.js";
import { DayOfWeek, DayOfWeek$inboundSchema } from "./dayofweek.js";

/**
 * Date pattern for days on which event can occur. Used when dateMode is DATE_PATTERN.
 */
export type DatePattern1 = {
  /**
   * Note along with date pattern requirements.
   */
  note?: string | undefined;
  /**
   * The ISO 8601 end date of the event.
   */
  endDate: RFCDate;
  /**
   * The ISO 8601 start date of the event.
   */
  startDate: RFCDate;
  /**
   * Number of days the event will go on.
   */
  durationDays: number;
  /**
   * Days of the week when event can start.
   */
  startDays: Array<DayOfWeek>;
  /**
   * List of dates generated based on the date pattern.
   */
  generatedDates?: Array<Dates1> | undefined;
};

/** @internal */
export const DatePattern1$inboundSchema: z.ZodType<
  DatePattern1,
  z.ZodTypeDef,
  unknown
> = z.object({
  note: z.string().optional(),
  endDate: z.string().transform(v => new RFCDate(v)),
  startDate: z.string().transform(v => new RFCDate(v)),
  durationDays: z.number().int(),
  startDays: z.array(DayOfWeek$inboundSchema),
  generatedDates: z.array(Dates1$inboundSchema).optional(),
});

export function datePattern1FromJSON(
  jsonString: string,
): SafeParseResult<DatePattern1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DatePattern1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DatePattern1' from JSON`,
  );
}
