/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A JSON object representing an electronic device used to capture leads.
 */
export type LeadDeviceJson = {
  /**
   * The id of device used to capture lead
   */
  id?: string | undefined;
  /**
   * The name of device used to capture lead
   */
  name?: string | undefined;
};

/** @internal */
export const LeadDeviceJson$inboundSchema: z.ZodType<
  LeadDeviceJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  name: z.string().optional(),
});

export function leadDeviceJsonFromJSON(
  jsonString: string,
): SafeParseResult<LeadDeviceJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LeadDeviceJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LeadDeviceJson' from JSON`,
  );
}
