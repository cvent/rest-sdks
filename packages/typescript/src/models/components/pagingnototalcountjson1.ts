/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  PaginationLinksJson,
  PaginationLinksJson$inboundSchema,
} from "./paginationlinksjson.js";

/**
 * Represents pagination information for a collection of resources.
 */
export type PagingNoTotalCountJson1 = {
  /**
   * The pagination token for the previous page, if one exists. You can use this token to navigate to the previous page of data.
   */
  previousToken?: string | undefined;
  /**
   * The pagination token for the next page. If this value is present in the response, there is another page of data you can fetch.
   */
  nextToken?: string | undefined;
  /**
   * The pagination token for the current page.
   */
  currentToken?: string | undefined;
  /**
   * The number of records to return on the page. Not to exceed 200.
   */
  limit?: number | undefined;
  /**
   * Represents pagination links for navigating between pages of data.
   */
  links: PaginationLinksJson;
};

/** @internal */
export const PagingNoTotalCountJson1$inboundSchema: z.ZodType<
  PagingNoTotalCountJson1,
  z.ZodTypeDef,
  unknown
> = z.object({
  previousToken: z.string().optional(),
  nextToken: z.string().optional(),
  currentToken: z.string().optional(),
  limit: z.number().int().optional(),
  _links: PaginationLinksJson$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
  });
});

export function pagingNoTotalCountJson1FromJSON(
  jsonString: string,
): SafeParseResult<PagingNoTotalCountJson1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PagingNoTotalCountJson1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PagingNoTotalCountJson1' from JSON`,
  );
}
