/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * JSON schema for the Lead object
 */
export type LeadJson = {
  /**
   * Lead id
   */
  id: string;
};

/** @internal */
export const LeadJson$inboundSchema: z.ZodType<
  LeadJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
});

export function leadJsonFromJSON(
  jsonString: string,
): SafeParseResult<LeadJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LeadJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LeadJson' from JSON`,
  );
}
