/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Address1, Address1$inboundSchema } from "./address1.js";
import {
  ContactMethods,
  ContactMethods$inboundSchema,
} from "./contactmethods.js";

/**
 * Contact details of planner.
 */
export type Contact1 = {
  /**
   * The first name of the planner.
   */
  firstName: string;
  /**
   * The last name of the planner.
   */
  lastName: string;
  /**
   * Email address of the planner.
   */
  email: string;
  /**
   * Address details.
   */
  address?: Address1 | undefined;
  /**
   * Rate of commission chargeable by the planner. Precision up to 2 decimal places is accepted.
   */
  commissionRate?: number | undefined;
  /**
   * True indicates commission is to be charged from supplier.
   */
  commissionable?: boolean | undefined;
  /**
   * Phone number of the planner.
   */
  phone: string;
  /**
   * Name of the organization.
   */
  organizationName: string;
  /**
   * True indicates planner is a third party planner.
   */
  thirdPartyPlanner?: boolean | undefined;
  /**
   * List of preferred contact methods for the planner.
   */
  preferredContactMethods?: Array<ContactMethods> | undefined;
};

/** @internal */
export const Contact1$inboundSchema: z.ZodType<
  Contact1,
  z.ZodTypeDef,
  unknown
> = z.object({
  firstName: z.string(),
  lastName: z.string(),
  email: z.string(),
  address: Address1$inboundSchema.optional(),
  commissionRate: z.number().optional(),
  commissionable: z.boolean().optional(),
  phone: z.string(),
  organizationName: z.string(),
  thirdPartyPlanner: z.boolean().optional(),
  preferredContactMethods: z.array(ContactMethods$inboundSchema).optional(),
});

export function contact1FromJSON(
  jsonString: string,
): SafeParseResult<Contact1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Contact1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Contact1' from JSON`,
  );
}
