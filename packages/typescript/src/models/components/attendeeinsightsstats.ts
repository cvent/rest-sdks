/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Attendee insights statistics.
 */
export type AttendeeInsightsStats = {
  /**
   * Average engagement score across all attendees.
   */
  averageAttendeeScore: number;
  /**
   * Count of attendees who've participated in an engagement score.
   */
  attendeeCount: number;
  /**
   * Count of all activities included in an engagement score.
   */
  activityCount: number;
};

/** @internal */
export const AttendeeInsightsStats$inboundSchema: z.ZodType<
  AttendeeInsightsStats,
  z.ZodTypeDef,
  unknown
> = z.object({
  averageAttendeeScore: z.number().int().default(0),
  attendeeCount: z.number().int().default(0),
  activityCount: z.number().int().default(0),
});

export function attendeeInsightsStatsFromJSON(
  jsonString: string,
): SafeParseResult<AttendeeInsightsStats, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AttendeeInsightsStats$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AttendeeInsightsStats' from JSON`,
  );
}
