/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * An existing speaker category.
 */
export type ExistingSpeakerCategory = {
  /**
   * The name of the speaker category
   */
  name?: string | undefined;
  /**
   * A string that has to be a format matching the industry standard uuid
   */
  id: string;
  /**
   * The speaker category is active or not active
   */
  active?: boolean | undefined;
};

/** @internal */
export const ExistingSpeakerCategory$inboundSchema: z.ZodType<
  ExistingSpeakerCategory,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string().optional(),
  id: z.string(),
  active: z.boolean().optional(),
});

export function existingSpeakerCategoryFromJSON(
  jsonString: string,
): SafeParseResult<ExistingSpeakerCategory, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExistingSpeakerCategory$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExistingSpeakerCategory' from JSON`,
  );
}
