/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Unique URL for a guest to access a reservation in Passkey's booking tool.
 */
export type BookingWebsiteLinkJson = {
  /**
   * A url provided that can be followed for linking
   */
  href: string;
};

/** @internal */
export const BookingWebsiteLinkJson$inboundSchema: z.ZodType<
  BookingWebsiteLinkJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  href: z.string(),
});

export function bookingWebsiteLinkJsonFromJSON(
  jsonString: string,
): SafeParseResult<BookingWebsiteLinkJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BookingWebsiteLinkJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BookingWebsiteLinkJson' from JSON`,
  );
}
