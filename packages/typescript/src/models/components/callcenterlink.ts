/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Unique URL for Passkey users (rather than guests) to access a reservation in Passkey's call center tool.
 */
export type CallCenterLink = {
  /**
   * A url provided that can be followed for linking
   */
  href: string;
};

/** @internal */
export const CallCenterLink$inboundSchema: z.ZodType<
  CallCenterLink,
  z.ZodTypeDef,
  unknown
> = z.object({
  href: z.string(),
});

export function callCenterLinkFromJSON(
  jsonString: string,
): SafeParseResult<CallCenterLink, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CallCenterLink$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CallCenterLink' from JSON`,
  );
}
