/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The type of target associated with the banner link.
 */
export const BannerLinkTargetJsonType = {
  AttendeeHubPage: "Attendee Hub Page",
  DiscussionTopic: "Discussion Topic",
  ExternalUrl: "External Url",
  FloorPlanDetails: "Floor Plan Details",
  SessionDetails: "Session Details",
  SpeakerDetails: "Speaker Details",
  VirtualBooth: "Virtual Booth",
} as const;
/**
 * The type of target associated with the banner link.
 */
export type BannerLinkTargetJsonType = ClosedEnum<
  typeof BannerLinkTargetJsonType
>;

/**
 * The target of the link clicked on the banner.
 */
export type BannerLinkTargetJson = {
  /**
   * The type of target associated with the banner link.
   */
  type?: BannerLinkTargetJsonType | undefined;
  /**
   * The details of the target associated with the banner link.
   */
  details?: string | undefined;
};

/** @internal */
export const BannerLinkTargetJsonType$inboundSchema: z.ZodNativeEnum<
  typeof BannerLinkTargetJsonType
> = z.nativeEnum(BannerLinkTargetJsonType);

/** @internal */
export const BannerLinkTargetJson$inboundSchema: z.ZodType<
  BannerLinkTargetJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: BannerLinkTargetJsonType$inboundSchema.optional(),
  details: z.string().optional(),
});

export function bannerLinkTargetJsonFromJSON(
  jsonString: string,
): SafeParseResult<BannerLinkTargetJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BannerLinkTargetJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BannerLinkTargetJson' from JSON`,
  );
}
