/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Day contains a date and a counter
 */
export type Day = {
  /**
   * Counter for number of Days
   */
  dayNumber?: number | undefined;
  /**
   * The ISO 8601 date of the booking
   */
  date?: RFCDate | undefined;
};

/** @internal */
export const Day$inboundSchema: z.ZodType<Day, z.ZodTypeDef, unknown> = z
  .object({
    dayNumber: z.number().int().optional(),
    date: z.string().transform(v => new RFCDate(v)).optional(),
  });
/** @internal */
export type Day$Outbound = {
  dayNumber?: number | undefined;
  date?: string | undefined;
};

/** @internal */
export const Day$outboundSchema: z.ZodType<Day$Outbound, z.ZodTypeDef, Day> = z
  .object({
    dayNumber: z.number().int().optional(),
    date: z.instanceof(RFCDate).transform(v => v.toString()).optional(),
  });

export function dayToJSON(day: Day): string {
  return JSON.stringify(Day$outboundSchema.parse(day));
}
export function dayFromJSON(
  jsonString: string,
): SafeParseResult<Day, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Day$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Day' from JSON`,
  );
}
