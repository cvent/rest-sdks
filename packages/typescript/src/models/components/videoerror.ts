/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  VideoErrorCode,
  VideoErrorCode$inboundSchema,
} from "./videoerrorcode.js";

/**
 * This is used to denote the errors for a video entity.
 */
export type VideoError = {
  /**
   * This is used to denote the error codes for a video entity.
   */
  code: VideoErrorCode;
  /**
   * Error message for the video. Message is a developer convenience never to be used in any context that needs localisation, subject to change at any time.
   */
  message: string;
};

/** @internal */
export const VideoError$inboundSchema: z.ZodType<
  VideoError,
  z.ZodTypeDef,
  unknown
> = z.object({
  code: VideoErrorCode$inboundSchema,
  message: z.string(),
});

export function videoErrorFromJSON(
  jsonString: string,
): SafeParseResult<VideoError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VideoError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VideoError' from JSON`,
  );
}
