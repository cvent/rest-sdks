/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdditionalGuestFeesJson,
  AdditionalGuestFeesJson$inboundSchema,
} from "./additionalguestfeesjson.js";

/**
 * Rate details for a single reservation night.
 */
export type ReservationNightRateJson = {
  /**
   * Base rate.
   */
  base: number;
  /**
   * ISO 4217 currency code.
   */
  currency: string;
  /**
   * List of additional guest fees.
   */
  additionalGuestFees?: Array<AdditionalGuestFeesJson> | undefined;
  /**
   * The room rate displayed to guests, which includes any fees configured to display in the hotel tax settings.
   */
  displayRate?: number | undefined;
};

/** @internal */
export const ReservationNightRateJson$inboundSchema: z.ZodType<
  ReservationNightRateJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  base: z.number(),
  currency: z.string(),
  additionalGuestFees: z.array(AdditionalGuestFeesJson$inboundSchema)
    .optional(),
  displayRate: z.number().optional(),
});

export function reservationNightRateJsonFromJSON(
  jsonString: string,
): SafeParseResult<ReservationNightRateJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReservationNightRateJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReservationNightRateJson' from JSON`,
  );
}
