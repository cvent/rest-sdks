/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";

/**
 * How the rate is applied to the meeting room booking.
 *
 * @remarks
 *
 * Values:
 * - `COMPLIMENTARY` — Complimentary
 * - `INCLUDED` — Included
 * - `ONETIME_FEE` — One-Time Fee
 * - `PER_DAY` — Per Day
 * - `PER_HOUR` — Per Hour
 * - `PER_SQ_FT` — Per Square Foot
 * - `PER_SQ_MTR` — Per Square Meter
 * - `TOTAL` — Total
 */
export const RateApplicationType = {
  Complimentary: "COMPLIMENTARY",
  Included: "INCLUDED",
  OnetimeFee: "ONETIME_FEE",
  PerDay: "PER_DAY",
  PerHour: "PER_HOUR",
  PerSqFt: "PER_SQ_FT",
  PerSqMtr: "PER_SQ_MTR",
  Total: "TOTAL",
} as const;
/**
 * How the rate is applied to the meeting room booking.
 *
 * @remarks
 *
 * Values:
 * - `COMPLIMENTARY` — Complimentary
 * - `INCLUDED` — Included
 * - `ONETIME_FEE` — One-Time Fee
 * - `PER_DAY` — Per Day
 * - `PER_HOUR` — Per Hour
 * - `PER_SQ_FT` — Per Square Foot
 * - `PER_SQ_MTR` — Per Square Meter
 * - `TOTAL` — Total
 */
export type RateApplicationType = ClosedEnum<typeof RateApplicationType>;

/** @internal */
export const RateApplicationType$inboundSchema: z.ZodNativeEnum<
  typeof RateApplicationType
> = z.nativeEnum(RateApplicationType);
/** @internal */
export const RateApplicationType$outboundSchema: z.ZodNativeEnum<
  typeof RateApplicationType
> = RateApplicationType$inboundSchema;
