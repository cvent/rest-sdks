/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Attendee's contact membership information. Note: This field is deprecated. Previous documentation incorrectly listed support for this feature.
 *
 * @deprecated class: This will be removed in a future release, please migrate away from it as soon as possible.
 */
export type AttendeeContactMembershipJson = {
  /**
   * The type of membership the contact has signed up for.
   */
  type?: string | undefined;
  /**
   * Describes the specific plan the contact has signed up for.
   */
  plan?: string | undefined;
  /**
   * The ISO-8601 formatted date the contact began their membership.
   */
  joined?: RFCDate | undefined;
  /**
   * The ISO-8601 formatted date the contact's membership will expire.
   */
  expiration?: RFCDate | undefined;
  /**
   * The ISO-8601 formatted date the contact last renewed their membership.
   */
  lastRenewal?: RFCDate | undefined;
  /**
   * The amount currently due for this membership.
   */
  amountDue?: number | undefined;
  /**
   * True indicates the member details should be included in the member directory.
   */
  listInDirectory?: boolean | undefined;
};

/** @internal */
export const AttendeeContactMembershipJson$inboundSchema: z.ZodType<
  AttendeeContactMembershipJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: z.string().optional(),
  plan: z.string().optional(),
  joined: z.string().transform(v => new RFCDate(v)).optional(),
  expiration: z.string().transform(v => new RFCDate(v)).optional(),
  lastRenewal: z.string().transform(v => new RFCDate(v)).optional(),
  amountDue: z.number().optional(),
  listInDirectory: z.boolean().optional(),
});

export function attendeeContactMembershipJsonFromJSON(
  jsonString: string,
): SafeParseResult<AttendeeContactMembershipJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AttendeeContactMembershipJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AttendeeContactMembershipJson' from JSON`,
  );
}
