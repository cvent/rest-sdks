/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DiscountMethod,
  DiscountMethod$inboundSchema,
} from "./discountmethod.js";
import {
  VolumeDiscountThresholdType,
  VolumeDiscountThresholdType$inboundSchema,
} from "./volumediscountthresholdtype.js";

/**
 * Whether the discount is created at event level or account level.
 */
export const ExistingVolumeDiscountLevel = {
  Event: "EVENT",
  Account: "ACCOUNT",
} as const;
/**
 * Whether the discount is created at event level or account level.
 */
export type ExistingVolumeDiscountLevel = ClosedEnum<
  typeof ExistingVolumeDiscountLevel
>;

/**
 * Representation of an existing volume based discount.
 */
export type ExistingVolumeDiscount = {
  /**
   * The ISO 8601 zoned date time when this record was created.
   */
  created?: Date | undefined;
  /**
   * The identifier of the user that created this record.
   */
  createdBy?: string | undefined;
  /**
   * The ISO 8601 zoned date time when this record was updated.
   */
  lastModified?: Date | undefined;
  /**
   * The identifier of the user that last updated this record.
   */
  lastModifiedBy?: string | undefined;
  /**
   * Name of the discount.
   */
  name: string;
  /**
   * True indicates that the discount is active.
   */
  active: boolean;
  /**
   * True indicates that the discount can be clubbed together with other stackable discounts
   */
  stackable: boolean;
  /**
   * Details about how the discount applies.
   */
  method: DiscountMethod;
  /**
   * The ISO 8601 formatted date from which the discount is effective.
   */
  effectiveFrom?: RFCDate | undefined;
  /**
   * The ISO 8601 formatted date to which the discount is effective.
   */
  effectiveTo?: RFCDate | undefined;
  /**
   * Note about the discount.
   */
  note?: string | undefined;
  /**
   * Specifies who will receive the discount once the threshold for the number of ordered items is met or exceeded, depending on the type. `ALL`: All registrations receive the discount once the threshold is exceeded. `AFTER_THRESHOLD_LIMIT`: Every registration beyond the threshold receives the discount. `BEFORE_THRESHOLD_LIMIT:` Every registration, including those at the threshold, are discounted. Further registrations are at full price. If primaryRegistrant is not included then the first registrant is full price. Every registration afterward, up to and including the threshold, are discounted. Any additional registrations are at full price. `EVERY_NTH_REGISTRANT`: After reaching the threshold, every Nth registration counting from the threshold recieves a discount, where N is defined by the `interval` field.
   */
  thresholdType: VolumeDiscountThresholdType;
  /**
   * Defines the threshold limit for the volume discount.
   */
  thresholdLimit: number;
  /**
   * Defines the interval for the volume discount when threshold type is EVERY_NTH_REGISTRANT. Every Nth registration counting from the threshold receives a discount.
   */
  interval: number;
  /**
   * True indicates if the primary registrant is included in evaluating if threshold is met for the volume discount when threshold type is BEFORE_THRESHOLD_LIMIT.
   */
  includePrimaryRegistrant: boolean;
  /**
   * The type of discount. For account-level discounts, all discounts are of type DISCOUNT_CODE. `DISCOUNT_CODE`: A code the user or system applies to induce a discount. `VOLUME_DISCOUNT`: A discount that applies when a certain volume of items are purchased.
   */
  type: "VOLUME_DISCOUNT";
  /**
   * A unique identifier of the discount.
   */
  id?: string | undefined;
  /**
   * Whether the discount is created at event level or account level.
   */
  level?: ExistingVolumeDiscountLevel | undefined;
};

/** @internal */
export const ExistingVolumeDiscountLevel$inboundSchema: z.ZodNativeEnum<
  typeof ExistingVolumeDiscountLevel
> = z.nativeEnum(ExistingVolumeDiscountLevel);

/** @internal */
export const ExistingVolumeDiscount$inboundSchema: z.ZodType<
  ExistingVolumeDiscount,
  z.ZodTypeDef,
  unknown
> = z.object({
  created: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  createdBy: z.string().optional(),
  lastModified: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ).optional(),
  lastModifiedBy: z.string().optional(),
  name: z.string(),
  active: z.boolean().default(true),
  stackable: z.boolean().default(true),
  method: DiscountMethod$inboundSchema,
  effectiveFrom: z.string().transform(v => new RFCDate(v)).optional(),
  effectiveTo: z.string().transform(v => new RFCDate(v)).optional(),
  note: z.string().optional(),
  thresholdType: VolumeDiscountThresholdType$inboundSchema.default(
    "AFTER_THRESHOLD_LIMIT",
  ),
  thresholdLimit: z.number().int(),
  interval: z.number().int().default(1),
  includePrimaryRegistrant: z.boolean().default(false),
  type: z.literal("VOLUME_DISCOUNT"),
  id: z.string().optional(),
  level: ExistingVolumeDiscountLevel$inboundSchema.optional(),
});

export function existingVolumeDiscountFromJSON(
  jsonString: string,
): SafeParseResult<ExistingVolumeDiscount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExistingVolumeDiscount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExistingVolumeDiscount' from JSON`,
  );
}
