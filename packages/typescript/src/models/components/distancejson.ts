/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Unit of measurement for the distance.
 */
export const DistanceUnit = {
  Kilometers: "Kilometers",
  Meters: "Meters",
  Miles: "Miles",
  Yards: "Yards",
  Blocks: "Blocks",
} as const;
/**
 * Unit of measurement for the distance.
 */
export type DistanceUnit = ClosedEnum<typeof DistanceUnit>;

/**
 * Distance information.
 */
export type DistanceJson = {
  /**
   * Distance from the hotel to the venue.
   */
  distance: number;
  /**
   * Unit of measurement for the distance.
   */
  distanceUnit: DistanceUnit;
};

/** @internal */
export const DistanceUnit$inboundSchema: z.ZodNativeEnum<typeof DistanceUnit> =
  z.nativeEnum(DistanceUnit);

/** @internal */
export const DistanceJson$inboundSchema: z.ZodType<
  DistanceJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  distance: z.number(),
  distanceUnit: DistanceUnit$inboundSchema,
});

export function distanceJsonFromJSON(
  jsonString: string,
): SafeParseResult<DistanceJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DistanceJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DistanceJson' from JSON`,
  );
}
