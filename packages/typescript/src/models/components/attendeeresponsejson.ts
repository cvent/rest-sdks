/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SendEmailStatusJson,
  SendEmailStatusJson$inboundSchema,
} from "./sendemailstatusjson.js";

/**
 * Response object indicating the status of send email request for a particular attendee.
 */
export type AttendeeResponseJson = {
  /**
   * The ID of the attendee you're emailing.
   */
  id: string;
  /**
   * It is used to denote the status of the sent email.
   */
  status: SendEmailStatusJson;
  /**
   * The error message contains reason of a failed email.
   */
  errorMessage?: string | undefined;
};

/** @internal */
export const AttendeeResponseJson$inboundSchema: z.ZodType<
  AttendeeResponseJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  status: SendEmailStatusJson$inboundSchema,
  errorMessage: z.string().optional(),
});

export function attendeeResponseJsonFromJSON(
  jsonString: string,
): SafeParseResult<AttendeeResponseJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AttendeeResponseJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AttendeeResponseJson' from JSON`,
  );
}
