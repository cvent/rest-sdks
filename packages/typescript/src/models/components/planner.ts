/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Planner details.
 */
export type Planner = {
  /**
   * Planner first name.
   */
  firstName?: string | undefined;
  /**
   * Planner last name.
   */
  lastName?: string | undefined;
  /**
   * Planner email.
   */
  email?: string | undefined;
};

/** @internal */
export const Planner$inboundSchema: z.ZodType<Planner, z.ZodTypeDef, unknown> =
  z.object({
    firstName: z.string().optional(),
    lastName: z.string().optional(),
    email: z.string().optional(),
  });

export function plannerFromJSON(
  jsonString: string,
): SafeParseResult<Planner, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Planner$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Planner' from JSON`,
  );
}
