/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ProductTypeJson,
  ProductTypeJson$inboundSchema,
} from "./producttypejson.js";

/**
 * Represents a product. Products are items that can be purchased during registration.
 */
export type ProductJson = {
  /**
   * ID of the associated product.
   */
  id?: string | undefined;
  /**
   * Represents a product's type. `AdmissionItem`: Product is a ticket to an event, typically denotes the day(s) the individual is attending. Every attendee must have an admission item. `Session`: Product is a agenda item at the event. Typically denotes a talk, presentation, or other scheduled occurrence at the event. `SessionBundle`: Product is a session bundle that enrolls the attendee into all the sessions included in the bundle, sometimes at a discounted rate. `QuantityItem`: Product is a quantity of things an attendee is purchasing at an event, like t-shirts or raffle tickets.
   */
  type?: ProductTypeJson | undefined;
  /**
   * Name of the associated product.
   */
  name?: string | undefined;
};

/** @internal */
export const ProductJson$inboundSchema: z.ZodType<
  ProductJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  type: ProductTypeJson$inboundSchema.optional(),
  name: z.string().optional(),
});

export function productJsonFromJSON(
  jsonString: string,
): SafeParseResult<ProductJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProductJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProductJson' from JSON`,
  );
}
