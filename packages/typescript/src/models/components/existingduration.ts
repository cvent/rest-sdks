/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { UuidJson, UuidJson$inboundSchema } from "./uuidjson.js";

/**
 * Describes how the duration was obtained.
 */
export const ExistingDurationType = {
  Rfid: "RFID",
  Oa: "OA",
  Edit: "EDIT",
  Live: "LIVE",
  OnDemand: "ON_DEMAND",
  Api: "API",
} as const;
/**
 * Describes how the duration was obtained.
 */
export type ExistingDurationType = ClosedEnum<typeof ExistingDurationType>;

/**
 * The target entity for which the duration was collected.
 */
export const DurationType = {
  Session: "session",
  Appointment: "appointment",
  Exhibitor: "exhibitor",
} as const;
/**
 * The target entity for which the duration was collected.
 */
export type DurationType = ClosedEnum<typeof DurationType>;

/**
 * An existing duration.
 */
export type ExistingDuration = {
  /**
   * The ISO 8601 zoned date time when this record was created.
   */
  created?: Date | undefined;
  /**
   * The identifier of the user that created this record.
   */
  createdBy?: string | undefined;
  /**
   * The ISO 8601 zoned date time when this record was updated.
   */
  lastModified?: Date | undefined;
  /**
   * The identifier of the user that last updated this record.
   */
  lastModifiedBy?: string | undefined;
  /**
   * The reference to the related entity. Contains only the ID of the related entity.
   */
  event: UuidJson;
  /**
   * The reference to the related entity. Contains only the ID of the related entity.
   */
  session: UuidJson;
  /**
   * The reference to the related entity. Contains only the ID of the related entity.
   */
  attendee: UuidJson;
  /**
   * The earliest date and time when the attendee was seen, in ISO 8601 format.
   */
  firstSeen: Date;
  /**
   * The latest date and time when the attendee was seen, in ISO 8601 format.
   */
  lastSeen: Date;
  /**
   * A duration record is deleted if marked as recanted by the algorithm after determining the initial information has changed
   */
  deleted?: boolean | undefined;
  /**
   * Describes how the duration was obtained.
   */
  type?: ExistingDurationType | undefined;
  /**
   * The target entity for which the duration was collected.
   */
  durationType?: DurationType | undefined;
  /**
   * The amount of time (in milliseconds) than the attendee was engaged (in person or virtually) in a session or watching a video. Note: Duration is calculated based on the time the attendee spent in the session, not just on when they were first and last seen
   */
  duration: number;
  /**
   * internal id
   */
  id: string;
};

/** @internal */
export const ExistingDurationType$inboundSchema: z.ZodNativeEnum<
  typeof ExistingDurationType
> = z.nativeEnum(ExistingDurationType);

/** @internal */
export const DurationType$inboundSchema: z.ZodNativeEnum<typeof DurationType> =
  z.nativeEnum(DurationType);

/** @internal */
export const ExistingDuration$inboundSchema: z.ZodType<
  ExistingDuration,
  z.ZodTypeDef,
  unknown
> = z.object({
  created: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  createdBy: z.string().optional(),
  lastModified: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ).optional(),
  lastModifiedBy: z.string().optional(),
  event: UuidJson$inboundSchema,
  session: UuidJson$inboundSchema,
  attendee: UuidJson$inboundSchema,
  firstSeen: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  lastSeen: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  deleted: z.boolean().optional(),
  type: ExistingDurationType$inboundSchema.optional(),
  durationType: DurationType$inboundSchema.optional(),
  duration: z.number().int(),
  id: z.string(),
});

export function existingDurationFromJSON(
  jsonString: string,
): SafeParseResult<ExistingDuration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExistingDuration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExistingDuration' from JSON`,
  );
}
