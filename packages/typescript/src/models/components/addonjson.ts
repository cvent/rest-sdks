/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Add-on rate details.
 */
export type Rate = {
  /**
   * Base rate.
   */
  base: number;
  /**
   * ISO 4217 currency code.
   */
  currency: string;
};

/**
 * Common object that holds add-on information.
 */
export type AddOnJson = {
  /**
   * Add-on identifier.
   */
  id: number;
  /**
   * Add-on name.
   */
  name?: string | undefined;
  /**
   * Add-on rate details.
   */
  rate?: Rate | undefined;
};

/** @internal */
export const Rate$inboundSchema: z.ZodType<Rate, z.ZodTypeDef, unknown> = z
  .object({
    base: z.number(),
    currency: z.string(),
  });

export function rateFromJSON(
  jsonString: string,
): SafeParseResult<Rate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Rate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Rate' from JSON`,
  );
}

/** @internal */
export const AddOnJson$inboundSchema: z.ZodType<
  AddOnJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.number().int(),
  name: z.string().optional(),
  rate: z.lazy(() => Rate$inboundSchema).optional(),
});

export function addOnJsonFromJSON(
  jsonString: string,
): SafeParseResult<AddOnJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AddOnJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AddOnJson' from JSON`,
  );
}
