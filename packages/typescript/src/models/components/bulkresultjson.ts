/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BulkDataRecordJson,
  BulkDataRecordJson$inboundSchema,
} from "./bulkdatarecordjson.js";

/**
 * The response data for the bulk operation.
 */
export type BulkDataProperty = {
  /**
   * Key-value pairs where a key identifies a labeled placeholders in the url (e.g. "/endpoint/{entityId}") and a value represents the value to substitute. Necessary if the target endpoint has path parameters. Values must NOT be null.
   */
  pathParams?: { [k: string]: string } | undefined;
  /**
   * Query params specific to this data record to pass to the target endpoint. Entries merge with, and overlaps override, job-level queryParams. Values must NOT be null.
   */
  queryParams?: { [k: string]: string } | undefined;
  /**
   * Headers specific to this data record to pass to the target endpoint. Entries merge with, and overlaps override, job-level headers. Values must NOT be null.
   */
  headers?: { [k: string]: string } | undefined;
  /**
   * Arbitrary container to hold a JSON payload. The payload is the data you're sending, or the reply received when fetching results. For the request, this data must be structured in the way the API you're running a bulk job for accepts. For example, to bulk create sessions, you structure each of the `dataRecord` entries based on the format of the Create Session endpoint.
   */
  dataRecord: { [k: string]: BulkDataRecordJson };
};

/**
 * Unique identifier of the bulk job linked to this result.
 */
export type Job = {
  /**
   * A string that has to be a format matching the industry standard uuid
   */
  id?: string | undefined;
};

/**
 * This entity is used to represent a single item that is returned as part of a Bulk Job Result call.
 */
export type BulkResultJson = {
  /**
   * The response data for the bulk operation.
   */
  data: BulkDataProperty;
  /**
   * http status code representing processing status of a single item
   */
  status: number;
  /**
   * Quick description of what happened with processing
   */
  message?: string | undefined;
  /**
   * The processed request tied to this response. This field is only sent when processing fails.
   */
  request?: { [k: string]: any } | undefined;
  /**
   * The bulk result identifier.
   */
  id?: number | undefined;
  /**
   * Unique identifier of the bulk job linked to this result.
   */
  job?: Job | undefined;
  /**
   * Indicator that specifies if the bulk result failed.
   */
  failed?: boolean | undefined;
};

/** @internal */
export const BulkDataProperty$inboundSchema: z.ZodType<
  BulkDataProperty,
  z.ZodTypeDef,
  unknown
> = z.object({
  pathParams: z.record(z.string()).optional(),
  queryParams: z.record(z.string()).optional(),
  headers: z.record(z.string()).optional(),
  dataRecord: z.record(BulkDataRecordJson$inboundSchema),
});

export function bulkDataPropertyFromJSON(
  jsonString: string,
): SafeParseResult<BulkDataProperty, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BulkDataProperty$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BulkDataProperty' from JSON`,
  );
}

/** @internal */
export const Job$inboundSchema: z.ZodType<Job, z.ZodTypeDef, unknown> = z
  .object({
    id: z.string().optional(),
  });

export function jobFromJSON(
  jsonString: string,
): SafeParseResult<Job, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Job$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Job' from JSON`,
  );
}

/** @internal */
export const BulkResultJson$inboundSchema: z.ZodType<
  BulkResultJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  data: z.lazy(() => BulkDataProperty$inboundSchema),
  status: z.number().int(),
  message: z.string().optional(),
  request: z.record(z.any()).optional(),
  id: z.number().int().optional(),
  job: z.lazy(() => Job$inboundSchema).optional(),
  failed: z.boolean().optional(),
});

export function bulkResultJsonFromJSON(
  jsonString: string,
): SafeParseResult<BulkResultJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BulkResultJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BulkResultJson' from JSON`,
  );
}
