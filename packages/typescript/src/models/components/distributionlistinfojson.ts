/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DistributionListSecurityTypeJson,
  DistributionListSecurityTypeJson$inboundSchema,
} from "./distributionlistsecuritytypejson.js";
import {
  DistributionListStatusJson,
  DistributionListStatusJson$inboundSchema,
} from "./distributionliststatusjson.js";

/**
 * Model representing a distribution list info. Only required if the contact group type is DISTRIBUTION_LIST.
 */
export type DistributionListInfoJson = {
  /**
   * Type of distribution list security. For a public list, contacts can sign up themselves. For a semi-private list, contacts can add themselves, but a planner must approve their membership before they can receive emails. For a private list, only planners can add contacts. Contacts can always remove themselves from lists on the Manage Profile page.
   */
  securityType?: DistributionListSecurityTypeJson | undefined;
  /**
   * Internal note for Distribution Lists.
   */
  internalNote?: string | undefined;
  /**
   * Type of distribution list status.
   */
  status?: DistributionListStatusJson | undefined;
};

/** @internal */
export const DistributionListInfoJson$inboundSchema: z.ZodType<
  DistributionListInfoJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  securityType: DistributionListSecurityTypeJson$inboundSchema.optional(),
  internalNote: z.string().optional(),
  status: DistributionListStatusJson$inboundSchema.optional(),
});

export function distributionListInfoJsonFromJSON(
  jsonString: string,
): SafeParseResult<DistributionListInfoJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DistributionListInfoJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DistributionListInfoJson' from JSON`,
  );
}
