/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The type of target associated with the banner link.
 */
export const BannerLinkTargetType = {
  AttendeeHubPage: "Attendee Hub Page",
  DiscussionTopic: "Discussion Topic",
  ExternalUrl: "External Url",
  FloorPlanDetails: "Floor Plan Details",
  SessionDetails: "Session Details",
  SpeakerDetails: "Speaker Details",
  VirtualBooth: "Virtual Booth",
} as const;
/**
 * The type of target associated with the banner link.
 */
export type BannerLinkTargetType = ClosedEnum<typeof BannerLinkTargetType>;

/**
 * The target of the link clicked on the banner.
 */
export type BannerLinkTarget = {
  /**
   * The type of target associated with the banner link.
   */
  type?: BannerLinkTargetType | undefined;
  /**
   * The details of the target associated with the banner link.
   */
  details?: string | undefined;
};

/** @internal */
export const BannerLinkTargetType$inboundSchema: z.ZodNativeEnum<
  typeof BannerLinkTargetType
> = z.nativeEnum(BannerLinkTargetType);

/** @internal */
export const BannerLinkTarget$inboundSchema: z.ZodType<
  BannerLinkTarget,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: BannerLinkTargetType$inboundSchema.optional(),
  details: z.string().optional(),
});

export function bannerLinkTargetFromJSON(
  jsonString: string,
): SafeParseResult<BannerLinkTarget, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BannerLinkTarget$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BannerLinkTarget' from JSON`,
  );
}
