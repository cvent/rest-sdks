/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE costs are based on a quantity.
 */
export const BudgetCostType = {
  Fixed: "FIXED",
  Variable: "VARIABLE",
} as const;
/**
 * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE costs are based on a quantity.
 */
export type BudgetCostType = ClosedEnum<typeof BudgetCostType>;

/** @internal */
export const BudgetCostType$inboundSchema: z.ZodNativeEnum<
  typeof BudgetCostType
> = z.nativeEnum(BudgetCostType);
