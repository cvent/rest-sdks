/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Link, Link$inboundSchema } from "./link.js";
import {
  ThumbnailStatus,
  ThumbnailStatus$inboundSchema,
} from "./thumbnailstatus.js";
import { ThumbnailType, ThumbnailType$inboundSchema } from "./thumbnailtype.js";

/**
 * Video thumbnail
 */
export type VideoThumbnail = {
  /**
   * Represents a link to a related resource.
   */
  url?: Link | undefined;
  /**
   * File size in bytes for the thumbnail
   */
  size?: number | undefined;
  /**
   * frame height in pixels
   */
  height?: number | undefined;
  /**
   * frame width in pixels
   */
  width?: number | undefined;
  /**
   * This is used to denote the file type of a thumbnail
   */
  type?: ThumbnailType | undefined;
  /**
   * Denotes the status of a thumbnail. Started indicates the request to upload in the API was submitted, and the URL you'll upload a thumbnail to was returned. Uploaded indicates that the upload was completed. Processing indicates that a virus scan is in progress. Available indicates the thumbnail passed the scan and is ready for use. Rejected indicates there was a problem processing the thumbnail.
   */
  status?: ThumbnailStatus | undefined;
};

/** @internal */
export const VideoThumbnail$inboundSchema: z.ZodType<
  VideoThumbnail,
  z.ZodTypeDef,
  unknown
> = z.object({
  url: Link$inboundSchema.optional(),
  size: z.number().int().optional(),
  height: z.number().int().optional(),
  width: z.number().int().optional(),
  type: ThumbnailType$inboundSchema.optional(),
  status: ThumbnailStatus$inboundSchema.optional(),
});

export function videoThumbnailFromJSON(
  jsonString: string,
): SafeParseResult<VideoThumbnail, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VideoThumbnail$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VideoThumbnail' from JSON`,
  );
}
