/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ReservationNightRate,
  ReservationNightRate$inboundSchema,
} from "./reservationnightrate.js";

/**
 * Indicator of night's status.
 */
export const ReservationNightStatus = {
  Confirmed: "confirmed",
  Waitlisted: "waitlisted",
} as const;
/**
 * Indicator of night's status.
 */
export type ReservationNightStatus = ClosedEnum<typeof ReservationNightStatus>;

/**
 * Reservation night details.
 */
export type ReservationNight = {
  /**
   * Night date.
   */
  stayDate: RFCDate;
  /**
   * Indicator of night's status.
   */
  status: ReservationNightStatus;
  /**
   * True indicates the rates are hidden for this night.
   */
  hideRates?: boolean | undefined;
  /**
   * Rate details for a single reservation night.
   */
  rate: ReservationNightRate;
};

/** @internal */
export const ReservationNightStatus$inboundSchema: z.ZodNativeEnum<
  typeof ReservationNightStatus
> = z.nativeEnum(ReservationNightStatus);

/** @internal */
export const ReservationNight$inboundSchema: z.ZodType<
  ReservationNight,
  z.ZodTypeDef,
  unknown
> = z.object({
  stayDate: z.string().transform(v => new RFCDate(v)),
  status: ReservationNightStatus$inboundSchema,
  hideRates: z.boolean().optional(),
  rate: ReservationNightRate$inboundSchema,
});

export function reservationNightFromJSON(
  jsonString: string,
): SafeParseResult<ReservationNight, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReservationNight$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReservationNight' from JSON`,
  );
}
