/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A tax schedule used for location-based tax calculation.
 */
export type TaxSchedule = {
  /**
   * Tax schedule ID.
   */
  id: string;
  /**
   * Name of the tax schedule.
   */
  name: string;
  /**
   * Default tax rate for the schedule, expressed as a percentage.
   */
  defaultRate?: number | undefined;
};

/** @internal */
export const TaxSchedule$inboundSchema: z.ZodType<
  TaxSchedule,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  name: z.string(),
  defaultRate: z.number().optional(),
});

export function taxScheduleFromJSON(
  jsonString: string,
): SafeParseResult<TaxSchedule, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxSchedule$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxSchedule' from JSON`,
  );
}
