/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BudgetTaxGratuityType,
  BudgetTaxGratuityType$inboundSchema,
} from "./budgettaxgratuitytype.js";

/**
 * Budget item tax details.
 */
export type BudgetTax = {
  /**
   * A string that has to be a format matching the industry standard uuid
   */
  id: string;
  /**
   * Tax name.
   */
  name?: string | undefined;
  /**
   * Denotes the type of tax or gratuity.
   */
  taxType: BudgetTaxGratuityType;
  /**
   * Tax applied to the budget item. This value can be a percentage of the cost or a flat dollar amount.
   */
  tax: number;
  /**
   * Calculated tax amount based on tax field.
   */
  appliedTaxValue?: number | undefined;
};

/** @internal */
export const BudgetTax$inboundSchema: z.ZodType<
  BudgetTax,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  name: z.string().optional(),
  taxType: BudgetTaxGratuityType$inboundSchema,
  tax: z.number(),
  appliedTaxValue: z.number().optional(),
});

export function budgetTaxFromJSON(
  jsonString: string,
): SafeParseResult<BudgetTax, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BudgetTax$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BudgetTax' from JSON`,
  );
}
