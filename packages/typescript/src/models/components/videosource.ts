/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Denotes the status of the video in the third-party system.
 */
export const VideoSourceStatus = {
  Active: "Active",
  Inactive: "Inactive",
  Pending: "Pending",
  Deleted: "Deleted",
} as const;
/**
 * Denotes the status of the video in the third-party system.
 */
export type VideoSourceStatus = ClosedEnum<typeof VideoSourceStatus>;

/**
 * Source of video.
 */
export type VideoSource = {
  /**
   * The id of a video in the external system
   */
  id?: string | undefined;
  /**
   * Denotes the status of the video in the third-party system.
   */
  status?: VideoSourceStatus | undefined;
};

/** @internal */
export const VideoSourceStatus$inboundSchema: z.ZodNativeEnum<
  typeof VideoSourceStatus
> = z.nativeEnum(VideoSourceStatus);

/** @internal */
export const VideoSource$inboundSchema: z.ZodType<
  VideoSource,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  status: VideoSourceStatus$inboundSchema.optional(),
});

export function videoSourceFromJSON(
  jsonString: string,
): SafeParseResult<VideoSource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VideoSource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VideoSource' from JSON`,
  );
}
