/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ExistingMemberJson,
  ExistingMemberJson$inboundSchema,
} from "./existingmemberjson.js";
import { PagingJson, PagingJson$inboundSchema } from "./pagingjson.js";

/**
 * A paginated list of Events+ members.
 */
export type PaginatedExistingMember = {
  /**
   * Represents pagination information for a collection of resources.
   */
  paging: PagingJson;
  /**
   * Collection of Events+ members.
   */
  data: Array<ExistingMemberJson>;
};

/** @internal */
export const PaginatedExistingMember$inboundSchema: z.ZodType<
  PaginatedExistingMember,
  z.ZodTypeDef,
  unknown
> = z.object({
  paging: PagingJson$inboundSchema,
  data: z.array(ExistingMemberJson$inboundSchema),
});

export function paginatedExistingMemberFromJSON(
  jsonString: string,
): SafeParseResult<PaginatedExistingMember, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaginatedExistingMember$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaginatedExistingMember' from JSON`,
  );
}
