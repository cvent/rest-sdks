/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  HousingEventStatus,
  HousingEventStatus$inboundSchema,
} from "./housingeventstatus.js";
import { Venue, Venue$inboundSchema } from "./venue.js";

/**
 * Information about housing event with key information, providing a summarized view.
 */
export type HousingEventSummary = {
  /**
   * The unique ID of the housing event.
   */
  id: number;
  /**
   * Event name.
   */
  name: string;
  /**
   * The ISO 8601 formatted date and time of the first attended day of the event, excluding shoulder days.
   */
  start: Date;
  /**
   * The ISO 8601 date and time of the last attended day of the event, excluding shoulder days.
   */
  end: Date;
  /**
   * The ISO 8601 formatted date and time of a contractually agreed date which triggers configurable business rules, like releasing reserved room blocks back to general availability.
   */
  cutOff: Date;
  /**
   * The event timezone from the Olson specification.
   */
  timeZone: string;
  /**
   * Event venue details.
   */
  venue: Venue;
  /**
   * Housing event status.
   */
  status: HousingEventStatus;
};

/** @internal */
export const HousingEventSummary$inboundSchema: z.ZodType<
  HousingEventSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.number().int(),
  name: z.string(),
  start: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  end: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  cutOff: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  timeZone: z.string(),
  venue: Venue$inboundSchema,
  status: HousingEventStatus$inboundSchema,
});

export function housingEventSummaryFromJSON(
  jsonString: string,
): SafeParseResult<HousingEventSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HousingEventSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HousingEventSummary' from JSON`,
  );
}
