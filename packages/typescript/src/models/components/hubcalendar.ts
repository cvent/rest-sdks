/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The initial layout style used by front-ends to render the associated calendar.
 */
export const Layout = {
  List: "List",
  Grid: "Grid",
} as const;
/**
 * The initial layout style used by front-ends to render the associated calendar.
 */
export type Layout = ClosedEnum<typeof Layout>;

/**
 * The event calendar associated with this Events+ hub. The event calendar allows you to share multiple events and sessions in a list or calendar format.
 */
export type HubCalendar = {
  /**
   * The ID of the calendar associated to an Events+ hub.
   */
  id?: string | undefined;
  /**
   * The initial layout style used by front-ends to render the associated calendar.
   */
  layout?: Layout | undefined;
};

/** @internal */
export const Layout$inboundSchema: z.ZodNativeEnum<typeof Layout> = z
  .nativeEnum(Layout);

/** @internal */
export const HubCalendar$inboundSchema: z.ZodType<
  HubCalendar,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  layout: Layout$inboundSchema.optional(),
});

export function hubCalendarFromJSON(
  jsonString: string,
): SafeParseResult<HubCalendar, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HubCalendar$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HubCalendar' from JSON`,
  );
}
