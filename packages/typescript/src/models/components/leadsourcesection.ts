/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Represents lead source section details, which is a sub area of source generating the RFP.
 */
export type LeadSourceSection = {
  /**
   * Unique identifier for sub area of source generating the RFP. Details about this lead source section can be fetched using <a href="#tag/RFP-Management/operation/getRfpLeadSourceSection">lead source section API.</a>
   */
  id?: string | undefined;
};

/** @internal */
export const LeadSourceSection$inboundSchema: z.ZodType<
  LeadSourceSection,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
});

export function leadSourceSectionFromJSON(
  jsonString: string,
): SafeParseResult<LeadSourceSection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LeadSourceSection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LeadSourceSection' from JSON`,
  );
}
