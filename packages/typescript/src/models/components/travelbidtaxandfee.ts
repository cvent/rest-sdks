/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BidItemStatusType,
  BidItemStatusType$inboundSchema,
} from "./biditemstatustype.js";
import { TaxType1, TaxType1$inboundSchema } from "./taxtype1.js";

/**
 * A tax or fee associated with a travel bid.
 */
export type TravelBidTaxAndFee = {
  /**
   * Bid tax type.
   */
  code?: TaxType1 | undefined;
  /**
   * The amount of the tax/fee. This may be a percent or monetary value depending on the value in `percent`.
   */
  amount?: number | undefined;
  /**
   * True indicates the tax/fee amount represents a percentage, instead of a monetary value.
   */
  percent: boolean;
  /**
   * True indicates the tax/fee included in the negotiated rate.
   */
  included: boolean;
  /**
   * Additional notes on the tax/fee.
   */
  notes?: string | undefined;
  /**
   * The status of the item in negotiation. e.g. The corporation might mark an item as delete_requested, and the hotel, if they agree, can update the item to deleted. not_offered indicates the hotel does not offer this amenity (supply side only).
   */
  status?: BidItemStatusType | undefined;
};

/** @internal */
export const TravelBidTaxAndFee$inboundSchema: z.ZodType<
  TravelBidTaxAndFee,
  z.ZodTypeDef,
  unknown
> = z.object({
  code: TaxType1$inboundSchema.optional(),
  amount: z.number().optional(),
  percent: z.boolean().default(false),
  included: z.boolean().default(false),
  notes: z.string().optional(),
  status: BidItemStatusType$inboundSchema.optional(),
});

export function travelBidTaxAndFeeFromJSON(
  jsonString: string,
): SafeParseResult<TravelBidTaxAndFee, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TravelBidTaxAndFee$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TravelBidTaxAndFee' from JSON`,
  );
}
