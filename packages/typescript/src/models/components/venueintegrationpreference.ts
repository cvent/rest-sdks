/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  VenueIntegrationType,
  VenueIntegrationType$inboundSchema,
} from "./venueintegrationtype.js";

/**
 * Venue integration preference for an RFP.
 */
export type VenueIntegrationPreference = {
  /**
   * The type of venue integration a planner prefers when sourcing venues through an RFP. PASSKEY represents Passkey room-block management. CED represents Cvent Event Diagramming (3D diagramming / Interactive Floor Plans).
   */
  type: VenueIntegrationType;
  /**
   * True indicates the planner opted to share their preference for venues equipped with the specified integration with suppliers.
   */
  optIn: boolean;
};

/** @internal */
export const VenueIntegrationPreference$inboundSchema: z.ZodType<
  VenueIntegrationPreference,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: VenueIntegrationType$inboundSchema,
  optIn: z.boolean(),
});

export function venueIntegrationPreferenceFromJSON(
  jsonString: string,
): SafeParseResult<VenueIntegrationPreference, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VenueIntegrationPreference$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VenueIntegrationPreference' from JSON`,
  );
}
