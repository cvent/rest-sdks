/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Represents the capacity details for a voucher, including the total allowed redemptions and the number already redeemed.
 */
export type VoucherCapacity = {
  /**
   * The maximum number of times the voucher can be redeemed. Default value of -1 indicates unlimited capacity.
   */
  total: number;
  /**
   * The number of times the voucher has already been redeemed.
   */
  redeemedCount: number;
};

/** @internal */
export const VoucherCapacity$inboundSchema: z.ZodType<
  VoucherCapacity,
  z.ZodTypeDef,
  unknown
> = z.object({
  total: z.number().int(),
  redeemedCount: z.number().int(),
});

export function voucherCapacityFromJSON(
  jsonString: string,
): SafeParseResult<VoucherCapacity, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VoucherCapacity$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VoucherCapacity' from JSON`,
  );
}
