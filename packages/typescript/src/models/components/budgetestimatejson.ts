/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BudgetCostDetailJson1,
  BudgetCostDetailJson1$inboundSchema,
} from "./budgetcostdetailjson1.js";
import {
  BudgetCostTypeJson,
  BudgetCostTypeJson$inboundSchema,
} from "./budgetcosttypejson.js";

/**
 * The estimated budget of the Meeting Request.
 */
export type BudgetEstimateJson = {
  /**
   * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is not allowed for the account during the creation or update of the Meeting Request.
   */
  currency?: string | undefined;
  /**
   * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE costs are based on a quantity.
   */
  costType?: BudgetCostTypeJson | undefined;
  /**
   * A list of budget entry details. Each entry consists of the budget category or sub-category, the number of units, the cost per unit, and the total amount.
   */
  costDetail?: Array<BudgetCostDetailJson1> | undefined;
};

/** @internal */
export const BudgetEstimateJson$inboundSchema: z.ZodType<
  BudgetEstimateJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  currency: z.string().optional(),
  costType: BudgetCostTypeJson$inboundSchema.optional(),
  costDetail: z.array(BudgetCostDetailJson1$inboundSchema).optional(),
});

export function budgetEstimateJsonFromJSON(
  jsonString: string,
): SafeParseResult<BudgetEstimateJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BudgetEstimateJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BudgetEstimateJson' from JSON`,
  );
}
