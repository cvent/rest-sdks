/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ScheduleActionJson,
  ScheduleActionJson$inboundSchema,
} from "./scheduleactionjson.js";

/**
 * A scheduled action.
 */
export type ScheduleJson = {
  /**
   * Unique ID of the scheduled task.
   */
  id?: string | undefined;
  /**
   * The ISO 8601 zoned date time when the session will be moved to the next stage: provisioning, activation, deactivation or archival
   */
  scheduledExecution?: Date | undefined;
  /**
   * This is used to denote the action to perform with the scheduled task
   */
  action?: ScheduleActionJson | undefined;
};

/** @internal */
export const ScheduleJson$inboundSchema: z.ZodType<
  ScheduleJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  scheduledExecution: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ).optional(),
  action: ScheduleActionJson$inboundSchema.optional(),
});

export function scheduleJsonFromJSON(
  jsonString: string,
): SafeParseResult<ScheduleJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduleJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduleJson' from JSON`,
  );
}
