/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  RecipientsDataJson,
  RecipientsDataJson$inboundSchema,
} from "./recipientsdatajson.js";

/**
 * The response from a request to get the list of recipients with email status.
 */
export type EmailSendStatus = {
  /**
   * ID of the send email request.
   */
  emailRequestId: string;
  /**
   * The ISO 8601 zoned date time (in UTC) when this request was made.
   */
  requestedDate: Date;
  /**
   * The identifier of the user that requested this record.
   */
  requestedBy: string;
  /**
   * A list of recipients to whom emails were tried to send.
   */
  recipientStatus: Array<RecipientsDataJson>;
};

/** @internal */
export const EmailSendStatus$inboundSchema: z.ZodType<
  EmailSendStatus,
  z.ZodTypeDef,
  unknown
> = z.object({
  emailRequestId: z.string(),
  requestedDate: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ),
  requestedBy: z.string(),
  recipientStatus: z.array(RecipientsDataJson$inboundSchema),
});

export function emailSendStatusFromJSON(
  jsonString: string,
): SafeParseResult<EmailSendStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmailSendStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmailSendStatus' from JSON`,
  );
}
