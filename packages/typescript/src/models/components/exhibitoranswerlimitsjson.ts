/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Defines the limits for answers to exhibitor questions.
 */
export type ExhibitorAnswerLimitsJson = {
  /**
   * Minimum length of the answer for OpenEndedTextCommentBox questions. It must be less than or equal to maxLength.
   */
  minLength?: number | undefined;
  /**
   * Maximum length of the answer for OpenEndedTextCommentBox questions.
   */
  maxLength?: number | undefined;
  /**
   * Minimum number of choices required for MultiChoice questions. It must be less than or equal to maxSelections.
   */
  minSelections?: number | undefined;
  /**
   * Maximum number of choices allowed for MultiChoice questions. It cannot exceed the total number of available choices.
   */
  maxSelections?: number | undefined;
};

/** @internal */
export const ExhibitorAnswerLimitsJson$inboundSchema: z.ZodType<
  ExhibitorAnswerLimitsJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  minLength: z.number().int().default(1),
  maxLength: z.number().int().default(2000),
  minSelections: z.number().int().optional(),
  maxSelections: z.number().int().optional(),
});

export function exhibitorAnswerLimitsJsonFromJSON(
  jsonString: string,
): SafeParseResult<ExhibitorAnswerLimitsJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExhibitorAnswerLimitsJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExhibitorAnswerLimitsJson' from JSON`,
  );
}
