/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Representation of a leg detail in air request.
 */
export type LegDetailJson = {
  /**
   * Name of the airport where the flight of the passenger's journey originates.
   */
  origin?: string | undefined;
  /**
   * Name of the airport where the flight of the passenger's journey ends.
   */
  destination?: string | undefined;
  /**
   * The ISO 8601 zoned date time (in UTC) of the flight origin.
   */
  date?: Date | undefined;
  /**
   * General time of day or specific time when the flight of the passenger's journey starts.
   */
  time?: string | undefined;
};

/** @internal */
export const LegDetailJson$inboundSchema: z.ZodType<
  LegDetailJson,
  z.ZodTypeDef,
  unknown
> = z.object({
  origin: z.string().optional(),
  destination: z.string().optional(),
  date: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  time: z.string().optional(),
});

export function legDetailJsonFromJSON(
  jsonString: string,
): SafeParseResult<LegDetailJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LegDetailJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LegDetailJson' from JSON`,
  );
}
