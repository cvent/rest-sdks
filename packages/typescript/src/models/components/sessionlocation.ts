/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Used to denote a sessions location.
 */
export type SessionLocation = {
  /**
   * A string that represents a specified location.
   */
  id?: string | undefined;
  /**
   * Code / Abbreviation of the location.
   */
  code: string;
  /**
   * Name of the location.
   */
  name: string;
};

/** @internal */
export const SessionLocation$inboundSchema: z.ZodType<
  SessionLocation,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string().optional(),
  code: z.string(),
  name: z.string(),
});

export function sessionLocationFromJSON(
  jsonString: string,
): SafeParseResult<SessionLocation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionLocation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionLocation' from JSON`,
  );
}
