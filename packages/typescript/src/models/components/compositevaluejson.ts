/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  BudgetEstimateJson,
  BudgetEstimateJson$inboundSchema,
} from "./budgetestimatejson.js";
import {
  MeetingRoomRequirementJson,
  MeetingRoomRequirementJson$inboundSchema,
} from "./meetingroomrequirementjson.js";
import {
  SleepingRoomRequirementJson,
  SleepingRoomRequirementJson$inboundSchema,
} from "./sleepingroomrequirementjson.js";

/**
 * The structured representation of a complex question answer. Contains the same data as `value`, parsed into typed objects instead of JSON-serialized strings. This field is **read-only** — to write complex question answers, use `value` instead.
 *
 * @remarks
 *
 * The structure of the array depends on the question `type`.
 */
export type CompositeValueJson =
  | Array<MeetingRoomRequirementJson>
  | Array<SleepingRoomRequirementJson>
  | Array<BudgetEstimateJson>;

/** @internal */
export const CompositeValueJson$inboundSchema: z.ZodType<
  CompositeValueJson,
  z.ZodTypeDef,
  unknown
> = z.union([
  z.array(MeetingRoomRequirementJson$inboundSchema),
  z.array(SleepingRoomRequirementJson$inboundSchema),
  z.array(BudgetEstimateJson$inboundSchema),
]);

export function compositeValueJsonFromJSON(
  jsonString: string,
): SafeParseResult<CompositeValueJson, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompositeValueJson$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompositeValueJson' from JSON`,
  );
}
