/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { RFCDate } from "../../types/rfcdate.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AdditionalGuestFees,
  AdditionalGuestFees$inboundSchema,
} from "./additionalguestfees.js";
import { AttendeeType, AttendeeType$inboundSchema } from "./attendeetype.js";
import { HotelId, HotelId$inboundSchema } from "./hotelid.js";
import { RoomTypeId, RoomTypeId$inboundSchema } from "./roomtypeid.js";
import { TaxStructure, TaxStructure$inboundSchema } from "./taxstructure.js";

/**
 * Night availability details.
 */
export type AvailableNight = {
  /**
   * Contains unique ID of the hotel.
   */
  hotel: HotelId;
  /**
   * Contains unique ID of the room type.
   */
  roomType: RoomTypeId;
  /**
   * Attendee type details.
   */
  attendeeType: AttendeeType;
  /**
   * Date the rooms are available.
   */
  date: RFCDate;
  /**
   * Number of rooms available.
   */
  quantity: number;
  /**
   * Cost of room per night booked.
   */
  roomRate: number;
  /**
   * The room rate displayed to guests, which includes any fees configured to display in the hotel tax settings.
   */
  displayRate?: number | undefined;
  /**
   * List of additional guest fees.
   */
  additionalGuestFees?: Array<AdditionalGuestFees> | undefined;
  /**
   * Details about taxes and fees applicable to a room.
   */
  taxStructure?: TaxStructure | undefined;
  /**
   * ISO 4217 currency code.
   */
  currency: string;
  /**
   * Maximum number of guests the room can accommodate.
   */
  maxGuests?: number | undefined;
  /**
   * Minimum number of nights required for a stay.
   */
  minLengthOfStay: number;
  /**
   * True indicates the cost of the room is hidden from attendees on the booking site.
   */
  hideRate: boolean;
  /**
   * True indicates a waitlist is enabled for this night.
   */
  waitListEnabled: boolean;
};

/** @internal */
export const AvailableNight$inboundSchema: z.ZodType<
  AvailableNight,
  z.ZodTypeDef,
  unknown
> = z.object({
  hotel: HotelId$inboundSchema,
  roomType: RoomTypeId$inboundSchema,
  attendeeType: AttendeeType$inboundSchema,
  date: z.string().transform(v => new RFCDate(v)),
  quantity: z.number().int(),
  roomRate: z.number(),
  displayRate: z.number().optional(),
  additionalGuestFees: z.array(AdditionalGuestFees$inboundSchema).optional(),
  taxStructure: TaxStructure$inboundSchema.optional(),
  currency: z.string(),
  maxGuests: z.number().int().optional(),
  minLengthOfStay: z.number().int(),
  hideRate: z.boolean(),
  waitListEnabled: z.boolean(),
});

export function availableNightFromJSON(
  jsonString: string,
): SafeParseResult<AvailableNight, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AvailableNight$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AvailableNight' from JSON`,
  );
}
