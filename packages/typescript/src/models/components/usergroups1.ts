/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Group1, Group1$inboundSchema } from "./group1.js";

/**
 * Groups
 */
export type UserGroups1 = {
  /**
   * The collection of user schemas.
   */
  schemas?: Array<string> | undefined;
  /**
   * The number of schemas per page.
   */
  itemsPerPage?: number | undefined;
  /**
   * Starting index of the response.
   */
  startIndex?: number | undefined;
  /**
   * The total count of schemas.
   */
  totalResults?: number | undefined;
  /**
   * The collection of schema resources.
   */
  resources?: Array<Group1> | undefined;
};

/** @internal */
export const UserGroups1$inboundSchema: z.ZodType<
  UserGroups1,
  z.ZodTypeDef,
  unknown
> = z.object({
  schemas: z.array(z.string()).optional(),
  itemsPerPage: z.number().int().optional(),
  startIndex: z.number().int().optional(),
  totalResults: z.number().int().optional(),
  Resources: z.array(Group1$inboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    "Resources": "resources",
  });
});

export function userGroups1FromJSON(
  jsonString: string,
): SafeParseResult<UserGroups1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UserGroups1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UserGroups1' from JSON`,
  );
}
