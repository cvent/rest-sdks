/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  BudgetTaxGratuityType,
  BudgetTaxGratuityType$outboundSchema,
} from "./budgettaxgratuitytype.js";

/**
 * Budget item tax details.
 */
export type BudgetTaxInput = {
  /**
   * A string that has to be a format matching the industry standard uuid
   */
  id: string;
  /**
   * Denotes the type of tax or gratuity.
   */
  taxType: BudgetTaxGratuityType;
  /**
   * Tax applied to the budget item. This value can be a percentage of the cost or a flat dollar amount.
   */
  tax: number;
};

/** @internal */
export type BudgetTaxInput$Outbound = {
  id: string;
  taxType: string;
  tax: number;
};

/** @internal */
export const BudgetTaxInput$outboundSchema: z.ZodType<
  BudgetTaxInput$Outbound,
  z.ZodTypeDef,
  BudgetTaxInput
> = z.object({
  id: z.string(),
  taxType: BudgetTaxGratuityType$outboundSchema,
  tax: z.number(),
});

export function budgetTaxInputToJSON(budgetTaxInput: BudgetTaxInput): string {
  return JSON.stringify(BudgetTaxInput$outboundSchema.parse(budgetTaxInput));
}
