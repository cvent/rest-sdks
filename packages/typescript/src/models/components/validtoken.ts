/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Validity status of an authentication token.
 */
export type ValidToken = {
  /**
   * True indicates the presented authentication token is valid.
   */
  tokenValid?: boolean | undefined;
  /**
   * Supplementary status code. Typically will be 200 for a success case.
   */
  statusCode?: number | undefined;
};

/** @internal */
export const ValidToken$inboundSchema: z.ZodType<
  ValidToken,
  z.ZodTypeDef,
  unknown
> = z.object({
  tokenValid: z.boolean().optional(),
  statusCode: z.number().int().optional(),
});

export function validTokenFromJSON(
  jsonString: string,
): SafeParseResult<ValidToken, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ValidToken$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ValidToken' from JSON`,
  );
}
