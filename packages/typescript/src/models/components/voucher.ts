/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  VoucherCapacity,
  VoucherCapacity$inboundSchema,
} from "./vouchercapacity.js";

/**
 * Represents a voucher configured for an event. Event-level vouchers are special codes that can be assigned to invitees or attendees, enabling benefits such as free registration, discounted pricing, or other promotional entitlements during the registration process.
 */
export type Voucher = {
  /**
   * Voucher ID.
   */
  id: string;
  /**
   * The unique planner-defined code that invitees enter during registration to redeem the voucher.
   */
  code: string;
  /**
   * A description of the voucher and its intended use.
   */
  description?: string | undefined;
  /**
   * The email address that receives a notification whenever the voucher is redeemed.
   */
  alertEmailAddress?: string | undefined;
  /**
   * Represents the capacity details for a voucher, including the total allowed redemptions and the number already redeemed.
   */
  capacity?: VoucherCapacity | undefined;
  /**
   * The identity of the user or system that last modified the voucher.
   */
  lastModifiedBy?: string | undefined;
  /**
   * The ISO 8601 formatted date and time when the voucher was last modified, expressed in UTC.
   */
  lastModifiedDate?: Date | undefined;
};

/** @internal */
export const Voucher$inboundSchema: z.ZodType<Voucher, z.ZodTypeDef, unknown> =
  z.object({
    id: z.string(),
    code: z.string(),
    description: z.string().optional(),
    alertEmailAddress: z.string().optional(),
    capacity: VoucherCapacity$inboundSchema.optional(),
    lastModifiedBy: z.string().optional(),
    lastModifiedDate: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ).optional(),
  });

export function voucherFromJSON(
  jsonString: string,
): SafeParseResult<Voucher, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Voucher$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Voucher' from JSON`,
  );
}
