# ChainBrand

The brand that the chain belongs to.

## Example Usage

```typescript
import { ChainBrand } from "@cvent/sdk/models/components";

let value: ChainBrand = {
  id: "NorthernHotels",
};
```

## Fields

| Field                                          | Type                                           | Required                                       | Description                                    | Example                                        |
| ---------------------------------------------- | ---------------------------------------------- | ---------------------------------------------- | ---------------------------------------------- | ---------------------------------------------- |
| `id`                                           | *string*                                       | :heavy_minus_sign:                             | The ID of the brand that the chain belongs to. | NorthernHotels                                 |