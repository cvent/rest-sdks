# ReturnedType

This is used to denote the returned type of the attribute.

## Example Usage

```typescript
import { ReturnedType } from "@cvent/sdk/models/components";

let value: ReturnedType = "always";
```

## Values

```typescript
"always" | "never" | "default" | "request"
```