/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.cvent;

import com.cvent.utils.HTTPClient;
import com.cvent.utils.Headers;
import com.cvent.utils.RetryConfig;
import com.cvent.utils.SpeakeasyHTTPClient;
import com.cvent.utils.Utils;
import java.lang.String;
import java.util.Map;
import java.util.Optional;
import java.util.function.Consumer;

/**
 * Cvent REST APIs: # Introduction
 * The Cvent API Platform is built around REST. We aim to provide intuitive endpoints that can be
 * easily
 * discovered to help leverage the Cvent platform for your event needs. The RESTful APIs outlined here
 * use JSON-encoded request and response format, along with HTTP codes, to convey processing status of
 * requests received. The Cvent resources are protected using OAuth2.
 * 
 * <p># Getting Started
 * 
 * <p>If you're new to the Cvent API Platform, start by reading our
 * [Developer Quickstart](/docs/rest-api/tutorials/developer-quickstart) guide. This will
 * give you an overview of how to authenticate and make requests using our APIs.
 * 
 * <p>## Authentication
 * 
 * <p>The Cvent REST API uses [OAuth2](https://oauth.net/2/) to authorize requests to the platform. The
 * client
 * credentials authorization flow is supported.
 * 
 * <p>&lt;a name="oauth2-auth-code-planner-admin"&gt;&lt;/a&gt;
 * 
 * <p>Authorization code flow is only supported for planner users with the administrator role in Cvent.
 * Developer users
 * cannot use authorization code flow.
 * 
 * <p>&lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 * 
 * <p>Here's an example of using client credential flow to authorize. You'll supply your application's id
 * and secret to
 * make a [Token](#operation/oauth2Token) request.
 * 
 * <p>```bash
 * curl --location --request POST '{hostName}/{version}/oauth2/token' \
 * --header 'Content-Type: application/x-www-form-urlencoded' \
 * --header 'Authorization: Basic {api_credentials}' \
 * --data-urlencode 'grant_type=client_credentials' \
 * --data-urlencode 'client_id={client_id}'
 * ```
 * 
 * <p>| Key               | Description                                          | Value
 * |
 * | :---------------- | :--------------------------------------------------- |
 * :-----------------------------------------------------------------------------------------------------------
 * |
 * | {hostName}        | https://api-platform.cvent.com                       | Location if you're
 * account is in the North American datacenter                                               |
 * |                   | https://api-platform-eur.cvent.com                   | Location if you're
 * account is in the European data center. |
 * | {version}         | ea                                                   | The version of
 * the API you're using. Only `ea` is currently supported.
 * 
 * <p>|
 * | {api_credentials} | {client_id}:{client_secret} in base64 encoded format | Supply your client id
 * &amp; client credentials in a base 64 encoded format. |
 * | {client_id}       | Retrieved from your application                      | Your application's
 * client id. |
 * | {client_secret}   | Retrieved from your application                      | Your application's
 * client secret. |
 * 
 * <p>On a successful call, you'll receive the following response:
 * 
 * <p>```json
 * {
 * "access_token": "{accessToken}",
 * "expires_in": 3600,
 * "token_type": "Bearer"
 * }
 * ```
 * 
 * <p>This bearer token is valid for 3600 seconds (60 minutes) and must be used in subsequent calls.
 * 
 * <p>## Endpoints
 * 
 * <p>Endpoints start with `hostName` and `version`.
 * 
 * <p>The `hostname` will depend on the region that your Cvent account is hosted in. Please see the table
 * below to identify which hostname you should be using.
 * 
 * <p>| Region        |	Hostname                           |
 * |:--------------|:-----------------------------------|
 * | North America	| https://api-platform.cvent.com     |
 * | Europe        | https://api-platform-eur.cvent.com |
 * 
 * <p>The current `version` of the Cvent API is `ea`.
 * 
 * <p>## Rate Limits
 * 
 * <p>Cvent APIs enforce rate limits to ensure platform stability. Your limits depend on your tier: Free,
 * Standard, or Premium.
 * 
 * <p><br />
 * 
 * <p>### Usage Tiers
 * 
 * <p>| Tier     | Daily Calls | Calls per Second | Max Burst |
 * | -------- | ----------- | ---------------- | --------- |
 * | Free     | 1,000       | 2                | 1         |
 * | Standard | 15,000      | 10               | 10        |
 * | Premium  | 500,000     | 25               | 25        |
 * 
 * <p>- **Daily calls** define how many requests you can make in a 24-hour period. Quota
 * resets at 12 midnight (+0 GMT).
 * - **Calls per second** define how many requests you can make in a 1-second window.
 * - **Max Burst** defines how many requests you can make at once.
 * 
 * <p>If you are unsure what usage tier applies to your account, you can check via
 * [Get Current Usage Tier](#operation/getUsageTier).
 * 
 * <p>Please note that these limits may change as the Cvent API Platform evolves.
 * 
 * <p><br />
 * 
 * <p>### Handling Rate Limits
 * 
 * <p>Sometimes, you may exceed your rate limits. When this happens, the API will return a `429 Too Many
 * Requests`.
 * 
 * <p>See
 * [handling rate limits](/docs/rest-api/guides/handling-rate-limits) for best practices on how to
 * handle this.
 * 
 * <p>## Pagination
 * 
 * <p>Some APIs use pagination to manage records. Each page of records has a token associated to identify
 * it.
 * 
 * <p>If an API uses pagination, you’ll find up to three tokens in the response:
 * - **currentToken**: Describes the token of the current page.
 * - **nextToken**: Provides a token for the next page of records, if one exists.
 * - **previousToken**: Provides a token for the previous page of records, if one exists. Not all APIs
 * will return
 * this token.
 * 
 * <p>You specify which page of records to view via the `token` parameter in your API call. To navigate
 * through pages,
 * take the `nextToken` or `previousToken` value and pass it to your next call’s `token` parameter to
 * get the
 * respective page of records. For example, if you made this request:
 * 
 * <p>```bash
 * curl -X GET {hostname}/{version}/contacts?limit=100 \
 * -H 'Accept: application/json' \
 * -H 'Authorization: Bearer {accessToken}'
 * ```
 * 
 * <p>The response contains a paging array where you'll find the token information.
 * 
 * <p>```json
 * {
 * "paging": {
 * "currentToken": "90c5f062-76ad-4ea4-aa53-00eb698d9262",
 * "nextToken": "3b2359a7-4583-40ed-8afd-67e5f15373d3",
 * "limit": 100,
 * "totalCount": 102,
 * "_links": {...}
 * },
 * "data": [...]
 * }
 * ```
 * 
 * <p>Take the `nextToken` and use it in the `token` parameter on your subsequent call.
 * 
 * <p>```bash
 * curl -X GET {hostname}/{version}/contacts?limit=100&amp;token=3b2359a7-4583-40ed-8afd-67e5f15373d3 \
 * -H 'Accept: application/json' \
 * -H 'Authorization: Bearer {accessToken}'
 * ```
 * 
 * <p>When the response doesn’t contain a `nextToken` field, you’ve reached the last page. Occasionally,
 * you might
 * encounter an empty page at the end of results. This typically happens when the results were evenly
 * divisible.
 * Ensure your client code handles the possibility of receiving an empty data array when using the
 * `nextToken`.
 * 
 * <p>## Filtering
 * 
 * <p>Use filters to narrow down results. The filter follows the pattern
 * `filter='field' comparisonType 'value'`. The value can be enclosed with single
 * quotes (') or double quotes (").
 * 
 * <p>```bash
 * GET {hostName}/{version}/contacts?filter=lastName eq 'Smith'
 * ````
 * 
 * <p>To correctly pass a single quote in the filter's value, use double quotes around
 * the string.
 * 
 * <p>```bash
 * GET {hostName}/{version}/contacts?filter=lastName eq "O'Keenan"
 * ```
 * 
 * <p>To correctly pass a double quote in the filter's value, use double quotes around
 * the string and add an escape character `\` to each quote that is part of the
 * string.
 * 
 * <p>```bash
 * GET {hostName}/{version}/events?filter=eventName eq "\"Yearly\" Conference"
 * ```
 * 
 * <p>## Versioning
 * 
 * <p>Change is inevitable in API development. Planning for it is crucial.
 * 
 * <p>We track
 * both backward-compatible and backward-incompatible changes.
 * 
 * <p><br />
 * 
 * <p>### Backward Compatible Changes
 * 
 * <p>Backward compatible changes will be made often and are intended to avoid
 * any adverse impact on our customers. It is highly advisable that when reading
 * JSON payloads from Cvent, you are able to handle "unknown" attributes that
 * can be added over time. We consider the following changes backward-compatible:
 * 
 * <p>- Adding new resources
 * - Adding new optional request parameters to existing operations
 * - Adding new attributes to requests or responses
 * - Changing the length or format (not type) of resource identifiers. For example, an ID can change
 * from
 * "1234/1234" to "1234::1234".
 * - Increasing the length of string fields
 * 
 * <p><br />
 * 
 * <p>### Backward Incompatible Changes
 * 
 * <p>Backward-incompatible changes are made infrequently, however, they can be
 * disruptive to consumers. Due to this, our APIs are versioned to avoid
 * disruptions to customers. We leverage a URI-based versioning scheme,
 * which means that a version value is included in the Cvent API URL.
 * When breaking changes occur, a new version of the API is made available
 * while the existing version is deprecated but remains available for a
 * limited period of time. We consider the following backward-incompatible changes:
 * 
 * <p>- Adding a new required parameter (query string param or payload attribute)
 * - Deleting API resources
 * - Deleting any attribute from API responses
 * - Changing the data type on any parameter or attribute
 * 
 * <p>## Standards
 * As you begin working with our APIs, it's essential to be aware of standards around
 * country codes, time formats, and other important details that ensure smooth integration.
 * Learn more about our [API Standards](/docs/rest-api/reference/api-standards)
 * 
 * <p><a href="https://developers.cvent.com/docs">Cvent Developer Documentation</a>
 */
public class CventSDK {
    private static final Headers _headers = Headers.EMPTY;


    /**
     * SERVERS contains the list of server urls available to the SDK.
     */
    public static final String[] SERVERS = {

        "https://api-platform-eur.cvent.com/ea",

        "https://api-platform.cvent.com/ea",
    };

    /**
     * Operations for managing account users and user groups, including creation, retrieval, update, and
     * deletion. Use these endpoints to administer user access and roles within your account.
     */
    private final Users users;

    /**
     * Events are used to collect registrations and allow attendees to select their schedule. Use these
     * APIs to get information about your events and use the event ID to get further details about the
     * event, such as Attendees and Sessions.
     */
    private final Events events;

    /**
     * An appointment is a meeting scheduled between two or more parties. These APIs allow you to get
     * information about your Cvent Appointments: appointment attendees, their interests, and
     * availabilities.
     * 
     * <p>* **Appointment Events** - Appointment events help planners facilitate meetings between attendees.
     * Attendees express their interest and availability to schedule appointments. Use this API to get
     * information about appointments and appointment attendees.
     * 
     * <p>* **Appointment Attendees** - Appointment attendees are meeting participants. They can be attendees
     * of either events or appointment events. Get information about these participants or use their
     * appointment attendee ID to get their availabilities and interests.
     * 
     * <p>* **Availability** - Appointment availabilities describe an appointment attendees' preference in
     * meeting dates and times. Availability information is only available for *Planner Coordinated
     * Appointments*.
     * 
     * <p>* **Interests** - Appointment interests describe appointment attendees' preferences in meeting with
     * another organization or individual. Interest information is only available for *Planner Coordinated
     * Appointments*.
     * * **Available Times** - Available times lists time ranges available  for appointments scheduling.
     * * **Locations** - Locations lists details of all locations created for  scheduling appointments.
     * * **Appointment Types** - Appointment Types lists details of different  appointment types created.
     */
    private final Appointments appointments;

    /**
     * The Attendee entity primarily refers to the person throughout the lifecycle of an event including
     * registration, enrollment into sessions, and participation. Once contacts are added to events, an
     * Attendee entity is created. This API allows you to read attendee data and add new attendees to your
     * Cvent events.
     */
    private final Attendees attendees;

    /**
     * Event Credits reward attendees for participating in your events. Planners can award credits for the
     * entire event, specific sessions, or both. You can also award credits after attendees complete
     * surveys.
     * 
     * <p>Use these APIs to retrieve credit details for your attendees.
     */
    private final EventCredits eventCredits;

    /**
     * The Attendee Insights feature provides valuable information about your event attendees. It assists
     * planners, marketers, and exhibitors in targeting customers effectively, thereby enhancing engagement
     * and conversion rates for marketing campaigns. Utilize these APIs to fetch engagement score data from
     * your event and integrate it into your marketing systems.
     * 
     * <p>**Engagement Score** - Engagement scores are a container that track individual attendees' engagement
     * with your event. Attendees earn points by performing actions, which indicates their relative
     * engagement with your content.
     * 
     * <p>**Actions** - These are distinct, trackable activities that attendees can perform during an event.
     * Points are awarded for these actions and contribute to the engagement score.
     */
    private final AttendeeInsights attendeeInsights;

    /**
     * The Attendee Activities API gives valuable insight into your customer's experience at your Cvent
     * event.
     * Now, you can get a fuller picture of your customer's journey, including onsite activities, offsite
     * meetings,
     * breakout sessions, booth visits, and virtual seminars. You can use that data to compile a unified
     * customer
     * profile that integrates with your external systems and data.
     * 
     * <p>By gathering all this info into a single interface, the API allows you to see your customer's
     * engagement
     * at every step of the journey, create engagement models, and integrate that data with your external
     * tools.
     * You can use this data to understand what your customers are looking for, keep them engaged, build
     * loyalty,
     * drive upsell, and increase the long-term value of your events.
     * 
     * <p>Please note that attendee activities data is retained for a maximum of 2 years and is purged
     * thereafter.
     * 
     * <p>For more details on [attendee
     * activities](https://developers.cvent.com/docs/rest-api/explanation/attendee-activities).
     */
    private final AttendeeActivities attendeeActivities;

    /**
     * Audience Segments allow planners to segment their attendees into groups and
     * better manage the attendee experience based on their defined segments. Audience
     * Segments APIs will enable you to get, create, and update segment information.
     * These APIs will also allow you to get and update attendees associated with an
     * audience segment.
     */
    private final AudienceSegments audienceSegments;

    /**
     * Event planners use emails to invite registrants, market their events and request feedback from
     * attendees. Use these APIs to get historical data about your emails and see relevant details like the
     * types of emails sent, the recipient, and delivery status.
     */
    private final Emails emails;

    /**
     * Budget is an event feature used to organize spending and track
     * [allocations](https://support.cvent.com/s/communityarticle/Setting-Up-Budget-Allocations). Use this
     * API to view budget items, cards and card transactions related to the budget module.
     */
    private final Budget budget;

    /**
     * The Bulk API provides a simple interface to upload large amounts of data into Cvent. The API
     * processes the uploaded data asynchronously making API calls on behalf of the caller.
     * 
     * <p>Consumers of the bulk API will do the following:
     * * [Create a bulk job](#operation/createBulkJob) with or without data
     * * *(Optional)* If a bulk job was created without data, then [upload bulk job
     * data](#operation/uploadBulkJobData) as many times as needed
     * * *(Optional)* [Run bulk job](#operation/runBulkJob)
     * * Use [get bulk job](#operation/getBulkJobById) to track status
     * * Use [list bulk job results](#operation/listBulkJobResult) to get details of items uploaded
     * 
     * <p>For more details, see the [Bulk Job User Guide](/docs/rest-api/guides/bulk-api-user-guide).
     * 
     * <p>**Note:** These bulk jobs have a TTL and will expire once they are complete or never ran. Bulk jobs
     * will expire
     * 1 week after creation, and this one week is refreshed when data is uploaded and the job is run. The
     * Bulk Job
     * will be available for 1 week after completion to get results. Right now we do NOT support bulk GET
     * requests.
     */
    private final Bulk bulk;

    /**
     * Planners use eMarketing campaigns to contact an audience, such as newsletters, press releases, or
     * product updates. Campaign emails are used as newsletters, promotions, advertisements, or marketing
     * messages. Use these APIs to send an email and get historical data about your campaigns.
     */
    private final Campaigns campaigns;

    /**
     * **Card Tokenization**: Tokenization is the process Cvent uses to collect sensitive card details and
     * personally identifiable information (PII), directly from your customers in a secure manner.
     * This guarantees that no sensitive card data touches your server, and allows your integration to
     * operate in compliance with PCI standards. A card token is a unique identifier that represents
     * sensitive credit card information.
     * It is used as a secure alternative to directly handling credit card details during transactions.
     * The token can be used in place of the actual credit card data when making API calls, providing an
     * extra layer of security.
     */
    private final CardTokens cardTokens;

    /**
     * RegLink APIs allow you to exchange data with Cvent Passkey events and hotel reservation-booking
     * engines.
     * Generally, there are four primary categories of functionality that RegLink APIs support:
     * * **Sync Registrants.** Allow individual registrant information to be sent to Cvent Passkey,
     * streamlining the hotel reservation process for attendees. Attendees can then be directed to Passkey
     * post-registration and reserve a hotel room without re-entering their data again. This is done with
     * the reservation request endpoint.
     * * **Fetch Rooms.** The ability for an external system to fetch Passkey event details and hotel room
     * availability based on various criteria.
     * * **Fetch Reservations.** The ability for an external system to fetch detailed reservation
     * information for individual registrants. This is done with the Reservation endpoint and the Callback
     * system.
     * * **Manage Reservations.** The ability for an external system to create, modify or cancel a
     * registrant's hotel reservation directly or indirectly.
     * 
     * <p>If you need authentication credentials or have any questions regarding the RegLink APIs, contact the
     * [Cvent Passkey Integrations
     * Team](https://web.cvent.com/survey/c6a60a30-d981-4ae8-af28-686af0eeb9b7/welcome).
     * 
     * <p>* **Passkey.** A software solution allowing your invitees to book room reservations directly with
     * hotels.
     * * **Housing Event.** An Passkey event where invitees will be able to register and book rooms.
     * * **Reservation Request.** Represents a request to book rooms in the housing event.
     * * **RegLink.** A feature that links Passkey events to your registration events, enabling you to sync
     * data between the two events.
     * * **Callback.** [Passkey's callback system](/docs/passkey/REST/callbacks) allows for a push
     * notification to be sent to your system when a reservation is created, modified or cancelled.
     */
    private final Housing housing;

    /**
     * A contact is a record in your account’s address book that represents an individual. The data stored
     * within these records includes personal information, group memberships, and historical data on event
     * attendance. Contact APIs will allow you to get contact information, track updates to contact
     * information, and create contacts.
     */
    private final Contacts contacts;

    /**
     * These APIs allow you to create hooks. When triggered, a hook sends a request to your service to
     * get updated data related to the related Cvent object.
     * 
     * <p>For more information on using hooks, see the
     * [getting started guide](https://developers.cvent.com/docs/cid/attendee-identification-service).
     */
    private final Hooks hooks;

    /**
     * Custom Fields are created by event planners to track important information about specific objects
     * like events, contacts, or sessions. Use these APIs to view, create, and update custom fields in your
     * account and their related details.
     */
    private final CustomFields customFields;

    /**
     * * **Leads -** Leads include leads gathered by LeadCapture, Appointments, and Inbound Leads. Use this
     * API to get information for the lead and how it was captured.
     * 
     * <p>* **Lead Qualification Question -** Custom questions created by each exhibitor to qualify a lead.
     * Questions and answers are their own endpoints.
     * 
     * <p>* **Eliterature Requests -** List of E-literature requests data sent to lead.
     */
    private final Leads leads;

    /**
     * Event roles are event specific permission sets for your organization's users. Use these APIs to
     * retrieve, create, update, and delete event role assignments to your organization's users.
     */
    private final EventRole eventRole;

    /**
     * Surveys are lists of questions deployed to your contacts. Surveys can be standalone or can be
     * associated to a Cvent event. Use these APIs to search for surveys and retrieve the associated
     * questions and responses.
     */
    private final Surveys surveys;

    /**
     * These APIs retrieve and manage attendee messages—communications exchanged between attendees within
     * channels. Channels are virtual spaces created for one-on-one or group conversations, allowing
     * attendees to communicate directly or in groups during an event.
     */
    private final AttendeeMessages attendeeMessages;

    /**
     * Badge print jobs can be scheduled to a printer pool, so a printer in the printer pool can consume
     * the job and print the badge.
     */
    private final BadgePrintJob badgePrintJob;

    /**
     * Badge printer pools are set up from Cvent UI. You can use this API to retrieve badge printer pools.
     */
    private final BadgePrinterPools badgePrinterPools;

    /**
     * Discounts provide a way to reduce the cost of event registration items. Use these APIs to manage
     * event discounts, including creating, updating, and linking discounts to agenda items.
     */
    private final Discounts discounts;

    /**
     * Event travel lets planners capture air &amp; hotel requests from attendees and track air actuals and
     * hotel reservations at your event. Use these endpoints to retrieve your air, hotel and housing
     * reservation request data from your events. **Housing Reservation Request** - An association between
     * an attendee's information in a registration event and a Cvent Passkey event.
     * 
     * <p>Also known as a Passkey bridge.
     */
    private final EventTravel eventTravel;

    /**
     * * **Exhibitor -** An exhibitor is an organization that is sponsoring or exhibiting at your event.
     * This API allows you to get information about your exhibitors.
     * 
     * <p>* **Registration Pack -** Registration Pack provides capacities of different registration types
     * allocated by the planner to a specific exhibitor.
     * 
     * <p>* **Exhibitor Questions -** Set of questions for an exhibitor.
     * 
     * <p>* **Exhibitor Answers -** Set of answers to exhibitor questions for an exhibitor.
     * 
     * <p>* **Exhibitor Categories -** Set of exhibitor categories for an event. Exhibitor Categories are used
     * in Attendee Hub and Event App to differentiate exhibitors.
     * 
     * <p>* **Sponsorship Levels -** Sponsorship levels are defined by the planner. Use this API to get
     * details of existing sponsorship levels defined within the event. To assign sponsorship levels to
     * exhibitors, use the Exhibitor endpoints.
     */
    private final Exhibitor exhibitor;

    /**
     * * **Exhibitor Admin -** Exhibitor Admins are administrators that have access to the exhibitor
     * portal. In the portal, they are able to complete pre-event tasks, manage their team, purchase
     * LeadCapture (if applicable), and download leads. Use this API to get details for existing admins,
     * create new admins, and update admin details.
     * 
     * <p>* **Exhibitor Booth Staff -** Exhibitor booth staff is someone that manages the frontline of the
     * exhibitor's booth onsite or virtually. Booth staff needs to be registered for the event in order to
     * be associated with an exhibitor. Use this API to get details for the existing booth staff(s) for a
     * specific exhibitor, associate new booth staff to the exhibitor, and delete the association of a
     * booth staff if required.
     */
    private final ExhibitorTeam exhibitorTeam;

    /**
     * Exhibitor Content operations for an exhibitor. This API allows you to upload &amp; get exhibitor
     * content data such as files, weblinks.
     */
    private final ExhibitorContent exhibitorContent;

    /**
     * EventFeatures related APIs
     */
    private final EventFeatures eventFeatures;

    /**
     * When planning an event, meeting request forms are customizable online questionnaires to capture
     * information about the event and facilitate the approval of events. When a meeting request form is
     * submitted it becomes a meeting request. Use this API to view forms, create meeting requests and
     * track the status of meeting requests.
     * 
     * <p>**Legacy meeting request forms are not supported by this API.**
     */
    private final MeetingRequest meetingRequest;

    /**
     * Seating lets you plan seating at your events by configuring tables and assigning seats to your
     * attendees. The seating APIs allow you to create, update, and delete seating, tables, seats, and
     * seating assignments.
     * * **Seating** - A diagram of tables and seats. Your event can have many seatings.
     * * **Table** - A communal location where chairs are assigned. Each table is assigned to a seating.
     * * **Seat** - Individual seat at a table. Each seat can be assigned to an attendee.
     * * **Assignments** - Seat that is assigned to an attendee.
     */
    private final Seating seating;

    /**
     * A session is a presentation, discussion, or activity. Use these APIs to get information about your
     * sessions and use the session ID to get further data within the event.
     */
    private final Sessions sessions;

    /**
     * Allows you to upload files and get file location using the file ID. File ID can be used with other
     * APIs to associate the file to an entity. For example:
     * * <a href="#operation/addSessionDoc">Add Document To Session</a>
     * * <a href="#operation/addSpeakerDoc">Add Document To Speaker</a>
     * * <a href="#operation/assignContactProfileImage">Assign Contact Profile Picture</a>
     * * <a href="#operation/updateExhibitorCategoryBanner">Assign Banner to Exhibitor Category</a>
     * * <a href="#operation/updateExhibitorBanner">Assign Exhibitor Banner Image</a>
     * * <a href="#operation/updateExhibitorLogo">Assign Exhibitor Logo Image</a>
     * * <a href="#operation/assignSpeakerProfileImage">Assign Speaker Profile Picture</a>
     */
    private final File file;

    /**
     * APIs for managing hotel-related operations.
     */
    private final HousingHotels housingHotels;

    /**
     * An Events+ Hub persists basic information needed to assign an owner and optionally customize the
     * public presentation.
     */
    private final EventsPlusHub eventsPlusHub;

    /**
     * These API's provide compliance support for regulated industries.
     * 
     * <p>**Communication Compliance** lets you view communication activities across your account.
     * Various written forms of communication are captured. For example, emails, SMS, chat messages, or
     * push notifications
     * can be downloaded for archival or analysis.
     */
    private final Compliance compliance;

    /**
     * Available endpoints used to initiate an authentication request to obtain and validate an access
     * token.
     */
    private final Authentication authentication;

    /**
     * Process forms automate data collection and notifications related to planning and executing events.
     * Process form submissions are responses to a specific process form, providing data the form requests.
     * Use these APIs to manage process form submissions in your account.
     */
    private final ProcessForm processForm;

    /**
     * Speakers are individuals presenting at your event's session(s). Use Speaker APIs to read existing
     * speaker data, create new speakers or update existing speakers in your events.
     */
    private final Speakers speakers;

    /**
     * Beta - All APIs are in Beta. Proposal Drafts are editable copies of proposals. This API allows you
     * to edit proposal data privately before publishing.
     */
    private final ProposalDraft proposalDraft;

    /**
     * The [SCIM](https://www.simplecloud.info/) standard allows for easier cross-domain identity
     * management. This API allows you to manage your account users and SCIM groups (representing Cvent
     * user roles).
     */
    private final UserSCIM userSCIM;

    /**
     * Retrieves Check-In &amp; Check-Out Signatures Of Attendees
     */
    private final Signatures signatures;

    /**
     * The travel account, or corporation that represents the demand-side of travel RFPs.
     */
    private final TravelAccounts travelAccounts;

    /**
     * The Travel RFP APIs provide access to travel programs and proposals. A travel program
     * represents a request for proposal (RFP) that defines the specific travel needs and
     * requirements of a travel account. The travel account solicits hotels and other travel
     * suppliers to respond to the program with proposals, which provide detailed
     * information on rates and amenities.'
     */
    private final TravelRFPs travelRFPs;

    /**
     * The Travel Supplier APIs provide access to Cvent data related to hotels, apartments, and other
     * travel providers. This includes information related to properties, and sleeping rooms.
     */
    private final TravelSuppliers travelSuppliers;

    /**
     * Use these APIs view your REST API usage and limits metrics. For more details on limits - [Rate
     * Limits](#section/Getting-Started/Rate-Limits)
     */
    private final Usage usage;

    /**
     * Videos can be added to Cvent events with renditions at various resolutions, audio files, reactions
     * tracks, and text tracks. Attendee viewership is tracked to get insight into durations, devices used
     * and venue where the video is watched. Use these APIs to view and manage videos, related text tracks,
     * and audio files.
     * 
     * <p>Use these APIs also to get insight into video viewership.
     */
    private final Video video;

    /**
     * Webcasts are virtual or livestreaming components of your Cvent events. Use these APIs to integrate
     * your virtual events from outside sources into your Cvent workflows, create and delete webcasts from
     * within Cvent, and retrieve and update webcast details.
     */
    private final Webcasts webcasts;

    /**
     * Operations for managing account users and user groups, including creation, retrieval, update, and
     * deletion. Use these endpoints to administer user access and roles within your account.
     */
    public Users users() {
        return users;
    }

    /**
     * Events are used to collect registrations and allow attendees to select their schedule. Use these
     * APIs to get information about your events and use the event ID to get further details about the
     * event, such as Attendees and Sessions.
     */
    public Events events() {
        return events;
    }

    /**
     * An appointment is a meeting scheduled between two or more parties. These APIs allow you to get
     * information about your Cvent Appointments: appointment attendees, their interests, and
     * availabilities.
     * 
     * <p>* **Appointment Events** - Appointment events help planners facilitate meetings between attendees.
     * Attendees express their interest and availability to schedule appointments. Use this API to get
     * information about appointments and appointment attendees.
     * 
     * <p>* **Appointment Attendees** - Appointment attendees are meeting participants. They can be attendees
     * of either events or appointment events. Get information about these participants or use their
     * appointment attendee ID to get their availabilities and interests.
     * 
     * <p>* **Availability** - Appointment availabilities describe an appointment attendees' preference in
     * meeting dates and times. Availability information is only available for *Planner Coordinated
     * Appointments*.
     * 
     * <p>* **Interests** - Appointment interests describe appointment attendees' preferences in meeting with
     * another organization or individual. Interest information is only available for *Planner Coordinated
     * Appointments*.
     * * **Available Times** - Available times lists time ranges available  for appointments scheduling.
     * * **Locations** - Locations lists details of all locations created for  scheduling appointments.
     * * **Appointment Types** - Appointment Types lists details of different  appointment types created.
     */
    public Appointments appointments() {
        return appointments;
    }

    /**
     * The Attendee entity primarily refers to the person throughout the lifecycle of an event including
     * registration, enrollment into sessions, and participation. Once contacts are added to events, an
     * Attendee entity is created. This API allows you to read attendee data and add new attendees to your
     * Cvent events.
     */
    public Attendees attendees() {
        return attendees;
    }

    /**
     * Event Credits reward attendees for participating in your events. Planners can award credits for the
     * entire event, specific sessions, or both. You can also award credits after attendees complete
     * surveys.
     * 
     * <p>Use these APIs to retrieve credit details for your attendees.
     */
    public EventCredits eventCredits() {
        return eventCredits;
    }

    /**
     * The Attendee Insights feature provides valuable information about your event attendees. It assists
     * planners, marketers, and exhibitors in targeting customers effectively, thereby enhancing engagement
     * and conversion rates for marketing campaigns. Utilize these APIs to fetch engagement score data from
     * your event and integrate it into your marketing systems.
     * 
     * <p>**Engagement Score** - Engagement scores are a container that track individual attendees' engagement
     * with your event. Attendees earn points by performing actions, which indicates their relative
     * engagement with your content.
     * 
     * <p>**Actions** - These are distinct, trackable activities that attendees can perform during an event.
     * Points are awarded for these actions and contribute to the engagement score.
     */
    public AttendeeInsights attendeeInsights() {
        return attendeeInsights;
    }

    /**
     * The Attendee Activities API gives valuable insight into your customer's experience at your Cvent
     * event.
     * Now, you can get a fuller picture of your customer's journey, including onsite activities, offsite
     * meetings,
     * breakout sessions, booth visits, and virtual seminars. You can use that data to compile a unified
     * customer
     * profile that integrates with your external systems and data.
     * 
     * <p>By gathering all this info into a single interface, the API allows you to see your customer's
     * engagement
     * at every step of the journey, create engagement models, and integrate that data with your external
     * tools.
     * You can use this data to understand what your customers are looking for, keep them engaged, build
     * loyalty,
     * drive upsell, and increase the long-term value of your events.
     * 
     * <p>Please note that attendee activities data is retained for a maximum of 2 years and is purged
     * thereafter.
     * 
     * <p>For more details on [attendee
     * activities](https://developers.cvent.com/docs/rest-api/explanation/attendee-activities).
     */
    public AttendeeActivities attendeeActivities() {
        return attendeeActivities;
    }

    /**
     * Audience Segments allow planners to segment their attendees into groups and
     * better manage the attendee experience based on their defined segments. Audience
     * Segments APIs will enable you to get, create, and update segment information.
     * These APIs will also allow you to get and update attendees associated with an
     * audience segment.
     */
    public AudienceSegments audienceSegments() {
        return audienceSegments;
    }

    /**
     * Event planners use emails to invite registrants, market their events and request feedback from
     * attendees. Use these APIs to get historical data about your emails and see relevant details like the
     * types of emails sent, the recipient, and delivery status.
     */
    public Emails emails() {
        return emails;
    }

    /**
     * Budget is an event feature used to organize spending and track
     * [allocations](https://support.cvent.com/s/communityarticle/Setting-Up-Budget-Allocations). Use this
     * API to view budget items, cards and card transactions related to the budget module.
     */
    public Budget budget() {
        return budget;
    }

    /**
     * The Bulk API provides a simple interface to upload large amounts of data into Cvent. The API
     * processes the uploaded data asynchronously making API calls on behalf of the caller.
     * 
     * <p>Consumers of the bulk API will do the following:
     * * [Create a bulk job](#operation/createBulkJob) with or without data
     * * *(Optional)* If a bulk job was created without data, then [upload bulk job
     * data](#operation/uploadBulkJobData) as many times as needed
     * * *(Optional)* [Run bulk job](#operation/runBulkJob)
     * * Use [get bulk job](#operation/getBulkJobById) to track status
     * * Use [list bulk job results](#operation/listBulkJobResult) to get details of items uploaded
     * 
     * <p>For more details, see the [Bulk Job User Guide](/docs/rest-api/guides/bulk-api-user-guide).
     * 
     * <p>**Note:** These bulk jobs have a TTL and will expire once they are complete or never ran. Bulk jobs
     * will expire
     * 1 week after creation, and this one week is refreshed when data is uploaded and the job is run. The
     * Bulk Job
     * will be available for 1 week after completion to get results. Right now we do NOT support bulk GET
     * requests.
     */
    public Bulk bulk() {
        return bulk;
    }

    /**
     * Planners use eMarketing campaigns to contact an audience, such as newsletters, press releases, or
     * product updates. Campaign emails are used as newsletters, promotions, advertisements, or marketing
     * messages. Use these APIs to send an email and get historical data about your campaigns.
     */
    public Campaigns campaigns() {
        return campaigns;
    }

    /**
     * **Card Tokenization**: Tokenization is the process Cvent uses to collect sensitive card details and
     * personally identifiable information (PII), directly from your customers in a secure manner.
     * This guarantees that no sensitive card data touches your server, and allows your integration to
     * operate in compliance with PCI standards. A card token is a unique identifier that represents
     * sensitive credit card information.
     * It is used as a secure alternative to directly handling credit card details during transactions.
     * The token can be used in place of the actual credit card data when making API calls, providing an
     * extra layer of security.
     */
    public CardTokens cardTokens() {
        return cardTokens;
    }

    /**
     * RegLink APIs allow you to exchange data with Cvent Passkey events and hotel reservation-booking
     * engines.
     * Generally, there are four primary categories of functionality that RegLink APIs support:
     * * **Sync Registrants.** Allow individual registrant information to be sent to Cvent Passkey,
     * streamlining the hotel reservation process for attendees. Attendees can then be directed to Passkey
     * post-registration and reserve a hotel room without re-entering their data again. This is done with
     * the reservation request endpoint.
     * * **Fetch Rooms.** The ability for an external system to fetch Passkey event details and hotel room
     * availability based on various criteria.
     * * **Fetch Reservations.** The ability for an external system to fetch detailed reservation
     * information for individual registrants. This is done with the Reservation endpoint and the Callback
     * system.
     * * **Manage Reservations.** The ability for an external system to create, modify or cancel a
     * registrant's hotel reservation directly or indirectly.
     * 
     * <p>If you need authentication credentials or have any questions regarding the RegLink APIs, contact the
     * [Cvent Passkey Integrations
     * Team](https://web.cvent.com/survey/c6a60a30-d981-4ae8-af28-686af0eeb9b7/welcome).
     * 
     * <p>* **Passkey.** A software solution allowing your invitees to book room reservations directly with
     * hotels.
     * * **Housing Event.** An Passkey event where invitees will be able to register and book rooms.
     * * **Reservation Request.** Represents a request to book rooms in the housing event.
     * * **RegLink.** A feature that links Passkey events to your registration events, enabling you to sync
     * data between the two events.
     * * **Callback.** [Passkey's callback system](/docs/passkey/REST/callbacks) allows for a push
     * notification to be sent to your system when a reservation is created, modified or cancelled.
     */
    public Housing housing() {
        return housing;
    }

    /**
     * A contact is a record in your account’s address book that represents an individual. The data stored
     * within these records includes personal information, group memberships, and historical data on event
     * attendance. Contact APIs will allow you to get contact information, track updates to contact
     * information, and create contacts.
     */
    public Contacts contacts() {
        return contacts;
    }

    /**
     * These APIs allow you to create hooks. When triggered, a hook sends a request to your service to
     * get updated data related to the related Cvent object.
     * 
     * <p>For more information on using hooks, see the
     * [getting started guide](https://developers.cvent.com/docs/cid/attendee-identification-service).
     */
    public Hooks hooks() {
        return hooks;
    }

    /**
     * Custom Fields are created by event planners to track important information about specific objects
     * like events, contacts, or sessions. Use these APIs to view, create, and update custom fields in your
     * account and their related details.
     */
    public CustomFields customFields() {
        return customFields;
    }

    /**
     * * **Leads -** Leads include leads gathered by LeadCapture, Appointments, and Inbound Leads. Use this
     * API to get information for the lead and how it was captured.
     * 
     * <p>* **Lead Qualification Question -** Custom questions created by each exhibitor to qualify a lead.
     * Questions and answers are their own endpoints.
     * 
     * <p>* **Eliterature Requests -** List of E-literature requests data sent to lead.
     */
    public Leads leads() {
        return leads;
    }

    /**
     * Event roles are event specific permission sets for your organization's users. Use these APIs to
     * retrieve, create, update, and delete event role assignments to your organization's users.
     */
    public EventRole eventRole() {
        return eventRole;
    }

    /**
     * Surveys are lists of questions deployed to your contacts. Surveys can be standalone or can be
     * associated to a Cvent event. Use these APIs to search for surveys and retrieve the associated
     * questions and responses.
     */
    public Surveys surveys() {
        return surveys;
    }

    /**
     * These APIs retrieve and manage attendee messages—communications exchanged between attendees within
     * channels. Channels are virtual spaces created for one-on-one or group conversations, allowing
     * attendees to communicate directly or in groups during an event.
     */
    public AttendeeMessages attendeeMessages() {
        return attendeeMessages;
    }

    /**
     * Badge print jobs can be scheduled to a printer pool, so a printer in the printer pool can consume
     * the job and print the badge.
     */
    public BadgePrintJob badgePrintJob() {
        return badgePrintJob;
    }

    /**
     * Badge printer pools are set up from Cvent UI. You can use this API to retrieve badge printer pools.
     */
    public BadgePrinterPools badgePrinterPools() {
        return badgePrinterPools;
    }

    /**
     * Discounts provide a way to reduce the cost of event registration items. Use these APIs to manage
     * event discounts, including creating, updating, and linking discounts to agenda items.
     */
    public Discounts discounts() {
        return discounts;
    }

    /**
     * Event travel lets planners capture air &amp; hotel requests from attendees and track air actuals and
     * hotel reservations at your event. Use these endpoints to retrieve your air, hotel and housing
     * reservation request data from your events. **Housing Reservation Request** - An association between
     * an attendee's information in a registration event and a Cvent Passkey event.
     * 
     * <p>Also known as a Passkey bridge.
     */
    public EventTravel eventTravel() {
        return eventTravel;
    }

    /**
     * * **Exhibitor -** An exhibitor is an organization that is sponsoring or exhibiting at your event.
     * This API allows you to get information about your exhibitors.
     * 
     * <p>* **Registration Pack -** Registration Pack provides capacities of different registration types
     * allocated by the planner to a specific exhibitor.
     * 
     * <p>* **Exhibitor Questions -** Set of questions for an exhibitor.
     * 
     * <p>* **Exhibitor Answers -** Set of answers to exhibitor questions for an exhibitor.
     * 
     * <p>* **Exhibitor Categories -** Set of exhibitor categories for an event. Exhibitor Categories are used
     * in Attendee Hub and Event App to differentiate exhibitors.
     * 
     * <p>* **Sponsorship Levels -** Sponsorship levels are defined by the planner. Use this API to get
     * details of existing sponsorship levels defined within the event. To assign sponsorship levels to
     * exhibitors, use the Exhibitor endpoints.
     */
    public Exhibitor exhibitor() {
        return exhibitor;
    }

    /**
     * * **Exhibitor Admin -** Exhibitor Admins are administrators that have access to the exhibitor
     * portal. In the portal, they are able to complete pre-event tasks, manage their team, purchase
     * LeadCapture (if applicable), and download leads. Use this API to get details for existing admins,
     * create new admins, and update admin details.
     * 
     * <p>* **Exhibitor Booth Staff -** Exhibitor booth staff is someone that manages the frontline of the
     * exhibitor's booth onsite or virtually. Booth staff needs to be registered for the event in order to
     * be associated with an exhibitor. Use this API to get details for the existing booth staff(s) for a
     * specific exhibitor, associate new booth staff to the exhibitor, and delete the association of a
     * booth staff if required.
     */
    public ExhibitorTeam exhibitorTeam() {
        return exhibitorTeam;
    }

    /**
     * Exhibitor Content operations for an exhibitor. This API allows you to upload &amp; get exhibitor
     * content data such as files, weblinks.
     */
    public ExhibitorContent exhibitorContent() {
        return exhibitorContent;
    }

    /**
     * EventFeatures related APIs
     */
    public EventFeatures eventFeatures() {
        return eventFeatures;
    }

    /**
     * When planning an event, meeting request forms are customizable online questionnaires to capture
     * information about the event and facilitate the approval of events. When a meeting request form is
     * submitted it becomes a meeting request. Use this API to view forms, create meeting requests and
     * track the status of meeting requests.
     * 
     * <p>**Legacy meeting request forms are not supported by this API.**
     */
    public MeetingRequest meetingRequest() {
        return meetingRequest;
    }

    /**
     * Seating lets you plan seating at your events by configuring tables and assigning seats to your
     * attendees. The seating APIs allow you to create, update, and delete seating, tables, seats, and
     * seating assignments.
     * * **Seating** - A diagram of tables and seats. Your event can have many seatings.
     * * **Table** - A communal location where chairs are assigned. Each table is assigned to a seating.
     * * **Seat** - Individual seat at a table. Each seat can be assigned to an attendee.
     * * **Assignments** - Seat that is assigned to an attendee.
     */
    public Seating seating() {
        return seating;
    }

    /**
     * A session is a presentation, discussion, or activity. Use these APIs to get information about your
     * sessions and use the session ID to get further data within the event.
     */
    public Sessions sessions() {
        return sessions;
    }

    /**
     * Allows you to upload files and get file location using the file ID. File ID can be used with other
     * APIs to associate the file to an entity. For example:
     * * <a href="#operation/addSessionDoc">Add Document To Session</a>
     * * <a href="#operation/addSpeakerDoc">Add Document To Speaker</a>
     * * <a href="#operation/assignContactProfileImage">Assign Contact Profile Picture</a>
     * * <a href="#operation/updateExhibitorCategoryBanner">Assign Banner to Exhibitor Category</a>
     * * <a href="#operation/updateExhibitorBanner">Assign Exhibitor Banner Image</a>
     * * <a href="#operation/updateExhibitorLogo">Assign Exhibitor Logo Image</a>
     * * <a href="#operation/assignSpeakerProfileImage">Assign Speaker Profile Picture</a>
     */
    public File file() {
        return file;
    }

    /**
     * APIs for managing hotel-related operations.
     */
    public HousingHotels housingHotels() {
        return housingHotels;
    }

    /**
     * An Events+ Hub persists basic information needed to assign an owner and optionally customize the
     * public presentation.
     */
    public EventsPlusHub eventsPlusHub() {
        return eventsPlusHub;
    }

    /**
     * These API's provide compliance support for regulated industries.
     * 
     * <p>**Communication Compliance** lets you view communication activities across your account.
     * Various written forms of communication are captured. For example, emails, SMS, chat messages, or
     * push notifications
     * can be downloaded for archival or analysis.
     */
    public Compliance compliance() {
        return compliance;
    }

    /**
     * Available endpoints used to initiate an authentication request to obtain and validate an access
     * token.
     */
    public Authentication authentication() {
        return authentication;
    }

    /**
     * Process forms automate data collection and notifications related to planning and executing events.
     * Process form submissions are responses to a specific process form, providing data the form requests.
     * Use these APIs to manage process form submissions in your account.
     */
    public ProcessForm processForm() {
        return processForm;
    }

    /**
     * Speakers are individuals presenting at your event's session(s). Use Speaker APIs to read existing
     * speaker data, create new speakers or update existing speakers in your events.
     */
    public Speakers speakers() {
        return speakers;
    }

    /**
     * Beta - All APIs are in Beta. Proposal Drafts are editable copies of proposals. This API allows you
     * to edit proposal data privately before publishing.
     */
    public ProposalDraft proposalDraft() {
        return proposalDraft;
    }

    /**
     * The [SCIM](https://www.simplecloud.info/) standard allows for easier cross-domain identity
     * management. This API allows you to manage your account users and SCIM groups (representing Cvent
     * user roles).
     */
    public UserSCIM userSCIM() {
        return userSCIM;
    }

    /**
     * Retrieves Check-In &amp; Check-Out Signatures Of Attendees
     */
    public Signatures signatures() {
        return signatures;
    }

    /**
     * The travel account, or corporation that represents the demand-side of travel RFPs.
     */
    public TravelAccounts travelAccounts() {
        return travelAccounts;
    }

    /**
     * The Travel RFP APIs provide access to travel programs and proposals. A travel program
     * represents a request for proposal (RFP) that defines the specific travel needs and
     * requirements of a travel account. The travel account solicits hotels and other travel
     * suppliers to respond to the program with proposals, which provide detailed
     * information on rates and amenities.'
     */
    public TravelRFPs travelRFPs() {
        return travelRFPs;
    }

    /**
     * The Travel Supplier APIs provide access to Cvent data related to hotels, apartments, and other
     * travel providers. This includes information related to properties, and sleeping rooms.
     */
    public TravelSuppliers travelSuppliers() {
        return travelSuppliers;
    }

    /**
     * Use these APIs view your REST API usage and limits metrics. For more details on limits - [Rate
     * Limits](#section/Getting-Started/Rate-Limits)
     */
    public Usage usage() {
        return usage;
    }

    /**
     * Videos can be added to Cvent events with renditions at various resolutions, audio files, reactions
     * tracks, and text tracks. Attendee viewership is tracked to get insight into durations, devices used
     * and venue where the video is watched. Use these APIs to view and manage videos, related text tracks,
     * and audio files.
     * 
     * <p>Use these APIs also to get insight into video viewership.
     */
    public Video video() {
        return video;
    }

    /**
     * Webcasts are virtual or livestreaming components of your Cvent events. Use these APIs to integrate
     * your virtual events from outside sources into your Cvent workflows, create and delete webcasts from
     * within Cvent, and retrieve and update webcast details.
     */
    public Webcasts webcasts() {
        return webcasts;
    }
    private final AsyncCventSDK asyncSDK;

    /**
     * The Builder class allows the configuration of a new instance of the SDK.
     */
    public static class Builder {

        private final SDKConfiguration sdkConfiguration = new SDKConfiguration();
        private String serverUrl;
        private String server;
        

        private Builder() {
        }

        /**
         * Allows the default HTTP client to be overridden with a custom implementation.
         *
         * @param client The HTTP client to use for all requests.
         * @return The builder instance.
         */
        public Builder client(HTTPClient client) {
            this.sdkConfiguration.setClient(client);
            return this;
        }
        
        /**
         * Configures the SDK to use the provided security details.
         *
         * @param security The security details to use for all requests. Can be {@code null}.
         * @return The builder instance.
         */
        public Builder security(com.cvent.models.components.Security security) {
            this.sdkConfiguration.setSecuritySource(SecuritySource.of(security));
            return this;
        }

        /**
         * Configures the SDK to use a custom security source.
         *
         * @param securitySource The security source to use for all requests.
         * @return The builder instance.
         */
        public Builder securitySource(SecuritySource securitySource) {
            Utils.checkNotNull(securitySource, "securitySource");
            this.sdkConfiguration.setSecuritySource(securitySource);
            return this;
        }
        
        /**
         * Overrides the default server URL.
         *
         * @param serverUrl The server URL to use for all requests.
         * @return The builder instance.
         */
        public Builder serverURL(String serverUrl) {
            this.serverUrl = serverUrl;
            return this;
        }

        /**
         * Overrides the default server URL with a templated URL populated with the provided parameters.
         *
         * @param serverUrl The server URL to use for all requests.
         * @param params The parameters to use when templating the URL.
         * @return The builder instance.
         */
        public Builder serverURL(String serverUrl, Map<String, String> params) {
            this.serverUrl = Utils.templateUrl(serverUrl, params);
            return this;
        }
        
        /**
         * Overrides the default server by index.
         *
         * @param serverIdx The server to use for all requests.
         * @return The builder instance.
         */
        public Builder serverIndex(int serverIdx) {
            this.sdkConfiguration.setServerIdx(serverIdx);
            this.serverUrl= SERVERS[serverIdx];
            return this;
        }
        
        /**
         * Overrides the default configuration for retries
         *
         * @param retryConfig The retry configuration to use for all requests.
         * @return The builder instance.
         */
        public Builder retryConfig(RetryConfig retryConfig) {
            this.sdkConfiguration.setRetryConfig(Optional.of(retryConfig));
            return this;
        }

        /**
         * Enables debug logging for HTTP requests and responses, including JSON body content.
         * <p>
         * Convenience method that calls {@link HTTPClient#enableDebugLogging(boolean)}.
         * {@link SpeakeasyHTTPClient} honors this setting. If you are using a custom HTTP client,
         * it is up to the custom client to honor this setting.
         * </p>
         *
         * @param enabled Whether to enable debug logging.
         * @return The builder instance.
         */
        public Builder enableHTTPDebugLogging(boolean enabled) {
            this.sdkConfiguration.client().enableDebugLogging(enabled);
            return this;
        }

        // Visible for testing, may be accessed via reflection in tests
        Builder _hooks(com.cvent.utils.Hooks hooks) {
            sdkConfiguration.setHooks(hooks);  
            return this;  
        }
        
        // Visible for testing, may be accessed via reflection in tests
        Builder _hooks(Consumer<? super com.cvent.utils.Hooks> consumer) {
            consumer.accept(sdkConfiguration.hooks());
            return this;    
        }

        /**
         * Builds a new instance of the SDK.
         *
         * @return The SDK instance.
         */
        public CventSDK build() {
            if (serverUrl == null || serverUrl.isBlank()) {
                serverUrl = SERVERS[0];
                sdkConfiguration.setServerIdx(0);
            }
            sdkConfiguration.setServerUrl(serverUrl);
            return new CventSDK(sdkConfiguration);
        }
    }

    /**
     * Get a new instance of the SDK builder to configure a new instance of the SDK.
     *
     * @return The SDK builder instance.
     */
    public static Builder builder() {
        return new Builder();
    }

    private CventSDK(SDKConfiguration sdkConfiguration) {
        sdkConfiguration.initialize();
        this.users = new Users(sdkConfiguration);
        this.events = new Events(sdkConfiguration);
        this.appointments = new Appointments(sdkConfiguration);
        this.attendees = new Attendees(sdkConfiguration);
        this.eventCredits = new EventCredits(sdkConfiguration);
        this.attendeeInsights = new AttendeeInsights(sdkConfiguration);
        this.attendeeActivities = new AttendeeActivities(sdkConfiguration);
        this.audienceSegments = new AudienceSegments(sdkConfiguration);
        this.emails = new Emails(sdkConfiguration);
        this.budget = new Budget(sdkConfiguration);
        this.bulk = new Bulk(sdkConfiguration);
        this.campaigns = new Campaigns(sdkConfiguration);
        this.cardTokens = new CardTokens(sdkConfiguration);
        this.housing = new Housing(sdkConfiguration);
        this.contacts = new Contacts(sdkConfiguration);
        this.hooks = new Hooks(sdkConfiguration);
        this.customFields = new CustomFields(sdkConfiguration);
        this.leads = new Leads(sdkConfiguration);
        this.eventRole = new EventRole(sdkConfiguration);
        this.surveys = new Surveys(sdkConfiguration);
        this.attendeeMessages = new AttendeeMessages(sdkConfiguration);
        this.badgePrintJob = new BadgePrintJob(sdkConfiguration);
        this.badgePrinterPools = new BadgePrinterPools(sdkConfiguration);
        this.discounts = new Discounts(sdkConfiguration);
        this.eventTravel = new EventTravel(sdkConfiguration);
        this.exhibitor = new Exhibitor(sdkConfiguration);
        this.exhibitorTeam = new ExhibitorTeam(sdkConfiguration);
        this.exhibitorContent = new ExhibitorContent(sdkConfiguration);
        this.eventFeatures = new EventFeatures(sdkConfiguration);
        this.meetingRequest = new MeetingRequest(sdkConfiguration);
        this.seating = new Seating(sdkConfiguration);
        this.sessions = new Sessions(sdkConfiguration);
        this.file = new File(sdkConfiguration);
        this.housingHotels = new HousingHotels(sdkConfiguration);
        this.eventsPlusHub = new EventsPlusHub(sdkConfiguration);
        this.compliance = new Compliance(sdkConfiguration);
        this.authentication = new Authentication(sdkConfiguration);
        this.processForm = new ProcessForm(sdkConfiguration);
        this.speakers = new Speakers(sdkConfiguration);
        this.proposalDraft = new ProposalDraft(sdkConfiguration);
        this.userSCIM = new UserSCIM(sdkConfiguration);
        this.signatures = new Signatures(sdkConfiguration);
        this.travelAccounts = new TravelAccounts(sdkConfiguration);
        this.travelRFPs = new TravelRFPs(sdkConfiguration);
        this.travelSuppliers = new TravelSuppliers(sdkConfiguration);
        this.usage = new Usage(sdkConfiguration);
        this.video = new Video(sdkConfiguration);
        this.webcasts = new Webcasts(sdkConfiguration);
        sdkConfiguration = sdkConfiguration.hooks().sdkInit(sdkConfiguration);
        this.asyncSDK = new AsyncCventSDK(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncCventSDK async() {
        return asyncSDK;
    }

}
