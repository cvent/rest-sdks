/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent;

import static com.cvent.operations.Operations.AsyncRequestOperation;

import com.cvent.models.components.CardTokenRequest;
import com.cvent.models.operations.async.CreateCardTokensRequestBuilder;
import com.cvent.models.operations.async.CreateCardTokensResponse;
import com.cvent.operations.CreateCardTokens;
import com.cvent.utils.Headers;
import jakarta.annotation.Nullable;
import java.lang.String;
import java.util.concurrent.CompletableFuture;

/**
 * **Card Tokenization**: Tokenization is the process Cvent uses to collect sensitive card details and
 * personally identifiable information (PII), directly from your customers in a secure manner.
 * This guarantees that no sensitive card data touches your server, and allows your integration to
 * operate in compliance with PCI standards. A card token is a unique identifier that represents
 * sensitive credit card information.
 * It is used as a secure alternative to directly handling credit card details during transactions.
 * The token can be used in place of the actual credit card data when making API calls, providing an
 * extra layer of security.
 */
public class AsyncCardTokens {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final CardTokens syncSDK;

    AsyncCardTokens(CardTokens syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public CardTokens sync() {
        return syncSDK;
    }


    /**
     * Create a Credit Card Token
     * 
     * <p>Creates a short-lived token representing a credit card. This token replaces the credit card in API
     * methods. It can be used multiple times within a 15-minute time-to-live (TTL) period.
     * 
     * <p>After 15 minutes, the token will expire and can no longer be used. If the same credit card is needed
     * to perform additional API methods after the 15-minute TTL, the card will need to be resubmitted for
     * another short-lived token.
     * 
     * @return The async call builder
     */
    public CreateCardTokensRequestBuilder createCardTokens() {
        return new CreateCardTokensRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a Credit Card Token
     * 
     * <p>Creates a short-lived token representing a credit card. This token replaces the credit card in API
     * methods. It can be used multiple times within a 15-minute time-to-live (TTL) period.
     * 
     * <p>After 15 minutes, the token will expire and can no longer be used. If the same credit card is needed
     * to perform additional API methods after the 15-minute TTL, the card will need to be resubmitted for
     * another short-lived token.
     * 
     * @return {@code CompletableFuture<CreateCardTokensResponse>} - The async response
     */
    public CompletableFuture<CreateCardTokensResponse> createCardTokensDirect() {
        return createCardTokens(null, null);
    }

    /**
     * Create a Credit Card Token
     * 
     * <p>Creates a short-lived token representing a credit card. This token replaces the credit card in API
     * methods. It can be used multiple times within a 15-minute time-to-live (TTL) period.
     * 
     * <p>After 15 minutes, the token will expire and can no longer be used. If the same credit card is needed
     * to perform additional API methods after the 15-minute TTL, the card will need to be resubmitted for
     * another short-lived token.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param serverURL Overrides the server URL.
     * @return {@code CompletableFuture<CreateCardTokensResponse>} - The async response
     */
    public CompletableFuture<CreateCardTokensResponse> createCardTokens(@Nullable CardTokenRequest request, @Nullable String serverURL) {
        AsyncRequestOperation<CardTokenRequest, CreateCardTokensResponse> operation
              = new CreateCardTokens.Async(sdkConfiguration, serverURL, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
