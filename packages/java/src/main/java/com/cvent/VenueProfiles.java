/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent;

import static com.cvent.operations.Operations.RequestOperation;

import com.cvent.models.operations.GetVenueDetailsOverviewRequest;
import com.cvent.models.operations.GetVenueDetailsOverviewRequestBuilder;
import com.cvent.models.operations.GetVenueDetailsOverviewResponse;
import com.cvent.models.operations.PatchVenueDetailsRequest;
import com.cvent.models.operations.PatchVenueDetailsRequestBuilder;
import com.cvent.models.operations.PatchVenueDetailsResponse;
import com.cvent.models.operations.PatchVenueFacilityRequest;
import com.cvent.models.operations.PatchVenueFacilityRequestBuilder;
import com.cvent.models.operations.PatchVenueFacilityResponse;
import com.cvent.models.operations.UpdateVenueDetailsRequest;
import com.cvent.models.operations.UpdateVenueDetailsRequestBuilder;
import com.cvent.models.operations.UpdateVenueDetailsResponse;
import com.cvent.models.operations.UpdateVenueFacilityRequest;
import com.cvent.models.operations.UpdateVenueFacilityRequestBuilder;
import com.cvent.models.operations.UpdateVenueFacilityResponse;
import com.cvent.operations.GetVenueDetailsOverview;
import com.cvent.operations.PatchVenueDetails;
import com.cvent.operations.PatchVenueFacility;
import com.cvent.operations.UpdateVenueDetails;
import com.cvent.operations.UpdateVenueFacility;
import com.cvent.utils.Headers;
import com.cvent.utils.Options;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;

/**
 * Manage venue profile details including type, contact information, address, and other venue
 * properties.
 */
public class VenueProfiles {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncVenueProfiles asyncSDK;

    VenueProfiles(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncVenueProfiles(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     *
     * @return The async SDK
     */
    public AsyncVenueProfiles async() {
        return asyncSDK;
    }

    /**
     * Update Venue Details
     *
     * <p>Update venue details for the specified venue, replacing the existing venue details data.
     *
     * @return The call builder
     */
    public UpdateVenueDetailsRequestBuilder updateVenueDetails() {
        return new UpdateVenueDetailsRequestBuilder(sdkConfiguration);
    }

    /**
     * Update Venue Details
     *
     * <p>Update venue details for the specified venue, replacing the existing venue details data.
     *
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateVenueDetailsResponse updateVenueDetails(@Nonnull UpdateVenueDetailsRequest request) {
        return updateVenueDetails(request, null);
    }

    /**
     * Update Venue Details
     *
     * <p>Update venue details for the specified venue, replacing the existing venue details data.
     *
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateVenueDetailsResponse updateVenueDetails(
            @Nonnull UpdateVenueDetailsRequest request, @Nullable Options options) {
        RequestOperation<UpdateVenueDetailsRequest, UpdateVenueDetailsResponse> operation =
                new UpdateVenueDetails.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Patch Venue Details
     *
     * <p>Partially updates the specified venue's details using JSON Merge Patch, as defined in [RFC
     * 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
     *
     * <p>- Only fields present in the request body are updated; omitted fields remain unchanged.
     * - Set a field to `null` to unset it.
     * - If any part of the patch fails, the entire request will fail.
     *
     * <p>**Array Handling:**
     * - Arrays (`locationTypes`) are treated as **full replacements**, not partial updates.
     * - If an array property is not included in the payload, no updates are made to that field.
     * - If an empty array is sent, it replaces the existing array with an empty array.
     *
     * @return The call builder
     */
    public PatchVenueDetailsRequestBuilder patchVenueDetails() {
        return new PatchVenueDetailsRequestBuilder(sdkConfiguration);
    }

    /**
     * Patch Venue Details
     *
     * <p>Partially updates the specified venue's details using JSON Merge Patch, as defined in [RFC
     * 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
     *
     * <p>- Only fields present in the request body are updated; omitted fields remain unchanged.
     * - Set a field to `null` to unset it.
     * - If any part of the patch fails, the entire request will fail.
     *
     * <p>**Array Handling:**
     * - Arrays (`locationTypes`) are treated as **full replacements**, not partial updates.
     * - If an array property is not included in the payload, no updates are made to that field.
     * - If an empty array is sent, it replaces the existing array with an empty array.
     *
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public PatchVenueDetailsResponse patchVenueDetails(@Nonnull PatchVenueDetailsRequest request) {
        return patchVenueDetails(request, null);
    }

    /**
     * Patch Venue Details
     *
     * <p>Partially updates the specified venue's details using JSON Merge Patch, as defined in [RFC
     * 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
     *
     * <p>- Only fields present in the request body are updated; omitted fields remain unchanged.
     * - Set a field to `null` to unset it.
     * - If any part of the patch fails, the entire request will fail.
     *
     * <p>**Array Handling:**
     * - Arrays (`locationTypes`) are treated as **full replacements**, not partial updates.
     * - If an array property is not included in the payload, no updates are made to that field.
     * - If an empty array is sent, it replaces the existing array with an empty array.
     *
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public PatchVenueDetailsResponse patchVenueDetails(
            @Nonnull PatchVenueDetailsRequest request, @Nullable Options options) {
        RequestOperation<PatchVenueDetailsRequest, PatchVenueDetailsResponse> operation =
                new PatchVenueDetails.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Get Venue Details Overview
     *
     * <p>Retrieve an overview of venue details for the specified venue.
     *
     * @return The call builder
     */
    public GetVenueDetailsOverviewRequestBuilder getVenueDetailsOverview() {
        return new GetVenueDetailsOverviewRequestBuilder(sdkConfiguration);
    }

    /**
     * Get Venue Details Overview
     *
     * <p>Retrieve an overview of venue details for the specified venue.
     *
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetVenueDetailsOverviewResponse getVenueDetailsOverview(@Nonnull GetVenueDetailsOverviewRequest request) {
        return getVenueDetailsOverview(request, null);
    }

    /**
     * Get Venue Details Overview
     *
     * <p>Retrieve an overview of venue details for the specified venue.
     *
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetVenueDetailsOverviewResponse getVenueDetailsOverview(
            @Nonnull GetVenueDetailsOverviewRequest request, @Nullable Options options) {
        RequestOperation<GetVenueDetailsOverviewRequest, GetVenueDetailsOverviewResponse> operation =
                new GetVenueDetailsOverview.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update Venue Facility
     *
     * <p>Update the facility information for the specified venue, replacing all existing values with those
     * provided in the request body.
     *
     * <p>Field applicability varies by venue type:
     * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
     *
     * @return The call builder
     */
    public UpdateVenueFacilityRequestBuilder updateVenueFacility() {
        return new UpdateVenueFacilityRequestBuilder(sdkConfiguration);
    }

    /**
     * Update Venue Facility
     *
     * <p>Update the facility information for the specified venue, replacing all existing values with those
     * provided in the request body.
     *
     * <p>Field applicability varies by venue type:
     * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
     *
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateVenueFacilityResponse updateVenueFacility(@Nonnull UpdateVenueFacilityRequest request) {
        return updateVenueFacility(request, null);
    }

    /**
     * Update Venue Facility
     *
     * <p>Update the facility information for the specified venue, replacing all existing values with those
     * provided in the request body.
     *
     * <p>Field applicability varies by venue type:
     * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
     *
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateVenueFacilityResponse updateVenueFacility(
            @Nonnull UpdateVenueFacilityRequest request, @Nullable Options options) {
        RequestOperation<UpdateVenueFacilityRequest, UpdateVenueFacilityResponse> operation =
                new UpdateVenueFacility.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Patch Venue Facility
     *
     * <p>Partially update the facility information for the specified venue using JSON Merge Patch, as defined
     * in [RFC 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
     *
     * <p>- Only fields present in the request body are updated; omitted fields remain unchanged.
     * - Set a field to `null` to unset it.
     * - If any part of the patch fails, the entire request will fail.
     * - `proximityFromAirport.unit` is required when `proximityFromAirport.distance` is provided.
     *
     * <p>Field applicability varies by venue type:
     * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
     *
     * @return The call builder
     */
    public PatchVenueFacilityRequestBuilder patchVenueFacility() {
        return new PatchVenueFacilityRequestBuilder(sdkConfiguration);
    }

    /**
     * Patch Venue Facility
     *
     * <p>Partially update the facility information for the specified venue using JSON Merge Patch, as defined
     * in [RFC 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
     *
     * <p>- Only fields present in the request body are updated; omitted fields remain unchanged.
     * - Set a field to `null` to unset it.
     * - If any part of the patch fails, the entire request will fail.
     * - `proximityFromAirport.unit` is required when `proximityFromAirport.distance` is provided.
     *
     * <p>Field applicability varies by venue type:
     * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
     *
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public PatchVenueFacilityResponse patchVenueFacility(@Nonnull PatchVenueFacilityRequest request) {
        return patchVenueFacility(request, null);
    }

    /**
     * Patch Venue Facility
     *
     * <p>Partially update the facility information for the specified venue using JSON Merge Patch, as defined
     * in [RFC 7396](https://www.rfc-editor.org/rfc/rfc7396.html).
     *
     * <p>- Only fields present in the request body are updated; omitted fields remain unchanged.
     * - Set a field to `null` to unset it.
     * - If any part of the patch fails, the entire request will fail.
     * - `proximityFromAirport.unit` is required when `proximityFromAirport.distance` is provided.
     *
     * <p>Field applicability varies by venue type:
     * - Most facility information fields do not apply to CVB/DMC or Restaurant venues.
     *
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public PatchVenueFacilityResponse patchVenueFacility(
            @Nonnull PatchVenueFacilityRequest request, @Nullable Options options) {
        RequestOperation<PatchVenueFacilityRequest, PatchVenueFacilityResponse> operation =
                new PatchVenueFacility.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }
}
