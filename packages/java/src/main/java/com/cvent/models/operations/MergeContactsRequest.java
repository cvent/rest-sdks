/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.operations;

import com.cvent.models.components.ContactMerge;
import com.cvent.utils.SpeakeasyMetadata;
import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import jakarta.annotation.Nonnull;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class MergeContactsRequest {
    /**
     * ID of a contact.
     */
    @SpeakeasyMetadata("pathParam:style=simple,explode=false,name=id")
    private String id;

    /**
     * Merge the 'main' contact with one or two other 'secondary' contacts. By default, the main contact's
     * values will be used for the merged contact if present. Otherwise, the secondary contact's value is
     * used.
     * 
     * <p>For contact memberships and contact profile image, only the main contact's data is used.
     * 
     * <p>When merging contacts that contain secure fields, the `event/contacts:write-sensitive` scope is
     * required.
     * 
     * <p>**Conflict Resolution**
     * 
     * <p>* **contactFields**: A conflict arises for a contact field if it is empty in the main contact, two
     * secondary contacts are provided, and both secondary contacts have values for the field.
     * - You must provide the fieldName of the conflicted field and the ID of the contact whose value is to
     * be kept to continue with the merge.
     * - This array can also be used to override the default behavior and choose which contact to pull
     * values from for each contact field.
     * - The following contact fields are supported:
     * - `sourceId`
     * - `firstName`
     * - `lastName`
     * - `prefix`
     * - `email`
     * - `ccEmail`
     * - `company`
     * - `type.id`
     * - `nickname`
     * - `title`
     * - `workPhone`
     * - `middleName`
     * - `mobilePhone`
     * - `gender`
     * - `npi`
     * - `homeFax`
     * - `designation`
     * - `homePhone`
     * - `pager`
     * - `workFax`
     * - `lastModifiedBy`
     * - `lastModified`
     * - `created`
     * - `createdBy`
     * - `workAddress` (for address fields, the full address of work or home are taken all together from a
     * given contact)
     * - `homeAddress`
     * - `deleted`
     * - `optOut.date`
     * - `_links.linkedInUrl`
     * - `_links.facebookUrl`
     * - `_links.twitterUrl`
     * - `customField.{uuid of custom field}`
     * * **events**: A conflict arises for an event if it contains attendees associated with at least two
     * of the provided contacts.
     * - You must provide the ID of the conflicted event and the ID of the contact whose attendee is to be
     * associated with the merged contact to continue with the merge.
     * - Attendees with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
     * step will be deleted. Attendees with other statuses will be kept but will be associated with an
     * inactive contact.
     * 
     * <p>* **surveys**: A conflict arises for a survey containing respondents associated with at least two of
     * the provided contacts.
     * - You must provide the ID of the conflicted survey and the ID of the contact whose respondent will
     * be associated with the merged contact to continue with the merge.
     * - Respondents with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
     * step will be deleted. Respondents with other statuses will be kept but will be associated with an
     * inactive contact.
     * - Only legacy surveys support merge functionality at this time.
     * * **contactRelationships**: By default, the main contact's relationships are carried over to the
     * merged contact, and those of the other contact(s) are discarded. This is the default regardless of
     * whether the main contact has relationships or not.
     * - You can provide a secondary contact's ID to carry over its relationships instead of the main
     * contact. Only one contact's relationships can be carried over.
     */
    @SpeakeasyMetadata("request:mediaType=application/json")
    private ContactMerge contactMerge;

    @JsonCreator
    public MergeContactsRequest(
            @Nonnull String id,
            @Nonnull ContactMerge contactMerge) {
        this.id = Optional.ofNullable(id)
            .orElseThrow(() -> new IllegalArgumentException("id cannot be null"));
        this.contactMerge = Optional.ofNullable(contactMerge)
            .orElseThrow(() -> new IllegalArgumentException("contactMerge cannot be null"));
    }

    /**
     * ID of a contact.
     */
    public String id() {
        return this.id;
    }

    /**
     * Merge the 'main' contact with one or two other 'secondary' contacts. By default, the main contact's
     * values will be used for the merged contact if present. Otherwise, the secondary contact's value is
     * used.
     * 
     * <p>For contact memberships and contact profile image, only the main contact's data is used.
     * 
     * <p>When merging contacts that contain secure fields, the `event/contacts:write-sensitive` scope is
     * required.
     * 
     * <p>**Conflict Resolution**
     * 
     * <p>* **contactFields**: A conflict arises for a contact field if it is empty in the main contact, two
     * secondary contacts are provided, and both secondary contacts have values for the field.
     * - You must provide the fieldName of the conflicted field and the ID of the contact whose value is to
     * be kept to continue with the merge.
     * - This array can also be used to override the default behavior and choose which contact to pull
     * values from for each contact field.
     * - The following contact fields are supported:
     * - `sourceId`
     * - `firstName`
     * - `lastName`
     * - `prefix`
     * - `email`
     * - `ccEmail`
     * - `company`
     * - `type.id`
     * - `nickname`
     * - `title`
     * - `workPhone`
     * - `middleName`
     * - `mobilePhone`
     * - `gender`
     * - `npi`
     * - `homeFax`
     * - `designation`
     * - `homePhone`
     * - `pager`
     * - `workFax`
     * - `lastModifiedBy`
     * - `lastModified`
     * - `created`
     * - `createdBy`
     * - `workAddress` (for address fields, the full address of work or home are taken all together from a
     * given contact)
     * - `homeAddress`
     * - `deleted`
     * - `optOut.date`
     * - `_links.linkedInUrl`
     * - `_links.facebookUrl`
     * - `_links.twitterUrl`
     * - `customField.{uuid of custom field}`
     * * **events**: A conflict arises for an event if it contains attendees associated with at least two
     * of the provided contacts.
     * - You must provide the ID of the conflicted event and the ID of the contact whose attendee is to be
     * associated with the merged contact to continue with the merge.
     * - Attendees with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
     * step will be deleted. Attendees with other statuses will be kept but will be associated with an
     * inactive contact.
     * 
     * <p>* **surveys**: A conflict arises for a survey containing respondents associated with at least two of
     * the provided contacts.
     * - You must provide the ID of the conflicted survey and the ID of the contact whose respondent will
     * be associated with the merged contact to continue with the merge.
     * - Respondents with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
     * step will be deleted. Respondents with other statuses will be kept but will be associated with an
     * inactive contact.
     * - Only legacy surveys support merge functionality at this time.
     * * **contactRelationships**: By default, the main contact's relationships are carried over to the
     * merged contact, and those of the other contact(s) are discarded. This is the default regardless of
     * whether the main contact has relationships or not.
     * - You can provide a secondary contact's ID to carry over its relationships instead of the main
     * contact. Only one contact's relationships can be carried over.
     */
    public ContactMerge contactMerge() {
        return this.contactMerge;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * ID of a contact.
     */
    public MergeContactsRequest withId(@Nonnull String id) {
        this.id = Utils.checkNotNull(id, "id");
        return this;
    }


    /**
     * Merge the 'main' contact with one or two other 'secondary' contacts. By default, the main contact's
     * values will be used for the merged contact if present. Otherwise, the secondary contact's value is
     * used.
     * 
     * <p>For contact memberships and contact profile image, only the main contact's data is used.
     * 
     * <p>When merging contacts that contain secure fields, the `event/contacts:write-sensitive` scope is
     * required.
     * 
     * <p>**Conflict Resolution**
     * 
     * <p>* **contactFields**: A conflict arises for a contact field if it is empty in the main contact, two
     * secondary contacts are provided, and both secondary contacts have values for the field.
     * - You must provide the fieldName of the conflicted field and the ID of the contact whose value is to
     * be kept to continue with the merge.
     * - This array can also be used to override the default behavior and choose which contact to pull
     * values from for each contact field.
     * - The following contact fields are supported:
     * - `sourceId`
     * - `firstName`
     * - `lastName`
     * - `prefix`
     * - `email`
     * - `ccEmail`
     * - `company`
     * - `type.id`
     * - `nickname`
     * - `title`
     * - `workPhone`
     * - `middleName`
     * - `mobilePhone`
     * - `gender`
     * - `npi`
     * - `homeFax`
     * - `designation`
     * - `homePhone`
     * - `pager`
     * - `workFax`
     * - `lastModifiedBy`
     * - `lastModified`
     * - `created`
     * - `createdBy`
     * - `workAddress` (for address fields, the full address of work or home are taken all together from a
     * given contact)
     * - `homeAddress`
     * - `deleted`
     * - `optOut.date`
     * - `_links.linkedInUrl`
     * - `_links.facebookUrl`
     * - `_links.twitterUrl`
     * - `customField.{uuid of custom field}`
     * * **events**: A conflict arises for an event if it contains attendees associated with at least two
     * of the provided contacts.
     * - You must provide the ID of the conflicted event and the ID of the contact whose attendee is to be
     * associated with the merged contact to continue with the merge.
     * - Attendees with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
     * step will be deleted. Attendees with other statuses will be kept but will be associated with an
     * inactive contact.
     * 
     * <p>* **surveys**: A conflict arises for a survey containing respondents associated with at least two of
     * the provided contacts.
     * - You must provide the ID of the conflicted survey and the ID of the contact whose respondent will
     * be associated with the merged contact to continue with the merge.
     * - Respondents with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
     * step will be deleted. Respondents with other statuses will be kept but will be associated with an
     * inactive contact.
     * - Only legacy surveys support merge functionality at this time.
     * * **contactRelationships**: By default, the main contact's relationships are carried over to the
     * merged contact, and those of the other contact(s) are discarded. This is the default regardless of
     * whether the main contact has relationships or not.
     * - You can provide a secondary contact's ID to carry over its relationships instead of the main
     * contact. Only one contact's relationships can be carried over.
     */
    public MergeContactsRequest withContactMerge(@Nonnull ContactMerge contactMerge) {
        this.contactMerge = Utils.checkNotNull(contactMerge, "contactMerge");
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        MergeContactsRequest other = (MergeContactsRequest) o;
        return 
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.contactMerge, other.contactMerge);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            id, contactMerge);
    }
    
    @Override
    public String toString() {
        return Utils.toString(MergeContactsRequest.class,
                "id", id,
                "contactMerge", contactMerge);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String id;

        private ContactMerge contactMerge;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * ID of a contact.
         */
        public Builder id(@Nonnull String id) {
            this.id = Utils.checkNotNull(id, "id");
            return this;
        }

        /**
         * Merge the 'main' contact with one or two other 'secondary' contacts. By default, the main contact's
         * values will be used for the merged contact if present. Otherwise, the secondary contact's value is
         * used.
         * 
         * <p>For contact memberships and contact profile image, only the main contact's data is used.
         * 
         * <p>When merging contacts that contain secure fields, the `event/contacts:write-sensitive` scope is
         * required.
         * 
         * <p>**Conflict Resolution**
         * 
         * <p>* **contactFields**: A conflict arises for a contact field if it is empty in the main contact, two
         * secondary contacts are provided, and both secondary contacts have values for the field.
         * - You must provide the fieldName of the conflicted field and the ID of the contact whose value is to
         * be kept to continue with the merge.
         * - This array can also be used to override the default behavior and choose which contact to pull
         * values from for each contact field.
         * - The following contact fields are supported:
         * - `sourceId`
         * - `firstName`
         * - `lastName`
         * - `prefix`
         * - `email`
         * - `ccEmail`
         * - `company`
         * - `type.id`
         * - `nickname`
         * - `title`
         * - `workPhone`
         * - `middleName`
         * - `mobilePhone`
         * - `gender`
         * - `npi`
         * - `homeFax`
         * - `designation`
         * - `homePhone`
         * - `pager`
         * - `workFax`
         * - `lastModifiedBy`
         * - `lastModified`
         * - `created`
         * - `createdBy`
         * - `workAddress` (for address fields, the full address of work or home are taken all together from a
         * given contact)
         * - `homeAddress`
         * - `deleted`
         * - `optOut.date`
         * - `_links.linkedInUrl`
         * - `_links.facebookUrl`
         * - `_links.twitterUrl`
         * - `customField.{uuid of custom field}`
         * * **events**: A conflict arises for an event if it contains attendees associated with at least two
         * of the provided contacts.
         * - You must provide the ID of the conflicted event and the ID of the contact whose attendee is to be
         * associated with the merged contact to continue with the merge.
         * - Attendees with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
         * step will be deleted. Attendees with other statuses will be kept but will be associated with an
         * inactive contact.
         * 
         * <p>* **surveys**: A conflict arises for a survey containing respondents associated with at least two of
         * the provided contacts.
         * - You must provide the ID of the conflicted survey and the ID of the contact whose respondent will
         * be associated with the merged contact to continue with the merge.
         * - Respondents with a 'No Response' or 'Visited' status whose linked contacts are not chosen in this
         * step will be deleted. Respondents with other statuses will be kept but will be associated with an
         * inactive contact.
         * - Only legacy surveys support merge functionality at this time.
         * * **contactRelationships**: By default, the main contact's relationships are carried over to the
         * merged contact, and those of the other contact(s) are discarded. This is the default regardless of
         * whether the main contact has relationships or not.
         * - You can provide a secondary contact's ID to carry over its relationships instead of the main
         * contact. Only one contact's relationships can be carried over.
         */
        public Builder contactMerge(@Nonnull ContactMerge contactMerge) {
            this.contactMerge = Utils.checkNotNull(contactMerge, "contactMerge");
            return this;
        }

        public MergeContactsRequest build() {
            return new MergeContactsRequest(
                id, contactMerge);
        }

    }
}
