/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.operations;

import com.cvent.utils.SpeakeasyMetadata;
import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class GetUsageRequest {
    /**
     * Returns usage for the seven days before (inclusive) the date provided. If after is also provided,
     * then
     * only the dates between (inclusive) those dates are returned. before must be a valid ISO8601 date
     * stamp.
     * before and after must not be more then seven days apart.
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=before")
    private String before;

    /**
     * Returns usage for the seven days after (inclusive) the date provided. If before is also provided,
     * then
     * only the dates between (inclusive) those dates are returned. after must be a valid ISO8601 date
     * stamp.
     * after and before must not be more then seven days apart.
     */
    @SpeakeasyMetadata("queryParam:style=form,explode=true,name=after")
    private String after;

    @JsonCreator
    public GetUsageRequest(
            @Nullable String before,
            @Nullable String after) {
        this.before = before;
        this.after = after;
    }
    
    public GetUsageRequest() {
        this(null, null);
    }

    /**
     * Returns usage for the seven days before (inclusive) the date provided. If after is also provided,
     * then
     * only the dates between (inclusive) those dates are returned. before must be a valid ISO8601 date
     * stamp.
     * before and after must not be more then seven days apart.
     */
    public Optional<String> before() {
        return Optional.ofNullable(this.before);
    }

    /**
     * Returns usage for the seven days after (inclusive) the date provided. If before is also provided,
     * then
     * only the dates between (inclusive) those dates are returned. after must be a valid ISO8601 date
     * stamp.
     * after and before must not be more then seven days apart.
     */
    public Optional<String> after() {
        return Optional.ofNullable(this.after);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Returns usage for the seven days before (inclusive) the date provided. If after is also provided,
     * then
     * only the dates between (inclusive) those dates are returned. before must be a valid ISO8601 date
     * stamp.
     * before and after must not be more then seven days apart.
     */
    public GetUsageRequest withBefore(@Nullable String before) {
        this.before = before;
        return this;
    }


    /**
     * Returns usage for the seven days after (inclusive) the date provided. If before is also provided,
     * then
     * only the dates between (inclusive) those dates are returned. after must be a valid ISO8601 date
     * stamp.
     * after and before must not be more then seven days apart.
     */
    public GetUsageRequest withAfter(@Nullable String after) {
        this.after = after;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetUsageRequest other = (GetUsageRequest) o;
        return 
            Utils.enhancedDeepEquals(this.before, other.before) &&
            Utils.enhancedDeepEquals(this.after, other.after);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            before, after);
    }
    
    @Override
    public String toString() {
        return Utils.toString(GetUsageRequest.class,
                "before", before,
                "after", after);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String before;

        private String after;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Returns usage for the seven days before (inclusive) the date provided. If after is also provided,
         * then
         * only the dates between (inclusive) those dates are returned. before must be a valid ISO8601 date
         * stamp.
         * before and after must not be more then seven days apart.
         */
        public Builder before(@Nullable String before) {
            this.before = before;
            return this;
        }

        /**
         * Returns usage for the seven days after (inclusive) the date provided. If before is also provided,
         * then
         * only the dates between (inclusive) those dates are returned. after must be a valid ISO8601 date
         * stamp.
         * after and before must not be more then seven days apart.
         */
        public Builder after(@Nullable String after) {
            this.after = after;
            return this;
        }

        public GetUsageRequest build() {
            return new GetUsageRequest(
                before, after);
        }

    }
}
