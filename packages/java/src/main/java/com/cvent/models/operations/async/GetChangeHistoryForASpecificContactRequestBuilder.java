/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.operations.async;

import static com.cvent.operations.Operations.AsyncRequestOperation;
import static com.cvent.utils.Exceptions.unchecked;
import static com.cvent.utils.reactive.ReactiveUtils.mapAsync;

import com.cvent.SDKConfiguration;
import com.cvent.models.operations.GetChangeHistoryForASpecificContactRequest;
import com.cvent.operations.GetChangeHistoryForASpecificContact;
import com.cvent.utils.Blob;
import com.cvent.utils.Headers;
import com.cvent.utils.Options;
import com.cvent.utils.RetryConfig;
import com.cvent.utils.Utils;
import com.cvent.utils.pagination.AsyncPaginator;
import com.cvent.utils.pagination.CursorTracker;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import java.net.http.HttpResponse;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.Flow;
import org.reactivestreams.FlowAdapters;
import org.reactivestreams.Publisher;
import org.reactivestreams.Subscriber;

public class GetChangeHistoryForASpecificContactRequestBuilder {
    private final SDKConfiguration sdkConfiguration;
    private final Headers _headers = new Headers();
    private GetChangeHistoryForASpecificContactRequest request;
    private final Options.Builder optionsBuilder;

    public GetChangeHistoryForASpecificContactRequestBuilder(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.optionsBuilder = Options.builder();
    }

    public GetChangeHistoryForASpecificContactRequestBuilder retryConfig(RetryConfig retryConfig) {
        this.optionsBuilder.retryConfig(retryConfig);
        return this;
    }

    public GetChangeHistoryForASpecificContactRequestBuilder request(
            @Nonnull GetChangeHistoryForASpecificContactRequest request) {
        this.request = Utils.checkNotNull(request, "request");
        return this;
    }

    private GetChangeHistoryForASpecificContactRequest _buildRequest() {
        return this.request;
    }

    public GetChangeHistoryForASpecificContactRequestBuilder header(String name, String value) {
        Utils.checkNotNull(name, "name");
        Utils.checkNotNull(value, "value");
        this._headers.add(name, value);
        return this;
    }

    /**
     * Executes the request and returns the response.
     *
     * @return The response from the server.
     */
    public CompletableFuture<GetChangeHistoryForASpecificContactResponse> call() {
        Options options = optionsBuilder.build();
        AsyncRequestOperation<GetChangeHistoryForASpecificContactRequest, GetChangeHistoryForASpecificContactResponse> operation =
                new GetChangeHistoryForASpecificContact.Async(
                        sdkConfiguration, options, sdkConfiguration.retryScheduler(), _headers);
        return operation.doRequest(this._buildRequest()).thenCompose(operation::handleResponse);
    }

    /**
     * Returns a {@link Publisher} that performs next page calls till no more pages
     * are returned.
     *
     * <p>The returned {@link Publisher} can be used with reactive frameworks:
     * <pre><code>
     * Publisher&lt;GetChangeHistoryForASpecificContactResponse&gt; publisher = builder.callAsPublisher();
     * publisher.subscribe(new Subscriber&lt;GetChangeHistoryForASpecificContactResponse&gt;() {
     *     // Handle onNext, onError, onComplete
     * });
     * </code></pre>
     *
     * @return A {@link Publisher} that emits pages asynchronously
     */
    public Publisher<GetChangeHistoryForASpecificContactResponse> callAsPublisher() {
        GetChangeHistoryForASpecificContactRequest request = this.request;
        Options options = optionsBuilder.build();
        AsyncRequestOperation<GetChangeHistoryForASpecificContactRequest, GetChangeHistoryForASpecificContactResponse> operation =
                new GetChangeHistoryForASpecificContact.Async(
                        sdkConfiguration, options, sdkConfiguration.retryScheduler(), _headers);

        Flow.Publisher<HttpResponse<Blob>> asyncPaginator = new AsyncPaginator<>(
                request, new CursorTracker<>("$.paging.nextToken", String.class), (req, pos) -> {
                    var modifiedReq = pos == null ? req : req.withToken(pos);
                    return operation.doRequest(modifiedReq);
                });

        Flow.Publisher<GetChangeHistoryForASpecificContactResponse> flowPublisher =
                mapAsync(asyncPaginator, operation::handleResponse);

        // Convert Flow.Publisher to Reactive Streams Publisher at the last stage
        return FlowAdapters.toPublisher(flowPublisher);
    }
}
