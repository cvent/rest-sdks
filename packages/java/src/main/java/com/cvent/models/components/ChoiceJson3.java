/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.components;

import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;

/**
 * ChoiceJson3
 *
 * <p>This is used to denote the choice of custom field.
 */
public class ChoiceJson3 {
    /**
     * The ID of the custom field choice. If you supply the choice's existing ID in a PUT call, the choice
     * keeps its current ID and the choice text is updated. If this `id` field is left blank in a PUT call,
     * this choice text will be replaced with the text supplied, and a new choice ID is created.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("id")
    private String id;

    /**
     * The text for the custom field choice.
     */
    @JsonProperty("text")
    private String text;

    @JsonCreator
    public ChoiceJson3(@JsonProperty("id") @Nullable String id, @JsonProperty("text") @Nonnull String text) {
        this.id = id;
        this.text = Optional.ofNullable(text).orElseThrow(() -> new IllegalArgumentException("text cannot be null"));
    }

    public ChoiceJson3(@Nonnull String text) {
        this(null, text);
    }

    /**
     * The ID of the custom field choice. If you supply the choice's existing ID in a PUT call, the choice
     * keeps its current ID and the choice text is updated. If this `id` field is left blank in a PUT call,
     * this choice text will be replaced with the text supplied, and a new choice ID is created.
     */
    public Optional<String> id() {
        return Optional.ofNullable(this.id);
    }

    /**
     * The text for the custom field choice.
     */
    public String text() {
        return this.text;
    }

    public static Builder builder() {
        return new Builder();
    }

    /**
     * The ID of the custom field choice. If you supply the choice's existing ID in a PUT call, the choice
     * keeps its current ID and the choice text is updated. If this `id` field is left blank in a PUT call,
     * this choice text will be replaced with the text supplied, and a new choice ID is created.
     */
    public ChoiceJson3 withId(@Nullable String id) {
        this.id = id;
        return this;
    }

    /**
     * The text for the custom field choice.
     */
    public ChoiceJson3 withText(@Nonnull String text) {
        this.text = Utils.checkNotNull(text, "text");
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ChoiceJson3 other = (ChoiceJson3) o;
        return Utils.enhancedDeepEquals(this.id, other.id) && Utils.enhancedDeepEquals(this.text, other.text);
    }

    @Override
    public int hashCode() {
        return Utils.enhancedHash(id, text);
    }

    @Override
    public String toString() {
        return Utils.toString(ChoiceJson3.class, "id", id, "text", text);
    }

    @SuppressWarnings("UnusedReturnValue")
    public static final class Builder {

        private String id;

        private String text;

        private Builder() {
            // force use of static builder() method
        }

        /**
         * The ID of the custom field choice. If you supply the choice's existing ID in a PUT call, the choice
         * keeps its current ID and the choice text is updated. If this `id` field is left blank in a PUT call,
         * this choice text will be replaced with the text supplied, and a new choice ID is created.
         */
        public Builder id(@Nullable String id) {
            this.id = id;
            return this;
        }

        /**
         * The text for the custom field choice.
         */
        public Builder text(@Nonnull String text) {
            this.text = Utils.checkNotNull(text, "text");
            return this;
        }

        public ChoiceJson3 build() {
            return new ChoiceJson3(id, text);
        }
    }
}
