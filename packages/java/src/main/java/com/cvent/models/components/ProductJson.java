/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.components;

import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;

/**
 * ProductJson
 *
 * <p>Represents a product. Products are items that can be purchased during registration.
 */
public class ProductJson {
    /**
     * ID of the associated product.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("id")
    private String id;

    /**
     * Represents a product's type. `AdmissionItem`: Product is a ticket to an event, typically denotes the
     * day(s) the individual is attending. Every attendee must have an admission item.
     *
     * <p>`Session`: Product is a agenda item at the event. Typically denotes a talk, presentation, or other
     * scheduled occurrence at the event. `SessionBundle`: Product is a session bundle that enrolls the
     * attendee into all the sessions included in the bundle, sometimes at a discounted rate.
     *
     * <p>`QuantityItem`: Product is a quantity of things an attendee is purchasing at an event, like t-shirts
     * or raffle tickets.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("type")
    private ProductTypeJson type;

    /**
     * Name of the associated product.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("name")
    private String name;

    @JsonCreator
    public ProductJson(
            @JsonProperty("id") @Nullable String id,
            @JsonProperty("type") @Nullable ProductTypeJson type,
            @JsonProperty("name") @Nullable String name) {
        this.id = id;
        this.type = type;
        this.name = name;
    }

    public ProductJson() {
        this(null, null, null);
    }

    /**
     * ID of the associated product.
     */
    public Optional<String> id() {
        return Optional.ofNullable(this.id);
    }

    /**
     * Represents a product's type. `AdmissionItem`: Product is a ticket to an event, typically denotes the
     * day(s) the individual is attending. Every attendee must have an admission item.
     *
     * <p>`Session`: Product is a agenda item at the event. Typically denotes a talk, presentation, or other
     * scheduled occurrence at the event. `SessionBundle`: Product is a session bundle that enrolls the
     * attendee into all the sessions included in the bundle, sometimes at a discounted rate.
     *
     * <p>`QuantityItem`: Product is a quantity of things an attendee is purchasing at an event, like t-shirts
     * or raffle tickets.
     */
    public Optional<ProductTypeJson> type() {
        return Optional.ofNullable(this.type);
    }

    /**
     * Name of the associated product.
     */
    public Optional<String> name() {
        return Optional.ofNullable(this.name);
    }

    public static Builder builder() {
        return new Builder();
    }

    /**
     * ID of the associated product.
     */
    public ProductJson withId(@Nullable String id) {
        this.id = id;
        return this;
    }

    /**
     * Represents a product's type. `AdmissionItem`: Product is a ticket to an event, typically denotes the
     * day(s) the individual is attending. Every attendee must have an admission item.
     *
     * <p>`Session`: Product is a agenda item at the event. Typically denotes a talk, presentation, or other
     * scheduled occurrence at the event. `SessionBundle`: Product is a session bundle that enrolls the
     * attendee into all the sessions included in the bundle, sometimes at a discounted rate.
     *
     * <p>`QuantityItem`: Product is a quantity of things an attendee is purchasing at an event, like t-shirts
     * or raffle tickets.
     */
    public ProductJson withType(@Nullable ProductTypeJson type) {
        this.type = type;
        return this;
    }

    /**
     * Name of the associated product.
     */
    public ProductJson withName(@Nullable String name) {
        this.name = name;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ProductJson other = (ProductJson) o;
        return Utils.enhancedDeepEquals(this.id, other.id)
                && Utils.enhancedDeepEquals(this.type, other.type)
                && Utils.enhancedDeepEquals(this.name, other.name);
    }

    @Override
    public int hashCode() {
        return Utils.enhancedHash(id, type, name);
    }

    @Override
    public String toString() {
        return Utils.toString(ProductJson.class, "id", id, "type", type, "name", name);
    }

    @SuppressWarnings("UnusedReturnValue")
    public static final class Builder {

        private String id;

        private ProductTypeJson type;

        private String name;

        private Builder() {
            // force use of static builder() method
        }

        /**
         * ID of the associated product.
         */
        public Builder id(@Nullable String id) {
            this.id = id;
            return this;
        }

        /**
         * Represents a product's type. `AdmissionItem`: Product is a ticket to an event, typically denotes the
         * day(s) the individual is attending. Every attendee must have an admission item.
         *
         * <p>`Session`: Product is a agenda item at the event. Typically denotes a talk, presentation, or other
         * scheduled occurrence at the event. `SessionBundle`: Product is a session bundle that enrolls the
         * attendee into all the sessions included in the bundle, sometimes at a discounted rate.
         *
         * <p>`QuantityItem`: Product is a quantity of things an attendee is purchasing at an event, like t-shirts
         * or raffle tickets.
         */
        public Builder type(@Nullable ProductTypeJson type) {
            this.type = type;
            return this;
        }

        /**
         * Name of the associated product.
         */
        public Builder name(@Nullable String name) {
            this.name = name;
            return this;
        }

        public ProductJson build() {
            return new ProductJson(id, type, name);
        }
    }
}
