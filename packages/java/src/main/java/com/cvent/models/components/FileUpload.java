/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.components;

import com.cvent.utils.SpeakeasyMetadata;
import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;

/**
 * FileUpload
 * 
 * <p>File Upload Object
 */
public class FileUpload {
    /**
     * Allowed File Types: ai, bmp, doc, docx, eml, eps, gif, ics, jpeg, msg, otf, ost, pdf, png, ppt,
     * pptx, tif, tiff, ttf, txt, woff, woff2, xls, xlsx
     */
    @SpeakeasyMetadata("multipartForm:file,name=file")
    private FileUploadFile file;

    @JsonCreator
    public FileUpload(
            @Nullable FileUploadFile file) {
        this.file = file;
    }
    
    public FileUpload() {
        this(null);
    }

    /**
     * Allowed File Types: ai, bmp, doc, docx, eml, eps, gif, ics, jpeg, msg, otf, ost, pdf, png, ppt,
     * pptx, tif, tiff, ttf, txt, woff, woff2, xls, xlsx
     */
    public Optional<FileUploadFile> file() {
        return Optional.ofNullable(this.file);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Allowed File Types: ai, bmp, doc, docx, eml, eps, gif, ics, jpeg, msg, otf, ost, pdf, png, ppt,
     * pptx, tif, tiff, ttf, txt, woff, woff2, xls, xlsx
     */
    public FileUpload withFile(@Nullable FileUploadFile file) {
        this.file = file;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        FileUpload other = (FileUpload) o;
        return 
            Utils.enhancedDeepEquals(this.file, other.file);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            file);
    }
    
    @Override
    public String toString() {
        return Utils.toString(FileUpload.class,
                "file", file);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private FileUploadFile file;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Allowed File Types: ai, bmp, doc, docx, eml, eps, gif, ics, jpeg, msg, otf, ost, pdf, png, ppt,
         * pptx, tif, tiff, ttf, txt, woff, woff2, xls, xlsx
         */
        public Builder file(@Nullable FileUploadFile file) {
            this.file = file;
            return this;
        }

        public FileUpload build() {
            return new FileUpload(
                file);
        }

    }
}
