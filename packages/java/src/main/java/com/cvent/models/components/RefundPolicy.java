/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.components;

import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nullable;
import java.lang.Double;
import java.lang.Override;
import java.lang.String;
import java.time.LocalDate;
import java.util.Optional;

/**
 * RefundPolicy
 *
 * <p>Refund policy associated to a fee.
 */
public class RefundPolicy {
    /**
     * The total refund amount when the related item is canceled from the attendee's agenda.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("amount")
    private Double amount;

    /**
     * The total refund percentage when the related item is canceled from the attendee's agenda. Percentage
     * is returned only if the refund policy is `RefundByPercentage`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("percentage")
    private Double percentage;

    /**
     * The type of refund policy for the fee. `RefundByFixedAmount`: Returns a fixed amount to the attendee
     * as chosen by the planner. `RefundByCancellationAmount`: Refunds everything to the attendee minus an
     * cancellation fee chosen by the planner.
     *
     * <p>`RefundByPercentage`: Refunds a percentage of the amount as chosen by the planner.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("type")
    private RefundType type;

    /**
     * Attendees must cancel by this ISO 8601 formatted date to receive the type of refund. Otherwise,
     * nothing is refunded.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("cancelByDate")
    private LocalDate cancelByDate;

    @JsonCreator
    public RefundPolicy(
            @JsonProperty("amount") @Nullable Double amount,
            @JsonProperty("percentage") @Nullable Double percentage,
            @JsonProperty("type") @Nullable RefundType type,
            @JsonProperty("cancelByDate") @Nullable LocalDate cancelByDate) {
        this.amount = amount;
        this.percentage = percentage;
        this.type = type;
        this.cancelByDate = cancelByDate;
    }

    public RefundPolicy() {
        this(null, null, null, null);
    }

    /**
     * The total refund amount when the related item is canceled from the attendee's agenda.
     */
    public Optional<Double> amount() {
        return Optional.ofNullable(this.amount);
    }

    /**
     * The total refund percentage when the related item is canceled from the attendee's agenda. Percentage
     * is returned only if the refund policy is `RefundByPercentage`.
     */
    public Optional<Double> percentage() {
        return Optional.ofNullable(this.percentage);
    }

    /**
     * The type of refund policy for the fee. `RefundByFixedAmount`: Returns a fixed amount to the attendee
     * as chosen by the planner. `RefundByCancellationAmount`: Refunds everything to the attendee minus an
     * cancellation fee chosen by the planner.
     *
     * <p>`RefundByPercentage`: Refunds a percentage of the amount as chosen by the planner.
     */
    public Optional<RefundType> type() {
        return Optional.ofNullable(this.type);
    }

    /**
     * Attendees must cancel by this ISO 8601 formatted date to receive the type of refund. Otherwise,
     * nothing is refunded.
     */
    public Optional<LocalDate> cancelByDate() {
        return Optional.ofNullable(this.cancelByDate);
    }

    public static Builder builder() {
        return new Builder();
    }

    /**
     * The total refund amount when the related item is canceled from the attendee's agenda.
     */
    public RefundPolicy withAmount(@Nullable Double amount) {
        this.amount = amount;
        return this;
    }

    /**
     * The total refund percentage when the related item is canceled from the attendee's agenda. Percentage
     * is returned only if the refund policy is `RefundByPercentage`.
     */
    public RefundPolicy withPercentage(@Nullable Double percentage) {
        this.percentage = percentage;
        return this;
    }

    /**
     * The type of refund policy for the fee. `RefundByFixedAmount`: Returns a fixed amount to the attendee
     * as chosen by the planner. `RefundByCancellationAmount`: Refunds everything to the attendee minus an
     * cancellation fee chosen by the planner.
     *
     * <p>`RefundByPercentage`: Refunds a percentage of the amount as chosen by the planner.
     */
    public RefundPolicy withType(@Nullable RefundType type) {
        this.type = type;
        return this;
    }

    /**
     * Attendees must cancel by this ISO 8601 formatted date to receive the type of refund. Otherwise,
     * nothing is refunded.
     */
    public RefundPolicy withCancelByDate(@Nullable LocalDate cancelByDate) {
        this.cancelByDate = cancelByDate;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        RefundPolicy other = (RefundPolicy) o;
        return Utils.enhancedDeepEquals(this.amount, other.amount)
                && Utils.enhancedDeepEquals(this.percentage, other.percentage)
                && Utils.enhancedDeepEquals(this.type, other.type)
                && Utils.enhancedDeepEquals(this.cancelByDate, other.cancelByDate);
    }

    @Override
    public int hashCode() {
        return Utils.enhancedHash(amount, percentage, type, cancelByDate);
    }

    @Override
    public String toString() {
        return Utils.toString(
                RefundPolicy.class,
                "amount",
                amount,
                "percentage",
                percentage,
                "type",
                type,
                "cancelByDate",
                cancelByDate);
    }

    @SuppressWarnings("UnusedReturnValue")
    public static final class Builder {

        private Double amount;

        private Double percentage;

        private RefundType type;

        private LocalDate cancelByDate;

        private Builder() {
            // force use of static builder() method
        }

        /**
         * The total refund amount when the related item is canceled from the attendee's agenda.
         */
        public Builder amount(@Nullable Double amount) {
            this.amount = amount;
            return this;
        }

        /**
         * The total refund percentage when the related item is canceled from the attendee's agenda. Percentage
         * is returned only if the refund policy is `RefundByPercentage`.
         */
        public Builder percentage(@Nullable Double percentage) {
            this.percentage = percentage;
            return this;
        }

        /**
         * The type of refund policy for the fee. `RefundByFixedAmount`: Returns a fixed amount to the attendee
         * as chosen by the planner. `RefundByCancellationAmount`: Refunds everything to the attendee minus an
         * cancellation fee chosen by the planner.
         *
         * <p>`RefundByPercentage`: Refunds a percentage of the amount as chosen by the planner.
         */
        public Builder type(@Nullable RefundType type) {
            this.type = type;
            return this;
        }

        /**
         * Attendees must cancel by this ISO 8601 formatted date to receive the type of refund. Otherwise,
         * nothing is refunded.
         */
        public Builder cancelByDate(@Nullable LocalDate cancelByDate) {
            this.cancelByDate = cancelByDate;
            return this;
        }

        public RefundPolicy build() {
            return new RefundPolicy(amount, percentage, type, cancelByDate);
        }
    }
}
