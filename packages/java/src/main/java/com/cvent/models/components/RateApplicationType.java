/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.components;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * RateApplicationType
 *
 * <p>How the rate is applied to the meeting room booking.
 *
 * <p>Values:
 * - `COMPLIMENTARY` — Complimentary
 * - `INCLUDED` — Included
 * - `ONETIME_FEE` — One-Time Fee
 * - `PER_DAY` — Per Day
 * - `PER_HOUR` — Per Hour
 * - `PER_SQ_FT` — Per Square Foot
 * - `PER_SQ_MTR` — Per Square Meter
 * - `TOTAL` — Total
 */
public enum RateApplicationType {
    COMPLIMENTARY("COMPLIMENTARY"),
    INCLUDED("INCLUDED"),
    ONETIME_FEE("ONETIME_FEE"),
    PER_DAY("PER_DAY"),
    PER_HOUR("PER_HOUR"),
    PER_SQ_FT("PER_SQ_FT"),
    PER_SQ_MTR("PER_SQ_MTR"),
    TOTAL("TOTAL");

    @JsonValue
    private final String value;

    RateApplicationType(String value) {
        this.value = value;
    }

    public String value() {
        return value;
    }

    public static Optional<RateApplicationType> fromValue(String value) {
        for (RateApplicationType o : RateApplicationType.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}
