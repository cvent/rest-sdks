/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.components;

import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.List;
import java.util.Optional;

/**
 * BudgetEstimate
 *
 * <p>The estimated budget of the Meeting Request.
 */
public class BudgetEstimate {
    /**
     * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
     * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
     * not allowed for the account during the creation or update of the Meeting Request.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("currency")
    private String currency;

    /**
     * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
     * costs are based on a quantity.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("costType")
    private BudgetCostType costType;

    /**
     * A list of budget entry details. Each entry consists of the budget category or sub-category, the
     * number of units, the cost per unit, and the total amount.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("costDetail")
    private List<BudgetCostDetail12> costDetail;

    @JsonCreator
    public BudgetEstimate(
            @JsonProperty("currency") @Nullable String currency,
            @JsonProperty("costType") @Nullable BudgetCostType costType,
            @JsonProperty("costDetail") @Nullable List<BudgetCostDetail12> costDetail) {
        this.currency = currency;
        this.costType = costType;
        this.costDetail = costDetail;
    }

    public BudgetEstimate() {
        this(null, null, null);
    }

    /**
     * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
     * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
     * not allowed for the account during the creation or update of the Meeting Request.
     */
    public Optional<String> currency() {
        return Optional.ofNullable(this.currency);
    }

    /**
     * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
     * costs are based on a quantity.
     */
    public Optional<BudgetCostType> costType() {
        return Optional.ofNullable(this.costType);
    }

    /**
     * A list of budget entry details. Each entry consists of the budget category or sub-category, the
     * number of units, the cost per unit, and the total amount.
     */
    public Optional<List<BudgetCostDetail12>> costDetail() {
        return Optional.ofNullable(this.costDetail);
    }

    public static Builder builder() {
        return new Builder();
    }

    /**
     * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
     * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
     * not allowed for the account during the creation or update of the Meeting Request.
     */
    public BudgetEstimate withCurrency(@Nullable String currency) {
        this.currency = currency;
        return this;
    }

    /**
     * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
     * costs are based on a quantity.
     */
    public BudgetEstimate withCostType(@Nullable BudgetCostType costType) {
        this.costType = costType;
        return this;
    }

    /**
     * A list of budget entry details. Each entry consists of the budget category or sub-category, the
     * number of units, the cost per unit, and the total amount.
     */
    public BudgetEstimate withCostDetail(@Nullable List<BudgetCostDetail12> costDetail) {
        this.costDetail = costDetail;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        BudgetEstimate other = (BudgetEstimate) o;
        return Utils.enhancedDeepEquals(this.currency, other.currency)
                && Utils.enhancedDeepEquals(this.costType, other.costType)
                && Utils.enhancedDeepEquals(this.costDetail, other.costDetail);
    }

    @Override
    public int hashCode() {
        return Utils.enhancedHash(currency, costType, costDetail);
    }

    @Override
    public String toString() {
        return Utils.toString(
                BudgetEstimate.class, "currency", currency, "costType", costType, "costDetail", costDetail);
    }

    @SuppressWarnings("UnusedReturnValue")
    public static final class Builder {

        private String currency;

        private BudgetCostType costType;

        private List<BudgetCostDetail12> costDetail;

        private Builder() {
            // force use of static builder() method
        }

        /**
         * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
         * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
         * not allowed for the account during the creation or update of the Meeting Request.
         */
        public Builder currency(@Nullable String currency) {
            this.currency = currency;
            return this;
        }

        /**
         * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
         * costs are based on a quantity.
         */
        public Builder costType(@Nullable BudgetCostType costType) {
            this.costType = costType;
            return this;
        }

        /**
         * A list of budget entry details. Each entry consists of the budget category or sub-category, the
         * number of units, the cost per unit, and the total amount.
         */
        public Builder costDetail(@Nullable List<BudgetCostDetail12> costDetail) {
            this.costDetail = costDetail;
            return this;
        }

        public BudgetEstimate build() {
            return new BudgetEstimate(currency, costType, costDetail);
        }
    }
}
