/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.components;

import com.cvent.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.List;
import java.util.Optional;

/**
 * BudgetEstimateJson
 * 
 * <p>The estimated budget of the Meeting Request.
 */
public class BudgetEstimateJson {
    /**
     * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
     * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
     * not allowed for the account during the creation or update of the Meeting Request.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("currency")
    private String currency;

    /**
     * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
     * costs are based on a quantity.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("costType")
    private BudgetCostTypeJson costType;

    /**
     * A list of budget entry details. Each entry consists of the budget category or sub-category, the
     * number of units, the cost per unit, and the total amount.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("costDetail")
    private List<BudgetCostDetailJson1> costDetail;

    @JsonCreator
    public BudgetEstimateJson(
            @JsonProperty("currency") @Nullable String currency,
            @JsonProperty("costType") @Nullable BudgetCostTypeJson costType,
            @JsonProperty("costDetail") @Nullable List<BudgetCostDetailJson1> costDetail) {
        this.currency = currency;
        this.costType = costType;
        this.costDetail = costDetail;
    }
    
    public BudgetEstimateJson() {
        this(null, null, null);
    }

    /**
     * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
     * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
     * not allowed for the account during the creation or update of the Meeting Request.
     */
    public Optional<String> currency() {
        return Optional.ofNullable(this.currency);
    }

    /**
     * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
     * costs are based on a quantity.
     */
    public Optional<BudgetCostTypeJson> costType() {
        return Optional.ofNullable(this.costType);
    }

    /**
     * A list of budget entry details. Each entry consists of the budget category or sub-category, the
     * number of units, the cost per unit, and the total amount.
     */
    public Optional<List<BudgetCostDetailJson1>> costDetail() {
        return Optional.ofNullable(this.costDetail);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
     * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
     * not allowed for the account during the creation or update of the Meeting Request.
     */
    public BudgetEstimateJson withCurrency(@Nullable String currency) {
        this.currency = currency;
        return this;
    }


    /**
     * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
     * costs are based on a quantity.
     */
    public BudgetEstimateJson withCostType(@Nullable BudgetCostTypeJson costType) {
        this.costType = costType;
        return this;
    }


    /**
     * A list of budget entry details. Each entry consists of the budget category or sub-category, the
     * number of units, the cost per unit, and the total amount.
     */
    public BudgetEstimateJson withCostDetail(@Nullable List<BudgetCostDetailJson1> costDetail) {
        this.costDetail = costDetail;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        BudgetEstimateJson other = (BudgetEstimateJson) o;
        return 
            Utils.enhancedDeepEquals(this.currency, other.currency) &&
            Utils.enhancedDeepEquals(this.costType, other.costType) &&
            Utils.enhancedDeepEquals(this.costDetail, other.costDetail);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            currency, costType, costDetail);
    }
    
    @Override
    public String toString() {
        return Utils.toString(BudgetEstimateJson.class,
                "currency", currency,
                "costType", costType,
                "costDetail", costDetail);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String currency;

        private BudgetCostTypeJson costType;

        private List<BudgetCostDetailJson1> costDetail;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * The ISO 4217 standard format currency code used for this budget. Note: the allowed currencies are
         * configured per account. A **BAD REQUEST (400)** error will be returned if the specified currency is
         * not allowed for the account during the creation or update of the Meeting Request.
         */
        public Builder currency(@Nullable String currency) {
            this.currency = currency;
            return this;
        }

        /**
         * Denotes the cost type of a budget item. FIXED costs stay the same regardless of quantity. VARIABLE
         * costs are based on a quantity.
         */
        public Builder costType(@Nullable BudgetCostTypeJson costType) {
            this.costType = costType;
            return this;
        }

        /**
         * A list of budget entry details. Each entry consists of the budget category or sub-category, the
         * number of units, the cost per unit, and the total amount.
         */
        public Builder costDetail(@Nullable List<BudgetCostDetailJson1> costDetail) {
            this.costDetail = costDetail;
            return this;
        }

        public BudgetEstimateJson build() {
            return new BudgetEstimateJson(
                currency, costType, costDetail);
        }

    }
}
