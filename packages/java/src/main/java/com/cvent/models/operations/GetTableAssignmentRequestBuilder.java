/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.cvent.models.operations;

import static com.cvent.operations.Operations.RequestOperation;
import static com.cvent.utils.Exceptions.unchecked;
import static com.cvent.utils.Utils.toStream;
import static com.cvent.utils.Utils.transform;

import com.cvent.SDKConfiguration;
import com.cvent.operations.GetTableAssignment;
import com.cvent.utils.Headers;
import com.cvent.utils.Utils;
import com.cvent.utils.pagination.CursorTracker;
import com.cvent.utils.pagination.Paginator;
import jakarta.annotation.Nonnull;
import java.io.InputStream;
import java.lang.Iterable;
import java.lang.String;
import java.net.http.HttpResponse;
import java.util.Iterator;
import java.util.stream.Stream;

public class GetTableAssignmentRequestBuilder {
    private final SDKConfiguration sdkConfiguration;
    private final Headers _headers = new Headers();
    private GetTableAssignmentRequest request;

    public GetTableAssignmentRequestBuilder(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
    }

    public GetTableAssignmentRequestBuilder request(@Nonnull GetTableAssignmentRequest request) {
        this.request = Utils.checkNotNull(request, "request");
        return this;
    }

    private GetTableAssignmentRequest _buildRequest() {
        return this.request;
    }

    public GetTableAssignmentRequestBuilder header(String name, String value) {
        Utils.checkNotNull(name, "name");
        Utils.checkNotNull(value, "value");
        this._headers.add(name, value);
        return this;
    }

    /**
     * Executes the request and returns the response.
     *
     * @return The response from the server.
     */
    public GetTableAssignmentResponse call() {
        RequestOperation<GetTableAssignmentRequest, GetTableAssignmentResponse> operation =
                new GetTableAssignment.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(this._buildRequest()));
    }

    /**
     * Returns an iterable that performs next page calls till no more pages
     * are returned.
     *
     * <p>The returned iterable can be used in a for-each loop:
     * <pre><code>
     * for (GetTableAssignmentResponse page : builder.callAsIterable()) {
     *     // Process each page
     * }
     * </code></pre>
     *
     * @return An iterable that can be used to iterate through all pages
     */
    public Iterable<GetTableAssignmentResponse> callAsIterable() {
        GetTableAssignmentRequest request = this.request;
        RequestOperation<GetTableAssignmentRequest, GetTableAssignmentResponse> operation =
                new GetTableAssignment.Sync(sdkConfiguration, _headers);

        Iterator<HttpResponse<InputStream>> iterator = new Paginator<>(
                request, new CursorTracker<>("$.paging.nextToken", String.class), (req, pos) -> {
                    var modifiedReq = pos == null ? req : req.withToken(pos);
                    return unchecked(() -> operation.doRequest(modifiedReq)).get();
                });

        return () -> transform(iterator, operation::handleResponse);
    }

    /**
     * Returns a stream that performs next page calls till no more pages
     * are returned.
     **/
    public Stream<GetTableAssignmentResponse> callAsStream() {
        return toStream(callAsIterable());
    }
}
